# 账本项目协作规则

## 项目范围

- 本工作区默认服务于 Local-First Trading Ledger，包括开发、Git、架构讨论、日志和论文素材。
- 用户直接描述账本任务，或使用“账本写代码 / 账本 Git / 账本讨论 / 账本继续 / 账本日志”时，按 `.agents/skills/ledger-workflow/SKILL.md` 执行。
- 开始任务时先判断工作模式，再读取最小必要上下文；不要为了熟悉项目遍历整个工作区。

## 固定入口

1. 动态进度：`01一些进度/日志/00-当前开发状态.md`
2. 长期产品源码：`01一些进度/产出/LocalFirstTradingLedger/`（`main`）
3. 论文源码：`01一些进度/产出/LocalFirstTradingLedger-CS2026/`（`CS2026`）
4. 当前周计划：`01一些进度/日志/week*_*/`
5. 长期原则：`00开始前的一些准备文件/00A-全栈之路AI助手使用指南.md`
6. 系统设计与使用说明：`99_tool/`

只在任务需要时继续读取相关代码、测试、设计或历史日志。

## 三个维护面与两个 Git 仓库

AI 日常按三个工作面维护项目，但必须保留真实的 Git 边界：

| 维护面 | 路径 | 所属 Git 仓库／分支 | 用途 |
| --- | --- | --- | --- |
| 开发日志与计划 | 工作区根目录 | 根文档仓库 `main` | 状态、Week 计划、日志、论文素材与协作规则 |
| 长期账本产品 | `01一些进度/产出/LocalFirstTradingLedger/` | 源码仓库 `main` worktree | 长期个人账本 |
| 毕业论文项目 | `01一些进度/产出/LocalFirstTradingLedger-CS2026/` | 同一源码仓库 `CS2026` worktree | 论文实现、实验与导师证据 |

- 不得把三个维护面误报为三个 Git 仓库；实际是一个根文档仓库，加一个具有两个长期 worktree 的源码仓库。
- 一次任务只选择一条源码轨道；根日志仅在实质进展需要留痕时随之更新，不因普通代码变动自动制造日志。
- 若同一任务同时影响根文档和选定的源码轨道，必须分别检查 `status`、分支和 diff，分别验证和汇报；只有在用户授权提交或推送时，才分别执行对应操作。不得把根文档与嵌套源码变更混成一个提交。

## 目录与读取边界

- `00frappe-books-typescript/` 是外部参考项目，不是用户账本源码。默认不读，只在明确比较架构或用户要求时读取相关文件。
- `02_NLP/` 是后续要接入账本的模块。非 NLP 任务默认不读，也不要移动或改写。
- `*.canvas` 只在架构图、数据流或用户明确要求时读取。
- `00开始前的一些准备文件/99-私人网络环境说明.md` 永不主动读取、引用或提交。

## Git 边界

- 工作区根目录是文档与计划仓库，固定使用 `main`。
- 两个 `LocalFirstTradingLedger*` 目录是同一个独立源码仓库的两个 worktree：原目录固定使用 `main`，`LocalFirstTradingLedger-CS2026/` 固定使用 `CS2026`。
- 根文档仓库包含个人开发日志，远端永久保持私有；不得将其改为公开仓库。
- 源码仓库面向公开发布。公开前必须检查正式分支、标签、PR／Issue 元数据、Actions／Artifacts／Release 和密钥类内容；检查通过并取得用户授权后才可设为公开。
- 2026-08-22 历史清理采用风险接受：不提交 GitHub Support 清理工单，接受旧 PR 引用中仅剩的历史数字可能可见；这不授权未来把新的真实数据或敏感信息提交到 Git。
- `/Users/zhuzhen0131/Downloads/history_OKX/` 及其所有原始数据、转换脚本、中间表、报告和 B 文件是电脑本地私有区，永久位于两个 Git 仓库之外。不得复制、链接、暂存、提交、推送，也不得写入 PR、Issue、测试夹具、源码样例或开发日志。
- 只有用户明确要求真实数据转换或私有验收时才能读写上述本地私有区；对外汇报只保留不反推真实账户内容的汇总结果。
- 历史重写后，只能从清洁远端历史重建的本地仓库继续普通推送；不得从含旧历史的本地 Git 元数据 pull、merge 或 push 回远端。
- Git 任务先确认目标仓库，再查看该仓库的 `status`、分支、日志和 diff；不得混用两个仓库的提交。
- 工作区根仓库面向用户本人：README、计划、日志等文档和 Git commit 使用中文。
- 源码仓库的 Git commit 标题、模块说明、`AGENTS.md` 和 Release Notes 使用英文；长期产品 `main` 的根 `README.md` 与产品界面使用中文，服务本人长期使用。
- `CS2026` 是毕业论文版本，根 README 在内的整个受 Git 管理当前工作树必须使用英文，但历史中文提交不改写。
- 出现“论文”或 `CS2026` 时只进入论文 worktree；“继续账本”、只说“账本”或未提论文时只进入 `main` worktree。写入前必须验证实际路径和分支相符。
- 不创建 `main` 与 `CS2026` 之间的合并 PR。AI 不得自动 merge、rebase、cherry-pick、复制修复或同时修改两条分支；跨分支线索只在当周日志中记录“可能值得参考”，等待用户明确批准。
- 保留用户已有改动。除非用户明确要求，不使用破坏性 Git 命令，不擅自提交或推送。

## 版本分层与迁移

账本有四个互相独立的版本号：`fileFormatVersion`（C 文件外壳）、`cryptoVersion`（加密参数）、`ledgerSchemaVersion`（账本数据形状）、`backupFormatVersion`（B 文件信封）。

- 四者不得合并，也不得因为其中一个变化就同步升级其余三个。只升真正变化的那一个。
- 四个版本号必须始终保存在明文外层，永不移入加密载荷。低版本文件必须能在解密前被识别，否则用户只会看到含义不明的失败，将来的迁移链也失去入口判断。
- **任何一个版本号升级时，必须在同一次改动中把该版本的黄金样例文件加入源码仓库测试夹具。** 一律使用虚构数据，文件名写明版本，已提交的黄金样例不得再修改。没有黄金样例，将来写迁移器只能凭推测还原旧结构，推错即造成无人能发现的静默数据损坏。
- 当前处于 Alpha：遇到低版本一律明确拒绝并说明不提供迁移，不崩溃、不静默失败、不覆盖或删除用户文件，拒绝后仍可新建账本并进入导入流程。
- 由拒绝切换为迁移的时点在进入 Beta 之前，且必须由用户明确宣布，AI 不得自行判断或提前实施。
- 切换之后统一采用“读旧、写新”：只维护单向升级链，永不写回旧格式，不做降级。每个迁移器是纯函数，发布后冻结不改，只在内存中执行，经用户确认后另存为新文件，失败时 fail closed。

完整理由见 `01一些进度/日志/week15_260826/000_W15-账本版本分层与迁移策略决策记录.md`。

## 执行与收尾

- 写代码时先定位相关实现与测试，修改后运行与风险相称的测试、lint 或 build。
- 讨论任务默认只读；没有明确实施要求时不修改代码或 Git 状态。
- 只在出现实质进展、里程碑变化或下一步变化时更新当前开发状态。
- Week 日志继续共用同一个周目录；源码执行文档必须在文件名和标题中写明分支，例如 `01A_W12-CS2026-……` 或 `02A_W12-main-……`。
- 文档遵循 `99_tool/00-文档产出规范.md`：先结论，写清规则、边界和通过线。
- 回答保持简洁直接，明确结果、验证证据和下一步。
