# Week 14 日志

## 2026-08-19 至 2026-08-20：账本 V3 现金、资产与行情

### 本周完成

- 产品层确定单一 USDT 现金池：买卖自动流转，支持入金、出金、外部支出、余额校准和负现金二次确认。
- 本地资产与 Binance mapping 解耦；无交易对的资产仍可离线记账，并保存多日手动价格。
- Binance 保持用户显式点击和最少请求。浏览器无法读取错误响应时使用 `BINANCE_VALIDATION_UNAVAILABLE`，不猜测、不重试、不删除本地事实。
- 完成 `LedgerData.schemaVersion = 3`、`BackupEnvelopeV3`、V3 C 内部账本、统一流水和现金参与总资产／分配／趋势；P&L 与交易热力图继续排除现金。

### 开发验证

- 功能实现边界：`zhennn/w14-v3-cash-assets-market-data@578f4a5`，共 8 个阶段提交和 7 个实现修复。
- 最终定向测试 56 files / 726 tests、全量 84 files / 911 tests；typecheck、lint、production build、diff-check 和各项边界扫描通过。
- 真实 Chrome CH-01～CH-14 在同一隔离原点完成，01C 依据修订后的合同判定开发候选 `PASS`。
- 开发中解决了 Binance 无效 symbol 的 CORS 隔离、普通 V3 B ready import 误要求 `Trade.rawText`，以及键盘删除事件冒泡等问题。

### 独立复审

- 01D 独立重跑自动门后全部通过，但源码审查发现 `W14-01D-P0-01`：`IMPORT_RECOVERY_BLOCKED` 后 Repository 虽停止读写，应用仍只提示用户手动锁定，没有自动撤销会话、退出解锁页并释放密钥持有者。
- 该问题违反文件安全 `C-10`，独立结论为 `FAIL`。发现确定性 P0 后没有启动独立 Chrome，也没有复用 01C 的浏览器证据。

### 独立复审后的状态

- 源码 README 同步 V3 候选与独立失败状态；功能分支暂停，未合并、未推送、无 upstream。
- 真实 V3 B、自然语言转换和第一次真实导入继续禁止，00B 不回写。
- 下一步限定为只修复 `W14-01D-P0-01`，重新跑完自动门和全新 Chrome，再交给新的独立执行者复验。

## 2026-08-20：R1 阻断修复、全量验证与候选冻结

### 修复执行

- 保留原 `01D = FAIL` 和 `C-10` P0，不改写旧证据；新增 `01R1A` 作为开发输入，并预先冻结 `01R1C` 作为后续独立复验输入。本轮只执行 01R1A，没有执行 01R1C，也没有生成 01R1D。
- 把 `IMPORT_RECOVERY_BLOCKED` 提升为当前会话的一次性 fatal signal：Hook 立即停止新操作、abort 在途工作并让迟到结果失效；Dashboard 立即卸载明文工作区；Gate 自动执行 quiesce、drain、Repository revoke、lease release 和旧连接清理。
- 补齐释放失败的安全重试：第一次 release 失败后旧 Repository 仍不可读写、Dashboard 不重挂；重试只继续同一关闭链。关闭成功后必须重新选文件、重新输入密码并建立新 session，旧 session 永久失效。

### 自动测试与质量门

- R1 直接测试：3 files / 49 tests；会话生命周期补充组合：11 files / 303 tests；Week 14 定向闭集：57 files / 729 tests；全量：85 files / 914 tests，全部通过且 0 skip。
- typecheck、lint、production build、diff-check、schema／联网／敏感与调试残留等边界扫描全部通过。
- 每次源码或测试变化后都让旧绿灯失效并重新执行；最后一次源码／测试变化之后，连续跑完上述自动门，再开始新的真实 Chrome 证据链。

### 真实 Chrome CH-01～CH-14

- 使用 Google Chrome `151.0.7922.140`、全新隔离 profile 和同一 production origin `http://127.0.0.1:3427/`。中途浏览器窗口误关后从隔离窗口恢复，继续执行 CH-01。
- 创建 `W14-R1-FAKE-PRIMARY.lftl`、准备 `local-first-trading-ledger_V312.lftl` 并完成导入。最初因窗口与系统选择器状态短暂需要用户协助，之后创建、选择、导入和导出均由执行侧通过 macOS 原生 picker 完成，不再让用户逐次代选文件；密码不写入日志或提交。
- CH-01～CH-14 全部 `PASS`：覆盖 V3 创建与重开、现金事实、负现金确认、离线资产、手动价格、显式 Binance 请求、正常 V3 B 导入、invalid-cash／V2 零写拒绝、双代恢复、1200／390px、键盘、汇总、P&L 和热力图。
- 最终页面数值为总资产 842.89、现金 136、SOL 596.89、KNIGHT 110；P&L 不随现金变化，热力图只计交易。清空事件缓冲并继续观察 10 秒后，Network 0、Console/Log 0。
- CH-13 曾有两次自动化到达撤回按钮过晚，在一次性恢复副本上完成已武装删除；另一个临时目标遗漏 predecessor，被产品在密码前正确拒绝。两项均属测试驱动／夹具问题，不是产品缺陷；修正目标后在独立有效副本上重新完成非破坏性检查，未修改冻结源码、正式测试或配置。

### 文档与 Git 冻结

- 生成 `01R1B`，开发结论为 `PASS`；同步源码 README、当前开发状态和本周日志。该结论只代表开发证据，不覆盖原独立 `01D = FAIL`。
- 源码形成两笔本地英文提交：`bab544f`（关闭恢复阻断后的旧会话）和 `789ebd5`（记录 R1 候选状态）。冻结 HEAD 为 `789ebd5`，相对源码 `main` 为 `0 behind / 18 ahead`，工作树 clean、无 upstream、未 merge、未 push。
- 项目继续冻结在功能分支；真实 V3 B、自然语言转换、00B 回写和合入 `main` 继续禁止。下一步只能由新的独立执行者按 `01R1C` 只读复验并生成 `01R1D`。

## 2026-08-21～22：R1 独立失败与 R2 焦点锁修复通过

### R1 独立结果

- 冻结的 `01R1C` 已被独立执行并生成 `01R1D = FAIL`。原会话级 P0 状态机通过，但真实 Chrome CH-13 发现 `W14-R1D-P1-01`：清空账本确认区没有初始聚焦和 `Tab / Shift+Tab` 焦点锁，焦点可逃到 Chrome 地址栏。
- 原 `01D = FAIL` 与新 `01R1D = FAIL` 都保留；自动测试全绿不能覆盖真实 Chrome 反证。

### R2 最小修复

- 在原功能分支 `789ebd5` 上继续，只修改 `SettingsWorkspace.tsx` 与 `SettingsWorkspace.test.tsx`。确认区现在展开后聚焦输入框，正反向 Tab 在当前可用的输入、确认、取消控件间回绕，Escape 与取消关闭后返回触发按钮。
- 新增永久回归覆盖初始焦点、正向两圈、反向一圈、Escape／取消返回、disabled 跳过和 ledger epoch 清理。受控红灯为 1 file 中 3 failed / 7 passed；修复后定向为 1 / 10。
- 本地英文源码提交：`ac30891 fix: trap focus in clear ledger confirmation`。

### 自动门与隔离真实 Chrome

- Week 14 闭集从 `main...HEAD` 枚举为 58 files / 739 tests；源码变化后两次独立 `npm test` 均为 85 / 919。typecheck、lint、Next 15.5.22 production build、`git diff --check` 与 `git diff --check main...HEAD` 全部退出 0。
- Google Chrome `151.0.7922.171` 的全新 Guest Profile 与 production origin `http://127.0.0.1:3457/` 成功启动；通过 macOS 原生 Save picker 创建文件名含 `W14-R2-DEV-FAKE` 的一次性虚构 `.lftl`。日常 Profile 未导航或处理账本，没有读取真实 B/C 或未知旧连接。
- 真实 AX trace 确认初始焦点进入确认输入框；正向 6 次 Tab 完成两圈，反向 3 次 Shift+Tab 完成一圈，全程没有地址栏、导航或其他页面控件；Escape 关闭后返回“打开清空账本操作”。未输入正式确认文字、未执行清空，虚构文件哈希与 mtime 未变。
- 同一 Guest 会话的补充 DevTools 观察中，Network 请求表为空，Console 没有 application error／exception。Guest 窗口与 production 服务均已定向关闭，日常 Chrome 窗口保留，端口 `3457` 无监听。

### 冻结与边界

- 源码 README 已同步原 01D／01R1D 失败与 R2 开发候选边界；README 后最终独立 `npm test` 再次为 85 files / 919 tests。本地英文文档提交为 `49fa99c docs: record clear confirmation repair status`。
- `01R2B = PASS（R2 开发修复候选；不是 Week 14 最终独立验收 PASS）`。源码结束于 `49fa99c`，相对 `main` 为 `0 behind / 20 ahead`，worktree clean、无 upstream、未 merge／push。
- 原 `01D = FAIL` 与 `01R1D = FAIL` 都继续保留；真实 V3 B、NLP、00B 和合入继续禁止。下一步只能交给全新独立执行者自行取证，不能把本轮开发证据直接转写成独立 PASS。

## 2026-08-22：产品负责人批准发布并完成 Week 14 收口

### 结论

Week 14 已完成发布收口。产品负责人正式接受现有 R2 开发证据，明确豁免所有后续测试和独立复验，并据此批准 V3、R1 与 R2 合入源码 `main`。这是一项产品负责人风险接受和发布决定，不是新的独立复验 `PASS`；原 `01D = FAIL` 与 `01R1D = FAIL` 继续保留。

### 为什么停止继续测试

- R2 已有定向 10/10、Week 14 闭集 58 files / 739 tests、全量 85 files / 919 tests 多次通过，以及既有 typecheck、lint、production build、diff-check 和 Guest Chrome 原生文件选择器焦点循环证据。
- 产品负责人接受上述既有开发证据，决定不再为 Week 14 重复消耗时间；下一步转入新的开发任务。
- 本次明确没有运行 Vitest、定向／全量测试、typecheck、lint、build、Chrome、文件选择器复验或新的独立复验。只执行 Git、SHA-256、分支、worktree、远端同步与文本完整性检查。

### 合并、README 与源码推送

- 合并前证据基线为 commit `49fa99ca70d87c455cebd3d48c5210fef4199ccb`、tree `eecd4e371e161ea1ae13cbdd7785720f533677dc`；源码与根仓库的哈希、分叉、clean 状态、分支和 worktree 均通过准入审计。
- `zhennn/w14-v3-cash-assets-market-data` 通过 `git merge --ff-only` 纳入 `main`，没有 merge commit、squash、rebase 或 cherry-pick。
- 源码 README 更新了产品负责人裁决、禁测事实和历史独立失败边界；英文提交为 `9855f842f172d792582c9b0f5d40367d2bedfe46 docs: record Week 14 release decision`，最终 tree 为 `6ea62e2d25b865be1535c2a458328352ad8ebd08`。该提交只修改 README，本次没有修改业务源码。
- 源码 `main` 已正常推送至 `origin/main` 并达到 `0/0`；`CS2026@a7d8b2158946c29829ecdfc8dd6ec053b0c8ee68` 普通 push 返回 `Everything up-to-date`，内容未改。
- main 推送成功且 feature 被完整包含后，本地 feature 使用安全的 `git branch -d` 删除；远端原本没有同名 feature。最终源码本地及远端都只保留 `main`、`CS2026`，两个 worktree 均 clean 且与远端为 `0/0`。

### 根文档归档与推送

- `01R1D` 未改任何字节，最终 SHA-256 仍为 `b8a507b0ea73b2a80ef473f5e35e4644f7e23ed60945acc433501e6d72ce55dd`；独立归档提交为 `36579bfdae23dac0eb10824c95d38e82b121a95e`。
- 当前开发状态、`01R2B` 本节和本日志由第二笔中文提交记录。根仓库 `main` 随后正常推送至 `origin/main`，fetch 后为 `0/0` 且工作树 clean。
- 根目录不存在 `README.md`，因此没有为了制造 diff 新建或修改根 README；源码仓库 README 已按要求更新。

### 历史失败与下一步

- `01D/01R1D = FAIL` 描述的是当时独立复验发现的真实问题，不能删除或改写；R2 开发证据说明后续修复情况，产品负责人裁决说明为何现在批准发布，三者不是同一种结论。
- 本次没有创建 `01R2D`，没有声称独立复验通过。最终结论只能写作“发布收口 PASS”。
- Week 14 到此结束。下一步从新的长期产品开发任务开始，不再继续追逐 Week 14 重复测试；真实数据操作、NLP 或其他范围在新任务中另行确认。
