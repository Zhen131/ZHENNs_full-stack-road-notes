# 当前开发状态

更新时间：2026-09-02（二）
当前结构：**2026-08-31 起只有一条源码轨道 `main`**，它同时是长期账本产品与毕业论文的实现本体。原 `CS2026` 毕业论文 worktree 与分支已按产品负责人决定删除，理由与保留措施见下方「毕业论文轨道」。annotated tag `CS2026-baseline-2026-08-02`（分叉点）与 `CS2026-english-ui-2026-08-02`（5 笔英文翻译）均保留。

仓库与隐私边界：根文档／开发日志仓库永久保持私有；源码仓库已在公开前安全检查通过后恢复公开。真实 OKX 原始数据、转换工具、中间表、报告和正式 B 永久留在电脑本地 `Downloads/history_OKX/`，不进入 Git、PR、Issue、测试夹具、源码样例或开发日志。用户接受旧 PR 引用中仅剩的历史数字可能可见，不提交 GitHub Support 清理工单；该风险接受不适用于未来新增的真实数据。

Git 哈希说明：2026-08-22 完成敏感历史重写并从清洁远端重建本地 Git 后，Week 15 五批已全部合入，正式 `main` 现为 `8df62d8`，相对 `origin/main` 领先 36 笔且**尚未推送**。第五批因 `main` 已有纯文档提交 `322ee67` 而无法快进，改用 `--no-ff` 合并提交 `3d7c1b4`。所有 Week 15 功能分支均经 `git branch --merged` 确认完全包含后删除；远端从未存在这些同名功能分支。旧 Week 文档中更早记录的 SHA 属于重写前证据坐标，事件结论仍保留，但旧对象不再存在于当前本地正式历史。

## main／长期账本产品

- **第七批多语言机制已通过 `07D` 独立验收（有条件通过）、条件已解除并合入 `main`。** 建立中／英／匈文案表、唯一语言 Context、设置页语言开关，数字统一普通空格千分位并以 CSS 防折断；**只以首页示范，其余页面文案未搬**。`07D` 的四项条件全部为「表述强于证据」，不影响数据正确性、不违反禁令；已为 `07C` 追加更正节（原文不改写），两项移交 09 批（见 `09A` 第八节）。
- **⚠️ 多语言当前的真实覆盖面**：中文文案表 **1,214 键**（此前记载的 655／660 均为 `awk` 按行计数所致的错误，多个 key 写在同一行被吞掉；已用 AST 口径更正）。**英文与匈牙利语各仅 32 键，覆盖率 2.6%**，其余经 `translate()` 回落中文。**在译文补齐前，不得声称正式主线已具备英文交付能力。** 英、匈译文亦未经母语者审校。
- **08／09 进行中：分支 `zhennn/w15-main-app-split@b6738a8`，相对 `main` 52 笔提交，工作树 clean，未推送。**
  - **08 第一轮完成度不足 10%**：只 4 笔提交，`DashboardShell.tsx` 1,575 → 1,480；**7 个巨型测试文件一个未拆**，其余三个大文件零改动。根因是 `08B` 二档退路过宽（写了「找不到干净边界可跳过」却未写「共享 helper 不算」）。未完成部分定义为 **08 第二轮**，见 `08A` 修订 B，**不新增批次编号**。
  - **09 搬迁已完成但独立验收 FAIL，不得合入。** 三轮共 57 笔提交，中文表 1,214 键；标点残留由 25 处降至 1 处（`layout.tsx` 服务端 metadata，合格跳过）。
  - **`09D` 查出 4 处中文被静默改写，1,185 条测试全绿未发现。** 根因同一：**一个 key 被复用到中文原文并不相同的多个调用点**。四处为 `BackupControls.tsx:614,1407` 与 `FeeRuleManager.tsx:299,337`，产品负责人已逐处复核证实。处置见 `09B` 修订 C：四处各建专用 key 改回原文、**不得改现有 key 的值**，并新增 `translationKeyUsage` 守卫。**修复完成前不得合入 `main`。**
  - **09 第五轮开发收口完成，仍待独立验收，未合入 `main`。** `a943f0b` 为 `PriceForm` 校验字段新增专用 key，恢复缝上“资产”并保留标签“价格资产”；`5c825c2` 将 `translationKeyUsage` 扩至 `translateDefault(...)`，逐条核对新增 10 个复用 key 的缝上原文，并以临时 worktree 通电证明新增调用形式会红。两笔提交前与收口全量均为 107 files／1186 tests；build、typecheck、lint、冻结派生快照、结构守卫和两项 whitespace 均通过。详见 `09C-2_W15-main-文案搬迁第五轮执行报告.md`。
  - **⚠️ `09A` Q-2 已被证伪**：「中文断言全绿 ⇒ 搬运是纯的」不成立。涉事 key 零测试覆盖，破坏三条文案只有 2 条变红且守错了调用点。**今后文案类批次不得再以中文断言全绿单独作为「渲染逐字不变」的证据，必须另配覆盖全量字面量的机械比对。**
  - **`08D` = PASS。** 4 笔提交经逐字节机械比对确认为真正纯搬运；`DashboardShell.tsx` 收尾实为 **1,480** 行（此前记载 1,462 为产品负责人从 `08C` 抄入未复算之误）。详见 `08A` 修订 C。
  - **`core`／`platform` 错误码改造已移出 09**（`09A` 修订 B）。将来另行立项，入口见 `09C` 第五节。
  - **`09C`／`08C` 的数字更正**：中文表 660→1,214；中文断言「349→352」为两条不同命令的产物、不构成比对（同一命令两侧均 357）；缝上 build 364→365 kB；`@/app` 11 处实为 11 个文件、12 条 import；`08C` I-7 的 hook 正则漏抓 `useReducer`／`useLayoutEffect`／`useId` 共 5 个。
  - **下一步：对 09 第五轮提交做独立验收；通过后合入 `main`，再做 08 第二轮（拆 7 个测试文件）→ 10 批。**
- **批次结构：08 = 搬家具（拆测试文件、搬走巨型函数外的一切、提取不含任何 hook 的纯展示子组件，一个 hook 都不许移动）；09 = 文案搬迁；10 = 新立项，真正切开巨型 hook 与组件。** 起因是摸底发现 `08A` 的隐含假设不成立：四个大文件里各躺着一个巨型函数／组件（`usePersistentLedger.ts` 1,963 行中有 1,674 行是单个 hook，内含 12 个 `useState`／7 个 `useEffect`／30 个 `useRef`／13 个 `useCallback`）。切开它必然改变 hook 注册结构，违反 `08A` A-1。**总工作量未变，只是把危险部分拎出来命名。** 08 之后 `usePersistentLedger.ts` 仅从 1,963 降至约 1,730，基本不改善——那是 10 批的对象。
- 当前主线：本地正式 `main@e0ff7df`，相对 `origin/main` 领先 **24 笔**，工作树 clean，**未 push**。Week 15 前七批全部收口并合入。
- 工作目录：`01一些进度/产出/LocalFirstTradingLedger/`（唯一 worktree）。
- **当前定位：渲染与存盘两处瓶颈都已解决，10²～10⁵ 四档全部可用。** 第六批「存盘成本」把 C 文件外壳由「纯 JSON 内嵌 base64 密文」改为「JSON 头 + 原始加密字节」，改用轮换的固定槽写入，并把账本事实按数量切分为独立加密、独立认证的块；普通保存只重写变化的块与一个头槽。**块之间没有哈希链**，因此编辑历史事实只重写它所在的那一块。
- **四档可用性判定（production，10⁵ 为小样本）：**

| 档位 | 冷启动 | 记一笔 | 查询筛选 | 页面切换最差 | 单字符输入 | 交易页元素 | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 10² | 0.20 s | 0.09 s | 0.03 s | 0.07 s | 0.03 s | 2,932 | 可用 |
| 10³ | 0.38 s | 0.22 s | 0.03 s | 0.06 s | 0.03 s | 2,962 | 可用 |
| 10⁴ | 0.86 s | 0.35 s | 0.03 s | 0.07 s | 0.03 s | 2,926 | 可用 |
| 10⁵ | 6.63 s | **1.56 s** | 0.03 s | 0.11 s | 0.03 s | 2,930 | **可用** |

- 「记一笔」在 10⁵ 档的分段轨迹：V2 基线 18,630 ms → S-1 去 base64 后 9,631 ms → S-3 分块后 **1,556 ms**。**10⁶ 仍不可用**：明文 B 文件在 JSON 序列化阶段触及 JavaScript 单条字符串上限，该路径不在第六批范围内（`06A` N-5），M-3 如实记为「未取得」，未以其他数字顶替。
- **⚠️ 性能证据等级（产品负责人 2026-09-01 再次明确接受并留痕）：四档数字只有执行者单方测过，10⁵ 为小样本，未经第二方复现。** `06D` 依约定未重跑任何浏览器量尺。**该结论不得被引用为「经过独立验证的性能结论」，写入论文时尤须注意。** 与之相对，三道正确性闸门（04 批冻结派生快照 7/7、往返测试五情形、IV 不重用）`06D` **全部自己实跑并全绿**。
- 最近验证（07 合入后在 `main` 上重跑）：默认全量 **106 files／1185 tests**、typecheck、lint、production build、结构守卫 2 files／8 tests、冻结派生快照 7/7、两项 whitespace 全部通过。
- **今后要求（07 收口时新增）：版本号复核必须指向权威定义处，不得指向镜像常量。** 07 批执行者曾把四个版本号全部指向 `ledgerFileChunkedContainerV3.ts:24-27` 的镜像常量块 `LEDGER_FILE_OUTER_V3_S3_CONSTANTS`；权威定义实为 `ledgerFileContract.ts:31` 与 `backupEnvelope.ts:20`。四个值当时全部正确，但该检查弱于其表述。
- **版本号现状：`fileFormatVersion` 3（本批唯一变化）／`cryptoVersion` 1／`ledgerSchemaVersion` 4／`backupFormatVersion` 3。** 加密参数常量文件整份未改动。
- **版本 2 的 `.lftl` 不再能被打开**：只在解密前识别明文版本号并明确拒绝，不验证密文、不派生密钥、不解密、不写回，原文件原样保留。**本批不实现迁移器**，「由拒绝切换为迁移」的时点仍未到来，须由用户将来明确宣布（依 `AGENTS.md` 应在进入 Beta 之前）。
- 黄金样例现有 6 份（`test-fixtures/golden/`）：B 文件 schema V3、schema V4 空壳、schema V4 有内容各一份；C 文件 `fileFormatVersion` 2 与 3 各一份；另有 README 索引。**V2 那份是将来写迁移器时唯一的物证，永久冻结。**
- 提交坐标：第六批 14 笔以 fast-forward 合入（`4a66905` 起至 `fd32c2e`），README 收口 `755b4bd`。功能分支 `zhennn/w15-main-chunked-storage` 经 `git branch --merged` 确认后已删除。全部只在本地。
- 当前产品入口：Week 15 第一至六批见 `01A`～`06D`；版本分层与迁移的长期规则见 `000_W15`；性能长期规则 R-1／R-2／R-3 见 `03A` 第二节。合同写作检查清单见 `99_tool/00-文档产出规范.md`。
- **真实数据状态：** 用户真实账本为 13 个资产、415 笔交易、165 条现金事件、57 条价格快照，**远低于任何已知瓶颈**。其 `.lftl` 为版本 2，合入本批后已不能打开；过渡方案为沿用 2026-08-28 生成的 B 文件（`backupFormatVersion` 3 ＋ `ledgerSchemaVersion` 4，落在支持范围内），待 07／08／09 全部完成后导入。自该日起未记录新交易，故内容完整。
- 风险：源码 `main` 领先远端 15 笔且尚未推送，推送前须按公开前安全检查流程复核。产品仍处 Alpha。

## 毕业论文轨道（原 CS2026，已并入 main）

- **2026-08-31：`CS2026` worktree 与本地分支已删除。** 产品负责人决定：毕业论文不再单独开轨道，`main` 就是论文的实现本体。原因是当初分叉的动机（担心网页端不安全、想转桌面端）已经不成立，而论文本身要求的正是 Web Application。
- **删除前的事实核对**：`CS2026` 自 2026-07-31 的 `bbdc367` 分叉后只多出 5 笔提交，全部是英文翻译（66 个文件的界面、工作流、测试与夹具）；同期 `main` 前进了 97 笔，并完成 Week 13 源码目录重构，被翻译的路径已全部不存在。因此其代码价值为零，措辞价值不为零。
- **保留措施**：新建 annotated tag `CS2026-english-ui-2026-08-02` 指向 `a532495`，永久保住那 5 笔。已验证 `CS2026-baseline-2026-08-02` 指向分叉点、**不包含**这 5 笔，故若无新 tag，删分支即会使其不可达并最终被垃圾回收。需要英文措辞时从新 tag 取用。
- **远端未处理**：`origin/CS2026` 仍然存在。删除远端分支属 push 操作，只能由用户本人执行；在此之前不得把它当作可用轨道，也不得向其推送。
- **英文交付改由多语言切换满足。** 产品负责人决定实现中文／英文／匈牙利语一键切换（导师为匈牙利语母语者）。第七批已通过独立验收并合入 `main`，机制就位且完成首页示范，**但只有首页与设置页接入**；剩余文案的搬迁是 09 批的工作。**在 09 批完成前仍不得声称正式主线已具备英文交付能力。****另注（2026-08-31 核实）：多语言／国际化在 `04-毕业论文项目方案.md` 与 `04A` 中一次都没有出现**，既不在论文范围内也不在 Future Work 里，因此它是产品负责人的判断，**不是论文文档记载的硬性前置**；此前该处的「硬性前置」表述已据此撤下。
- **待处理缺口：`04A-毕业论文执行范围调整-2026-07-10.md` 的「原方案承诺 vs 当前执行口径」对照表已过期。** 它写于 2026-07-10，未记录此后发生的架构级偏离：论文原文写的是 `IndexedDB as the local transactional database`，而实际正式账本自 Week 11 起已改为磁盘上的 `.lftl` 加密文件，IndexedDB 仅存文件句柄。另有图表库 Recharts→ECharts 6.1、第三张图变为交易热力图、benchmark 量级超额完成（原定 10⁵ 为 stretch，实际已测到 10⁵ 并探测 10⁶）。`04A` 明确写着「未经确认，不把工程计划调整表述为导师批准的论文范围变更」，因此这些偏离须在论文定稿前与导师确认。
- **加密实现零偏离**（2026-08-31 核对）：PBKDF2 + SHA-256 + 600,000 次迭代、AES-GCM、keyLength 256、ivBytes 12、tagLength 128、saltBytes 16，与论文原文承诺的 `PBKDF2 + AES-256-GCM` 完全一致，迭代次数高于 OWASP 对 PBKDF2-SHA256 的建议值。密钥只留运行时内存。
- 风险：Week 11 `02D` 与 `03B` 的历史独立证据边界不因本次删除而改变。用户 walkthrough 研究、加密／明文 ablation 尚未执行，不得提前声称论文证据已完成。

---

## 已完成

### Week 15 账本 V4、首页持仓表校正与性能基线

- 第一批把账本升到 V4：新增资产转入转出事实，覆盖充币、提币、内部划转与白拿，补上“非买卖引起的持仓变化无法表达”这一能力缺口；同时加入只作用于渲染层的数字格式化与首页持仓成本区。V4 不向下兼容，旧 V3 文件被明确拒绝且原样保留。
- 第二批把首页持仓表的“总花费”整列换成“剩余持仓成本”，统一首页三处对同一底层数字的叫法，并把“累计买入流出”移入完整持仓详情且标注不参与相减。底层公式与精度一个字未改。
- 第三批只造尺子不修卡顿：确定性合成账本生成器（固定种子 `w15-main-perf-baseline-v1`，五档 10²～10⁶）、Node 层量尺（M-2／M-7／M-8）与浏览器层量尺（M-1／M-3～M-6），浏览器层未降级，替身只替换系统文件选择对话框。产品代码 `src/` 零改动。
- 三批合入本地 `main`，功能分支确认完全包含后删除，未推送远端。三批的独立验收 `01D`／`02D`／`03D` 均为有条件通过，都不覆盖测试执行。
- `03D` 提出三项观察：O-1 根因“证伪”结论过宽须收窄，O-2 `M-2` 内含 `M-7(30d)`，O-3 `S-100K` 的首要天花板是 8 MiB 导入上限而非渲染。三项均须在 04／05 批产品定义中处理。

### 真实历史数据转换、同资产手续费与仓库隐私收口

- 源码 `main@9b8fed7` 已补齐同资产手续费：买入持仓增加 `quantity - fee` 且要求 `fee < quantity`；卖出持仓消耗 `quantity + fee` 并检查可用持仓；同资产费用不改变 USDT 现金，第三资产费用继续明确标记为不支持。
- 清洁公开版本重新通过全量 86 files / 933 tests、typecheck、lint 和 production build；手续费专项 7 files / 106 tests 通过。该专项结论不追溯改写原 `01D/01R1D = FAIL`，但原 01D P0 已由 R1 修复，01R1D 的失败原因是后续发现的焦点锁 P1，R2 与产品负责人发布裁决继续按既有边界保留。
- 私有转换在相同输入与决定上连续两次得到同一正式 B：13 个资产、415 笔交易、165 条现金事件、57 条价格快照、0 条 FeeRule，待确认 0；程序预检和真实 Chrome 只读预检均通过，完整校验和复核通过。
- 真实 Chrome 只选择并展示 B 的预检，没有点击“确认恢复”；虚构 C 的大小、修改时间和 SHA-256 均未变化。真实数据未导入。
- 根文档和源码正式分支／标签已完成历史清理；本地根仓库、源码 `main` 与 `CS2026` 已从清洁远端重建，旧 Git 元数据和临时恢复目录已删除。根文档远端保持私有，源码远端已恢复公开。
- GitHub Support 工单未提交。公开源码的正式分支、标签、PR／Issue 元数据、Actions、Artifacts、Release 与密钥模式检查通过；旧 PR 引用中的数字残留按用户决定接受。
- 下一步只有在用户另行明确授权时才进行真实 B 的正式恢复；默认继续保持本地私有和零导入。

### Week 14 V3 开发、独立复审与发布收口

- 01A 固定一个 USDT 现金池、本地资产与 Binance mapping 解耦、手动价格和纯浏览器显式联网边界；B-02 采用 `BINANCE_VALIDATION_UNAVAILABLE` 的改良版 C。
- 功能分支完成 8 个阶段提交和 7 个实现修复；实现边界为 `578f4a5`，新增现金、资产、行情、V3 B/C、统一流水和跨页投影。
- 开发侧最终为 56 files / 726 tests、全量 84 files / 911 tests，全部质量门与真实 Chrome CH-01～CH-14 通过；01C 结论为修订合同下的开发候选 `PASS`。
- 独立 01D 重新运行同等自动门并全部通过，但确认 `IMPORT_RECOVERY_BLOCKED` 缺少应用层自动锁定、会话撤销和密钥持有者释放，按 `C-10` 判定一个 P0，最终结论为 `FAIL`。
- 独立复审发现确定性 P0 后未启动 Chrome，不复用 01C 的浏览器证据，也没有读取真实 B/C 或自然语言账本。
- 源码 README 曾在功能分支同步候选事实；该状态已由 2026-08-22 的产品负责人发布裁决取代。
- R1 只修复原 P0：Hook 发布一次当前会话 fatal signal；Dashboard 卸载明文；Gate 自动 quiesce／drain／revoke／release，release 失败保持关闭并可安全重试，成功后清除旧连接并强制重选／重认证。
- R1 最终自动门为 3 / 49、11 / 303、57 / 729、85 / 914，全部质量门和全新真实 Chrome CH-01～CH-14 通过；开发报告 `01R1B = PASS`。
- R1 本地英文提交为 `bab544f` 与 `789ebd5`；随后 `01R1C` 已被独立执行并生成 `01R1D = FAIL`，原 P0 已通过，新失败来自清空确认区焦点外逃 P1。
- R2 以 `ac30891 fix: trap focus in clear ledger confirmation` 完成两个允许文件的最小修复；定向 1 / 10、Week 14 58 / 739、源码变化后全量两轮与 README 后最终全量 85 / 919、全部自动质量门和隔离 Guest Chrome 定向回归均通过。README 提交为 `49fa99c`，`01R2B` 只判开发修复候选 PASS，不覆盖独立失败。
- 产品负责人于 2026-08-22 接受上述既有 R2 开发证据并豁免新的测试与独立复验；本次未重跑任何测试。feature 以 fast-forward 纳入 `main`，源码发布 README 提交为 `9855f842f172d792582c9b0f5d40367d2bedfe46`，tree `6ea62e2d25b865be1535c2a458328352ad8ebd08`，已推送至 `origin/main`。
- 本地 feature 已用 `git branch -d` 删除，远端从未存在同名 feature；源码本地及远端最终只保留 `main` 与 `CS2026`，两个 worktree 均 clean 且分别与远端为 `0/0`。原 `01D/01R1D = FAIL` 未改写，本次没有产生新的独立复验 `PASS`。

### Week 13 UI 重构、首页交易活动区打磨与主干发布

- `Zhenn's Ledger` 已从单页纵向堆叠重组为首页、记账、交易、导入与导出、设置五个工作区；导航切换继续共用同一解锁账本实例，不重复 hydrate，记账草稿可跨页面保留。
- 首页以四项摘要、趋势、分配、前三持仓和 365 日交易活动形成 1280 × 800 一屏总览；底部收口为约 1:2 的持仓概览与交易活动双卡，移除独立最近交易卡。
- 365 日热力图按周一至周日保留 365 个日期和约 53 列；Tooltip 显示日期、买卖数量与资产方向摘要，有交易日可进入完整交易列表并定位、短暂高亮当天全部主行。
- 交易页补齐筛选、详情、依赖拒绝、两次预检、5 秒倒计时、撤回和最终删除；首页日期定位会先清除旧筛选、详情和删除预备，但不改写交易或删除合同。
- 导入导出、FeeRule、Binance mapping、清空与文件状态从旧长页面重组到明确位置；页面只消费既有业务与安全服务，没有改写 `LedgerData`、`.lftl V2`、BackupEnvelopeV2、加密或整本导入合同。
- 第一轮 UI 的 9 个实现提交与中文 README 已通过 PR #8 合入 `main@baae5ab`；首页打磨两笔实现提交为 `336a71d`、`e378d32`，经双亲合并提交 `76213d4` 进入主线，README 发布提交为 `0d0cb55`。
- 首页打磨开发侧必测 9 files / 86 tests、两轮全量 73 / 797、全部质量门与真实 Chrome 固定链通过；合入后再复跑 73 / 797、typecheck、lint、production build 和 diff-check，全部通过。
- `zhennn/w13-main-ui-polish` 已在确认完全合并后删除，远端从未创建同名分支；源码 `main` 已推送并与 `origin/main` 为 `0 / 0`。开发执行结论为 `PASS`，但尚未进入独立验收。

### Week 13 源码目录重构、R1 修复与主干发布

- 源码从历史技术类别目录收敛为 `app / core / features / platform / ui / test-support` 六个一级区域；跨边界使用登记后的稳定入口，同一区域使用精确相对引用。
- 结构守卫基于 TypeScript AST 建立全部受管 `.ts / .tsx` 的静态依赖图，并检查自身稳定入口、未登记深层 alias、跨边界 `../` 和强连通分量。
- `01C` 发现的 15 处自身稳定入口引用、3 组涉及 14 个文件的静态循环和守卫假绿灯已由 R1 清零；临时自身入口与最小循环夹具均能使守卫准确失败，撤销后恢复通过。
- macOS Open picker 保留 `.lftl` 提示并开放 `All Files` 回退；Save picker 仍严格使用 `.lftl`，应用继续在读取和密码流程前拒绝非 `.lftl`。
- R1 最终为 59 files / 737 tests，lint、production build、typecheck、diff-check 和真实 Chrome 原生 picker 流程通过；01D 判定开发执行 `PASS`，独立复审边界继续保留。
- 七个候选提交已从 `7481e78` fast-forward 合入源码 `main`，英文 README 发布提交为 `1a7ecb8`，并已推送到 `origin/main`。

### Week 12 第一批含费 P&L R1 最终独立 PASS

- `PNL-001～003` 已在 `zhennn/w12-pnl-fee-accounting` 形成九个本地提交：原候选六个提交，加上 R1 的生产修复、正式回归和 README 三个提交。
- 新建账本与新交易、手工 / Binance 价格、实际手续费统一使用 USDT；`schemaVersion` 仍为 1，旧 USD 事实未改写。
- 确定性持仓重放已计入同币种实际手续费；固定样例得到 6505 / 2797 / 2602 / 195 / 3903，零费、多次买入、部分卖出、全部卖清、缺价和异币费用均有正式证据。
- 交易列表、持仓表、摘要和三张图统一消费含费口径；备份与 `.lftl` 仍只保存事实，不保存派生金额。
- 原 01B 开发侧结论为 `PASS`，随后原 01D 因旧 USD 账 absent Binance mapping 静默写回而判 `FAIL`；原失败报告完整保留。
- R1 用运行期只读 resolver 分离临时 fallback 与持久化事实，01R1B 开发侧 PASS；全新独立执行者生成 01R1D，58 files / 730 tests、全部质量门和真实 Chrome 全链均 PASS。
- 00B 的三个 PNL 目标已据 01R1D 勾选。九个候选提交已 fast-forward 合入并统一推送源码 `main`，README 收口提交为 `44d1e56`；Week 11 `02D = BLOCKED` 未改变。

### Week 12 第二批 V2 与手续费规则开发 PASS、主干收口

- `FEE-001～003` 已由 02A 完成，02B 开发侧判定 `PASS`；`LedgerData.schemaVersion = 2`，V2 `.lftl` 与 BackupEnvelopeV2 成为正式新写合同。
- V1 `.lftl`、V1 备份和旧完整 IndexedDB 账本只识别并清楚拒绝，零迁移、零写回、零自动删除；IndexedDB 正常路径只保存三字段 connection record。
- fixed / percentage FeeRule 按平台 + 资产精确匹配；冲突不猜，版本更新创建新 ID 并停用旧规则，历史 Trade 只读取用户确认的实际 fee。
- 页面已提供规则新增、版本、停用、候选手续费、用户覆盖、现金影响和认证保存闭环。
- 自动化最终为 58 files / 727 tests，typecheck、lint、production build、diff-check 和残留扫描通过。
- 真实 Chrome 已覆盖系统 picker、V2 新建 / 重开、备份 / 恢复、V1 拒绝、权限、双标签、文件副本、外部 revision、current / previous 恢复、删除 / 重选、raw IndexedDB 与 390 / 1280 视口。
- 九个阶段提交已 fast-forward 合入源码 `main`，README 收口为 `7481e78`。用户决定不立即执行 02C、不生成 02D；独立审查改为后续集中复验。

### Week 12 网页优先路线与 00 层重写

- 删除 Week 12 目录中五份仍使用 W11 文件名的副本，重新建立 `000_W12`、`00A_W12`、`00B_W12`、`00C_W12-main` 和 `00D_W12`。
- 当前路线固定为：01 含费 P&L → 02 版本 2 与手续费规则 → 03 单 Agent → 04 双 Agent → 05 追问预览 → 06 虚构 Demo C 安全写入 → 07 公平评估与 Notebook。
- 新目标明确区分当前源码事实与未来设计：版本 2、旧整账退役、可选平台和固定 / 百分比手续费规则现已实现并合入；本地 Agent 仍尚未实现。USDT 与实际同币手续费已由 01R1D 独立通过。
- NLP 与比赛 Demo 不再依赖旧 `USE-001`、交易编辑或完整 UI 重构；V2 谨慎使用与集中独立复验边界统一见 00D。
- 论文与 `CS2026` 保持暂停；未进入 Sprint 时 2026-09-25 后、进入 Sprint 时 2026-10-10 后再讨论桌面端。
- 01A / 01B / 01C / 原 01D 及 R1A / R1B / R1C / R1D 均已形成完整证据链；原 FAIL 与最新 R1 PASS 分开保留。

### Week 11 产品共识与 00 层文件治理

- 原 Week 11–13 执行计划已归档，不再作为当前执行入口。
- `00A_W11-产品战略重构已确认共识.md` 已固定真实使用、C/B、净盈亏、安全纠错、UI、历史 K 线、NLP、比赛、论文和长期平台边界。
- `000_W11-规划文件关系与批次命名规则.md` 已固定 `00A → 00B → 同时生成 NNA/NNC → 执行后生成 NNB/NND → 回写 00B` 的批次流程。
- 00A 重命名和文件治理提交为 `4a1c606`；源码仓库未因本轮文档治理发生变化。
- `00B_W11-总需求快照与待办清单.md` 已生成，包含 63 个普通目标、7 个 DEC 和 23 个 GATE；建立提交为 `4f73752`。
- 00 层全链路审查结论为 `GO WITH FIXES`；已补齐 FILE-003 安全前置、C→B 灾难恢复通过线、目标与 GATE 映射，并同步当前状态和源码 README。
- 未取得独立 PASS 证据的目标不得提前打勾；第一批已由 01D-6 最终独立 PASS，四个目标已按规则回写 00B。
- 2026-07-30 起，NND 失败只暂停本批目标和依赖路线；P0、数据安全类 P1 和强制通过项必须阻塞，普通 P1、P2、P3 统一登记到 `00D_W11-已知缺陷与延期处理清单.md`。
- 每批最多立即安排一轮 `NNR1A / NNR1C` 阻塞修复；仍未通过时暂停相关路线，不再自动生成一串 D 后缀。
- 每批直接适用 GATE 深测、全部自动测试完整运行、其余 GATE 快速复核；发现疑点再升级深测。

### Week 11 第二批 C 文件安全闭环与正式接管（开发、独立审查与主干收口）

- 合并版 `02A` 已按三个阶段完成 `FILE-006、FILE-007、FILE-008、FILE-009、FILE-003、FILE-010`；唯一开发报告为 `02B_W11-C文件安全闭环与正式接管合并执行记录.md`。
- `FILE-006` 只在 current 确实损坏且 previous 独立验证有效时进入可见恢复；取消零写入，确认后必须 write、close、同句柄 readback 与完整验证。
- `FILE-007` 以 `isSameEntry()` 判断真实文件，加入跨页面 lease、短时写锁、写前复读与外部 revision 防覆盖；副本不误锁。
- 用户选择 dirty 立即锁定方案 B：先提示未保存修改，可重试保存或明确放弃后锁定；密码、会话与文件占用按 quiesce / drain 收口。
- `FILE-008 / FILE-009` 让 IndexedDB 只保存 C 文件句柄和最小身份连接记录；重连、权限、错误文件、旧异步结果、密码生命周期与释放重试均 fail closed。
- 用户选择 legacy 退出方案 A：旧完整 IndexedDB 账本仅可解锁迁移到新 C；新 C 完整写入、关闭、复读、身份与内容验证后，用户再以固定文本确认删除旧记录。
- `FILE-003 / FILE-010` 已把正常入口切换为 C，并限制清空为 ready C 的固定文本确认；清空生成新 current、保留旧 current 为 previous，复读验证成功后才显示完成。
- 开发侧全量为 51 个测试文件、596 项测试，typecheck、lint、production build 与 whitespace 通过。
- 02C 已对冻结提交 `1871bd4` 只读复跑阶段测试、两个补充组合、01D-6 回归、全量 596 项测试和全部质量门，均通过且未发现强制 FAIL。
- 真实 Google Chrome 可以显示候选页面，但系统保存选择器没有出现；因此无法取得真实 `.lftl`、权限、双标签页、重连、raw IndexedDB、立即锁定、legacy 迁移和 clear 的强制证据，02D 按合同判 BLOCKED。
- 六个 FILE 目标继续未完成，00B 不回写。合入 `main` 是用户明确授权的 Git 收口决定，不是独立验收通过。
- 冻结候选已 fast-forward 合入并推送源码 `main`；源码 README 的过期候选陈述由 `ceac405` 修正，本地与远端候选分支均已删除。对应 P3 `W11-DEF-001` 已于 2026-08-05 复核关闭。

### Week 11 第三批 B 历史导入与浏览器事务边界

- 合并版 `03A` 已执行并生成 `03B_W11-B历史导入预检与整本原子替换合并执行记录.md`；`03C` 后来在执行前作废，`03D` 不生成也不伪造。
- 阶段一 `IMPORT-001 / IMPORT-003 / IMPORT-004` 已完成完整明文 B 导出、只读预检、准确错误路径、可复制报告、可疑重复分组和 C 零写入边界。
- 阶段二 `IMPORT-002` 的浏览器正常链已完成：只允许同一会话中新建且仍为空的 C，绑定 B identity、候选、fileId、revision、session / hook generation 和选择代次；成功结果整本等于 B，不合并、不跳过、不自动去重。
- 写入前保存旧 C 完整 bytes、fileId、current revision 和 previous；candidate 完成 write、close、readback、解密、Validator 和 lineage 复验后才替换页面。
- post-close 失败只在磁盘仍是本事务精确 candidate 时补偿旧 C；补偿或最终复读无法证明时进入 `IMPORT_RECOVERY_BLOCKED`，当前 repository 后续读取和写入 fail closed。
- 正式测试覆盖写前 / 加密中外部变化、close 后异常、readback / 解密 / Validator / lineage 失败、补偿失败、生命周期切换和并发 render；最终为 55 files / 698 tests，全部质量门通过。
- 真实 Chrome 已完成 macOS Save/Open picker、新 C、合法 300 笔 B、导入后 hash / revision、锁定和重开；这证明正常链，不证明 Chrome 崩溃后的强制回滚。
- 03A 的绝对完成线超出纯浏览器单文件能力：页面在 close 后死亡、权限永久丢失或外部程序抢先改写时，JavaScript 无法继续补偿。03B 因此判定整批 BLOCKED，不回写 00B。
- 当时产品负责人曾确认把 Mac 桌面端提前为下一阶段优先方向；这是一条 Week 11 历史决定，已由 2026-08-09 的 Week 12 网页优先路线取代，不再作为当前下一步。
- 源码 README 以 `0c72d47` 同步当前能力与桌面迁移边界；实现提交 `7dcd297` 和 README 提交经 `084ae7d` 合入并推送 `main`。本地 `zhennn/w11-b-import-atomic` 已删除，远端本来就没有该候选分支。

### Week 11 第一批 C 文件合同与安全保存

- 第一批目标为 FILE-001、FILE-002、FILE-004、FILE-005；01A 开发执行文档和 01B 开发执行报告已经存在。
- 第一批初始候选与 README 曾以提交 `2e5c7e5` 保存在 `zhennn/w11-c-file-core-save`；两轮返工形成 13 个候选路径，并由 01D-6 对完整工作树完成只读复验。
- 01B 报告已实现 `.lftl V1`、唯一句柄、current + previous 双代、close 后同句柄复读及 C capability 边界，并报告 48 files / 434 tests、typecheck、lint、build 通过；01D 已独立复现这些既有绿灯，同时证明测试集合没有覆盖 P0 / P1。
- 原规则把 01C 写成结果文件、01D 写成另一份结果文件，与用户预期不符；现已统一为 A/C 输入、B/D 报告，未来每批 NNA 与 NNC 同时生成。
- 旧 01C 静态审查结果移入 `week11_260727/Z99Archive/`，只保留为历史风险证据，不冒充 01D。
- 新 01C 已补建为可交给另一 AI 跑目标模式的独立测试审查执行文档，固定源码只读、T0–T9、PASS / FAIL / BLOCKED、对抗性 salt 漂移和旧异步选择测试，以及 01D 输出合同。
- 01C 已由独立 AI 执行，正式源码全程只读；指定测试 39 / 33 / 78 项、全量 48 files / 434 tests、typecheck、lint、build 和 diff-check 通过。
- `01D_W11-C文件合同与安全保存独立测试审查报告.md` 已生成并判定 FAIL：P0 为 salt 漂移可被错误确认并发布 `saved`，P1 为旧 picker Promise 可在新选择、取消、卸载或创建后复活，P2 为正式测试缺少上述对抗场景。
- T8 因前序已确定 FAIL 而按规则未进入真实 Chrome，不记 BLOCKED；审查前后正式源码冻结哈希一致，临时副本已清理。
- 01D-1 / 01D-2 已关闭原 01D 的 salt 漂移、旧 picker 复活和正式测试缺口；新增 9 项正式对抗测试后，全量为 48 files / 443 tests。
- 01D-3 已独立重跑定向测试、全量质量门和候选哈希，确认上述三项修复有效。
- 01D-3 同时确认归档静态预审 P2-02 仍存在：C V1 的 KDF 派生调用读取 IndexedDB V2 常量的共享函数，违反 01C T2 的强制版本独立合同。
- T8 因 T2 已确定 FAIL 而未进入真实 Chrome，不记 BLOCKED。
- 01D-4 / 01D-5 已用格式无关的显式参数 primitive 解耦 C V1 与 IndexedDB V2：两套调用者分别读取自己的版本常量，原 V2 包装接口不变，修复前固定 C V1 密文仍可解密。
- 开发侧定向回归、全量 49 files / 445 tests、typecheck、lint、production build 和 diff-check 均通过。
- 01D-6 已完成真实 Chrome、系统 picker、宿主可见 `.lftl`、错密零写入、正密重开和 300 / 301 / 302 双代验收，最终 PASS。
- 同名目标存在时，系统先询问是否替换；取消零写入，用户主动确认后允许创建新 C。此前“确认替换也必须拒绝”的草稿结论已撤销。
- 第二批已升级为合并版 `02A / 02C`，覆盖 FILE-006、FILE-007、FILE-008、FILE-009、FILE-003、FILE-010；02A 已执行并产生 02B，02C 已执行并产生最终判定为 `BLOCKED` 的 02D。

### Week 10 收尾修复开发侧 Gate

- `LedgerClock` 改为单次操作只读取一次 `Date` 快照；页面跨本地午夜定时刷新，并在 focus / visible 时校准，卸载清理计时器和监听。
- 依赖升级到 Next `15.5.22`、React / React DOM `19.2.8`、ESLint `9.39.5`、eslint-config-next `15.5.22`；ESLint 已迁移 flat config，dev/start 项目脚本默认显式绑定 `127.0.0.1`，但仍可被额外命令行参数覆盖。
- 普通交易、Binance 映射、未来事实逐条删除和删除全部未来事实统一复用两段删除按钮；第一次点击不 mutation、不保存，取消和重复按键路径已覆盖。
- 未来交易删除重放包含未来事实的完整交易时间线；支撑后续卖出的买入会被拒绝，先删除依赖卖出后才能继续删除买入。
- 设置密码、再次输入密码和解锁密码均支持小眼睛按住查看；pointer、键盘、失焦、页面隐藏、disabled 和 submit 后恢复遮蔽。
- 备份区常驻明文风险；普通、dirty、只读导出只声明“已发起下载”，并披露浏览器保存位置和同步目录风险；导入确认明确覆盖不合并及原文件不会被应用移动、删除或主动上传。
- 最终自动化：`npm ci` 成功安装 449 packages；42 files / 383 tests、typecheck 0 error、lint 0 warning/error、Next production build、diff-check 和主版本依赖树全部通过。
- 全量 `npm audit --json` 为 4 high / 0 critical：3 条 production 聚合项为 Next -> PostCSS / sharp，1 条 brace-expansion 仅在开发工具链。`npm audit --omit=dev --json` 的 CLI 响应连续收到损坏 gzip/JSON；使用同一 npm 11.12.1 Arborist、同一官方 advisory 端点与 `omit=dev` 树手工解压后得到 3 high / 0 critical。原 Next WebSocket SSRF advisory 不再命中。
- 可达性复核：代码没有 `next/image`、直接 sharp、PostCSS parse 或不可信 CSS / 图片处理入口；当前未识别出可达的 production high / critical，但这不代表 audit 归零或绝对安全。
- production 浏览器以虚构数据验证三处密码、普通危险删除、Binance 映射两段删除及旧 API 价格保留、真实 Binance、三图、明文导出实际文件、clear/import/reload、忘记密码强确认和持久化恢复；console 0 warning / 0 error。
- 390×844 页面 `scrollWidth = clientWidth = 390`，删除按钮展开不撑宽页面；两张宽表只在局部容器滚动。1280px 页面 `scrollWidth = clientWidth = 1280`。浏览器 CSSOM 确认 reduced-motion 下 `transform: none`、`transition-property: none`；受控测试确认删除语义不变。
- 本轮源码提交：`39a0ab6`、`bf77864`、`ad8c5ff`、`c88f06a`、`225434f`、`5a21529`、`6812caa`；已通过合并提交 `1b58948` 进入并推送源码 `main`，本地和远端功能分支均已删除。

### Week 10 三图与 Binance 最新行情

- `LedgerData.schemaVersion` 保持 `1`；加入可选 Binance 映射与价格 provenance，不破坏旧账本读取。
- 新事实和 strict import 拒绝未来日期；既有未来事实进入受限纠正模式，禁止继续普通写入或自动行情。
- Binance 客户端固定公共 `exchangeInfo` 与 `ticker/price`，8 秒超时、无重试、无轮询、无 WebSocket；失败不清旧价、不写 `0`、不阻塞本地账本。
- `priceSelectionService.selectPriceAsOf(...)` 统一服务持仓表、饼图与历史曲线；自动模式同日 Binance 优先，手动模式手动价优先。
- `positionReplay` 统一当前和历史 DCA、卖出及成本规则；三图数据只由事实临时派生。
- 已完成当前 USD 等值持仓饼图、总市值/持仓成本阶梯曲线和 365 天交易热力图，以及 1/7/30/365/全部区间与日期筛选。
- 映射、API/manual provenance 与价格快照进入既有 V2 加密链；派生图表和会话状态不进入 `LedgerData`、IndexedDB 或备份。
- production 真实 Binance 3 项成功、0 项失败；随后完成 export/clear/import/reload 闭环、全/部分缺价、删除映射保留旧 API 价、极端热力和清仓历史复验。
- 390px 首轮发现 ECharts 撑宽页面，修复后页面宽度与视口一致，宽表只局部滚动；1280px 同样无页面溢出。
- 源码提交：`bdc7a84`、`375a96f`、`dfa75a0`、`247eb8e`、`28eb0fe`、`45f2359`、`06cef3b`、`fd5391e`。
- 02B 独立补充测试已完成：T0、T2、T4 通过；T1/T5 的 production 主链和响应式通过，但 raw IndexedDB V2 envelope 证据受浏览器能力限制；T3 的未来事实隔离、删除全部、合法替换、clear 与恢复写入通过，但正式 UI 无法逐条删除未来交易/价格。
- T6 已完成只读依赖归因：production audit 含 `next` 与 `postcss` 两个 high；`next@14.2.35` 的 WebSocket upgrade SSRF 对当前 `next start` 部署面可达，记 P1。
- 02C 的历史“不通过”项已由 03B 关闭；03D 复验保留两个形式 P1 和测试基础设施阻塞。产品负责人确认这些项不影响当前实际使用，接受剩余风险并授权收口。

### Week 9 IndexedDB 静态加密候选

- `StorageAdapter.read()` 改为 `unknown | null`；AccessController 与 Repository 分别执行同一 V2 runtime validator。
- Repository 保存顺序固定为：账本校验、JSON 序列化、AES-GCM 加密、envelope 校验、IndexedDB 原子写入。
- 启动入口固定为 `page.tsx -> LedgerAccessGate -> DashboardShell(required repository)`；未解锁不挂载 Dashboard 或持久化 Hook。
- 密码和非导出 CryptoKey 仅存在于当前会话；刷新后必须重新解锁。
- 旧/未知格式和损坏 V2 不自动迁移或覆盖；忘记密码重置必须输入 `清空本地加密账本`。
- Noop EncryptionService 仅保留给隔离测试，production composition 不再引用。
- 自动化、production 主链和待用户确认项记录于 `week9_260807/01B_W9-IndexedDB静态加密执行验收记录.md`。
- 收口审查确认的问题记录于 `week9_260807/02A_W9-静态加密收口问题清单.md`。
- 收口解决方案、执行顺序与最终 Gate 记录于 `week9_260807/02B_W9-静态加密收口问题解决方案.md`。
- 首次设密写入成功但验证回读失败时，控制器保留 V2 record 并返回 recovery-required；页面自动转入解锁，不挂载 Dashboard。
- production 完成 2 条交易、1 条价格、明文导出、Dashboard clear、无刷新导入、V2 直读和刷新解锁恢复闭环。

### Week 2 功能结果

- 建立 `models / calculators / validators / utils / services / repositories / adapters` 分层骨架。
- 使用 `DecimalString -> decimal.js -> 展示格式化` 处理数量和金额精度。
- 实现 `decimalMath` 统一运算入口。
- 实现 `positionCalculator`：
  - 按时间排序和按资产聚合。
  - 买入、部分卖出、剩余成本和加权平均成本。
  - `realizedPnl`。
  - 同资产、同币种按 `recordedAt` 选择最新 `PriceSnapshot`。
  - `recordedAt` 相同时使用数组中后出现的快照。
  - 推导 `latestPrice`、`marketValue` 和 `unrealizedPnl`。
  - 无匹配快照时三个可选字段保持 `undefined`。
  - 零仓位卖出和超卖防御。
- 实现 `tradeValidator`：
  - 类型、资产、数量、价格、总额和手续费校验。
  - `quantity * price` 与 `totalValue` 容差校验。
  - 无持仓卖出、超卖和等量清仓校验。
- 将核心测试统一到 Vitest。

### Week 3 第一轮认知学习结果

- 确认 Week 3 不是继续堆功能，而是偿还 Week 1-2 的认知债务；当前只完成第一轮，不再表述为认知债已经全部还清。
- 建立源码仓库根目录第一轮判断：
  - `README.md`、`package.json`、`src/` 是优先入口。
  - `.git/`、`.next/`、`node_modules/` 基本不作为学习入口。
  - 各类 config 文件先知道用途，遇到对应问题再深入。
- 理解 `node_modules/`、`package.json`、`package-lock.json`、`npm`、`dependencies`、`devDependencies` 的基本关系。
- 初步建立 `Node.js / npm / package` 心智模型。
- 初步建立 `React / Next.js / TSX / CSS / Tailwind` 心智模型。
- 新增并开始使用学习沉淀入口 `99_tool/00-学习笔记整理.md`。

### Week 4 Day 1 路线校准结果

- 明确 Week 4 当前不直接上 IndexedDB，先打稳 React 页面内存态。
- 确认页面状态采用 `useReducer + LedgerData`，不再以零散 `useState` 作为主方案。
- 明确 `ledgerReducer` 只负责状态变化：
  - 不校验 `TradeDraft`。
  - 不计算 `Position[]`。
  - 不直接读写 IndexedDB / localStorage。
  - 不把 `Position[]` 存进 state。
- 明确 `tradeService`、`positionService` 和 UI 的边界：
  - `tradeService` 负责把交易草稿校验并生成正式交易。
  - `positionService` 只读 `LedgerData.trades` 和 `LedgerData.priceSnapshots`，调用 `calculatePositions(...)`。
  - UI 收到 service 成功结果后 dispatch action；失败只展示 errors，不污染 `LedgerData`。
- 升级当前总结构图，并将旧图与旧修正清单移入 `Z99Archive`：
  - 当前总图：`01一些进度/日志/00-账本核心架构与数据流图-当前版.canvas`
  - 归档旧图：`01一些进度/日志/Z99Archive/000-账本核心架构与数据流图-横向泳道版.canvas`
- 新增 Week 4 Day 1 文档：
  - `01一些进度/日志/week4_260703/01A_W4-React状态与Service职责拆解.md`
  - `01一些进度/日志/week4_260703/01B_W4-useReducer状态地基执行与验收标准.md`
  - `01一些进度/日志/week4_260703/01C_W4-总结构图新版相较旧版优势.md`
  - `01一些进度/日志/week4_260703/99_Week4日志_260705.md`

### Week 4 Gate 1 reducer 状态地基结果

- 新增源码状态层：
  - `src/state/initialLedgerData.ts`
  - `src/state/ledgerReducer.ts`
  - `src/state/ledgerReducer.test.ts`
- `initialLedgerData` 提供空的内存版 `LedgerData`。
- `ledgerReducer` 当前支持：
  - `trade/add`
  - `trade/delete`
  - `ledger/reset`
- 已验证 reducer 边界：
  - 不调用 `validateTradeDraft(...)`。
  - 不调用 `calculatePositions(...)`。
  - 不保存 `Position[]`。
  - 不读写 `localStorage` / `IndexedDB`。
- 新增执行记录：
  - `01一些进度/日志/week4_260703/02A_W4-Gate1-reducer状态地基实现记录.md`

### Week 5 Day 1 事实盘点与路线重排结果

- 已按 2026-07-10 的源码事实确认：Week 4 只有 Gate 1 完成，Gate 2-5 未完成。
- 已确认 Week 5 不能直接进入 IndexedDB，必须先补完 React 内存态页面闭环。
- 新版主线锁定为：
  - Week 5-6：补完 Gate 2-5。
  - Week 7：IndexedDB。
  - Week 8：导入导出。
  - Week 9：加密。
  - Week 10：两张保留图表。
  - Week 11：benchmark 协议、生成器、harness 与 pilot。
  - Week 12：论文证据与 P0 release gate。
  - Week 13：最终 benchmark 与暑假版冻结。
- Week 12 NLP 与 Week 13 Agent 移出当前主线；第三张图、100k benchmark 和 UI 美化不进入本轮硬通过线。
- Week 5 Day 1 只做事实盘点、认知债盘点、计划重排和文档同步，不写源码。
- 当前状态、总路线、Week 4 历史状态、Week 5-13 每日清单与 Checklist 已完成同步。
- Week 5-13 每周目录现在都包含且只包含两份 `00` 入口文件：每日执行清单与 Checklist。
- 新增 `04A-毕业论文执行范围调整-2026-07-10.md`，原论文方案保留为历史基线。
- 独立源码仓库 README 已同步当前事实，并以提交 `901e370` 收口“已实现 / 计划接线”边界。

### Week 5 Day 2 positionService 结果

- 新增源码：
  - `src/services/positionService.ts`
  - `src/services/positionService.test.ts`
- 对外接口为 `getPositionsFromLedger(ledgerData: LedgerData): Position[]`。
- Service 只读取 `LedgerData.trades` 和 `LedgerData.priceSnapshots`，并调用既有 `calculatePositions(...)`。
- `Position[]` 只临时派生，不保存、不缓存，也不进入 reducer state。
- 测试覆盖空账本、无价格快照和有价格快照三类场景。
- 三个功能提交为 `fe7606f`、`0b10323`、`a0a1834`。
- 已通过本地 merge commit `12da42b` 合入源码 `main`，功能分支已删除，未推送远端。
- Day 2 结束时完整 Gate 2 仍需 Day 3 Dashboard 接入；该缺口现已在下一节关闭。

### Week 5 Day 3 Dashboard 真实持仓结果

- `DashboardShell.tsx` 已改为 client component。
- 页面使用 `useReducer(ledgerReducer, initialLedgerData)` 管理当前 `LedgerData`。
- 资产汇总通过 `getPositionsFromLedger(ledgerData)` 临时派生 `Position[]`。
- 写死的 `summaryRows` 已删除；写死的 `trades` 按 Day 6 边界保留。
- 有价格持仓显示币种；无价格时不制造零市值或零未实现盈亏。
- 空账本显示六列表格内的“暂无持仓”状态。
- 新增 `src/components/dashboard/DashboardShell.test.ts`，并为 Vite 8/Oxc 配置 automatic JSX runtime。
- Dashboard 定向测试、全量测试、lint、build、边界扫描、浏览器冒烟和独立代码复审均通过。
- Gate 2 已关闭。

### Week 5 Day 4 生产内置资产结果

- 新增 `src/data/builtInAssets.ts` 与对应测试，固定提供 BTC、ETH、ADA 三项生产资产。
- `createBuiltInAssets()` 每次返回新的数组和新的 Asset 对象，避免不同账本共享可变引用。
- `createInitialLedgerData()` 现在使用生产资产目录；交易、价格快照和手续费规则仍为空。
- `ledger/reset` 恢复内置资产并清空用户数据；连续 reset 不复用资产数组或对象。
- Validator 的 5 条 golden 草稿已改用生产初始化资产；BTC、ETH、ADA 可通过资产存在校验，DOGE 仍被拒绝。
- 生产代码未导入 `src/test/fixtures.ts`，也未引入网络请求、动态时间、随机 ID、新依赖或 schema 变化。
- 内置资产只用于新建账本和 reset；未来 hydrate/import 以保存或导入的完整 `LedgerData.assets` 为准，不自动混入内置资产。
- 全量测试、lint、build 与边界扫描均已通过。

### Week 5 Day 5 安全交易创建结果

- `tradeValidator` 现在会把候选交易加入同资产完整时间线后按 `occurredAt` 排序检查；同时间保留已有数组顺序，候选交易排在最后。
- 未来买入不能支撑更早卖出，回填卖出不能破坏后续已有卖出；任一时点出现负持仓时返回 `INSUFFICIENT_HOLDINGS`。
- 候选币种必须与 Asset `quoteCurrency` 及同资产已有交易一致，否则返回 `CURRENCY_MISMATCH`。
- 新增 `src/services/tradeService.ts` 和对应测试，对外接口为 `createValidatedTrade(...)`。
- Service 复用 Validator；成功返回正式 `Trade`，校验失败与 ID/时钟依赖失败分层返回。
- ID 与已有 Trade ID 比较，最多尝试 3 次；获得唯一 ID 后只读取一次时间。
- Service 不 dispatch、不调用 Calculator 或 positionService，也不修改输入或 `LedgerData`。
- 独立复审未发现阻断、高风险或中风险问题；全量测试、lint、build、diff-check 与生产边界扫描均已通过。
- 三个功能分支提交为 `ea08b0e`、`81de777`、`b53b692`；通过合并提交 `7ed6ca5` 进入并推送源码 `main`，功能分支已删除。

### Week 5 Day 6 真实交易列表结果

- 删除 Dashboard 文件级写死交易数组，交易列表改读当前 `ledgerData.trades`。
- 新增纯视图 `TradeTable`，只接收 `readonly Trade[]`，不使用 hook、dispatch、Service、Validator、Calculator 或第二份交易 state。
- 正式 Trade 固定映射日期、类型、资产、数量、均价和总金额；`buy / sell` 分别显示“买入 / 卖出”。
- 空账本在六列表格内显示专属空状态；交易行使用 `trade.id` 作为 key。
- 列表按账本保存顺序展示，不排序、不修改输入数组，也不使用 JavaScript 浮点重算金额。
- 隔离测试覆盖空状态、六列字段、类型映射、输入顺序、不可变性和 Reducer 输出到纯视图的契约。
- Dashboard 定向测试、全量测试、lint、生产 build、diff-check、静态边界扫描和浏览器初始空状态冒烟均通过。
- 独立复审未发现阻断性代码缺陷；确认非空 Dashboard 真实 dispatch、未来导入运行时校验、显示顺序和长列表性能仍需后续关闭。
- 源码功能与 README 提交为 `c5383a9`、`b633de9`；通过合并提交 `a7ac21c` 进入并推送源码 `main`。

### 07A 六项风险补漏分支结果

- P0-01 已关闭：Dashboard 交易表单已接入 `tradeService -> validator -> dispatch -> 列表/持仓`。
- P0-02 已关闭：删除前通过 Service 验证候选账本；会破坏后续卖出时间线的删除被阻止。
- P0-03 已关闭：价格草稿校验、正式快照 Service、Reducer action 和页面写入链已接通。
- P0-04 已关闭：新增完整 `LedgerData` 运行时 Validator；保存数据只有在校验通过后才能 hydrate。
- P1-01 已关闭：新增 jsdom、Testing Library 和 user-event 真实交互回归。
- P1-02 已关闭：持久化契约统一为 `Repository -> EncryptionService -> StorageAdapter -> IndexedDB envelope`；空库返回 `null`，hydration ready 前禁止写，连续写串行，失败暴露错误且不覆盖旧记录。
- 源码新增原生 IndexedDB whole-blob Adapter、整账 Repository、Noop EncryptionService、唯一组装点和页面持久化 Hook。
- 最终复审补强严格 ISO 日期、自定义资产表单同步和“恢复快照进入 reducer 后才 ready”的显式门禁。
- 新增安全删除成功路径与真实组装链 IndexedDB 重挂载测试。
- 功能分支 18 个中文提交已通过合并提交 `d936463` 进入并推送源码 `main`。
- 完整验证：18 个测试文件、152 个测试通过；lint、生产 build、diff-check 和边界扫描通过。
- 解决结果已写入 `week5_260710/07B_W5-07A风险补漏解决报告.md`。

### Week 6 React Gate 收口结果

- 资产汇总新增“剩余成本基础（暂不计手续费）”与“已实现盈亏（暂不计手续费）”，直接展示 `Position` 字符串字段。
- 空持仓调整为八列，生产组件未新增计算公式、浮点转换、state 或持久化字段。
- 新增 `DashboardShell.golden.test.tsx`，逐笔填写真实表单，覆盖 5 条 golden、BTC 价格、ADA 超卖和两类删除。
- 生产 UI 验收通过：BTC / ETH / ADA 数量、剩余成本、已实现盈亏正确；BTC `70000 USD` 得到市值 `11.4716 USD`、未实现盈亏 `0.4716 USD`。
- ADA 超卖保持 5 条交易不变；删除 2026-04-09 ADA 买入被拒；安全删除 BTC 后变为 4 条且 BTC 持仓消失。
- 人工验收发现并关闭页面级横向溢出；390 / 1280 宽度下 document 不溢出，宽表保留容器内滚动。
- 最终验证：19 个测试文件、154 项测试通过；lint、生产 build、diff-check、边界扫描和生产浏览器控制台检查通过。
- 详细证据：`week6_260717/01B_W6-全周开发与Gate验收记录.md`。

### Week 7 Storage Gate 执行结果

- 新增通用 `persistenceOperation: "idle" | "clearing"`，dispatch、自动保存和 clear 共用同步 operation ref 与写队列。
- 正常 clear 与 hydration error 恢复 clear 均使用固定文本 `清空本地账本` 二段确认。
- clear 成功后恢复新的 BTC / ETH / ADA 初始账本，不自动保存空 envelope；第一次新用户写入才重新持久化。
- 自动化覆盖排队写入、前置 save 失败、重复 clear、clear 失败、写保护、Repository 切换、卸载和 fake IndexedDB 重挂载。
- production 固定 BTC / ETH 新增、价格、删除、刷新、clear 与首次新写入恢复通过；控制台无 warning / error。
- Storage Gate 基线验证：19 个测试文件、169 项测试、lint、production build 和 diff-check 通过。
- 2026-07-22 已在真实 Chrome production origin 的 DevTools 直接读取 `ledger:v1`：写入后确认 `formatVersion = 1`、明文完整 `LedgerData` 与无 `Position[]`；页面 clear 后确认 record 不存在。Storage Gate 为 Go。
- 源码行为提交 `02e28f3`，源码 README 同步提交 `8f96cc6`；已进入源码 `main`。
- 详细证据：`week7_260724/01B_W7-持久化Gate验收记录.md`。

### Week 7 Gap 可靠性补漏结果

- S-01 已关闭：页面区分“已加入账本”、“正在保存到本地”和“已保存到本地”，仅真实 state 变化增加 mutation version。
- S-02 已关闭：最新保存失败可安全重试，旧 snapshot、旧 Repository generation 和重复点击不能覆盖新账本。
- S-03 已关闭：pending / save error 均视为 dirty；离开页面和 Repository 切换不得静默丢弃未落盘状态。
- 最新验证：19 个测试文件、188 项测试全部通过；lint、production build 和 diff-check 通过。
- 三个功能提交 `b609e8f`、`97e8b23`、`401baff` 已通过合并提交 `529983e` 进入并推送源码 `main`。
- S-07 在本段记录时仅完成资源测量；其后续实现与验证已在下一节更新。
- D-08 完整 Gate 阻断条件与 D-09 导出真相契约已同步到 Week 8 两份 `00` 入口，但不代表 Week 8 已开始。

### Week 7 S-07 ResourcePolicy 结果

- 用户已确认 v1 阈值：文件 8 MiB，assets 500，trades 25,000，priceSnapshots 5,000，feeRules 500；ID 128，symbol/currency 32，name/platform 128，note 4,096，rawText 16,384。
- 新增独立 `ResourcePolicy`：结构/业务 `validateLedgerData(...)` 保持原职责；未来 JSON 导入可在 parse 前检查 UTF-8 文件字节数。
- 既有结构合法但超限的账本只读 hydrate；禁止 mutation、自动保存与 clear，避免覆盖或销毁旧数据。
- 新 mutation 在进入 reducer 前检查资源策略，超限 state 不会写入页面或 IndexedDB。
- 候选提交：`c2b8c06`、`7b1597d`、`dc89f35`；完整验证为 20 个测试文件、195 项测试，lint、production build 与 diff-check 通过。
- production UI 已复验新增 BTC、BTC 价格、ETH 新增、刷新恢复、删除 ETH、clear 与 clear 后刷新空账本。
- 2026-07-22 已通过真实 Chrome DevTools 补齐 G-01 / G-02：写入后直接读取 `ledger:v1` envelope，clear 后直接确认 key 不存在；详细记录见 `week7_260724/09A_W7-Storage-Gate最终验收报告_260722.md`。

### Week 8 完整账本备份恢复结果

- 新增独立 `BackupEnvelopeV1`：固定格式版本、应用版本、严格 UTC 时间、账本 schema 版本和完整 `LedgerData`；不复用 IndexedDB envelope，也不包含 `Position[]` 或 UI 状态。
- 导出从点击瞬间内存账本创建脱离引用的快照；JSON 固定缩进和末尾换行，文件名为 `local-first-trading-ledger-backup-v1-YYYYMMDD-HHmmssZ.json`，并明确备份为明文。
- 导入在 `File.text()` 前检查 8 MiB `File.size`，文本后、JSON.parse 前复核 UTF-8 字节数；随后复用完整账本 Validator 和 ResourcePolicy。
- `usePersistentLedger.replaceLedgerFromBackup(...)` 排队执行一次既有 `Repository.save(...)`；仅成功后替换 React state。写失败保留页面账本和 IndexedDB 旧记录；hydration error 可直接恢复，不先 clear。
- `importing` 操作固定 Repository 与 token，重复导入复用 Promise，并延迟导入期间的 Repository 切换；只读账本仅允许受 8 MiB 限制的救援导出。
- 只读救援提示明确声明集合或字符串超限可能导致当前版本无法重新导入；超 8 MiB 导出明确不创建备份文件。
- schema 不匹配只返回一项 `BACKUP_SCHEMA_VERSION_MISMATCH`，不再重复报告。
- 自动化已补齐 hydration error recovery、只读零写入、卸载、Repository 切换、取消/卸载后旧 `File.text()`、完整 fixture 重挂载再导出、五类拒绝、Dashboard 全入口锁定，以及合法备份实际内容精确 8 MiB / `+1 byte` 边界。
- production UI 已完成 BTC 交易与价格保存、实际文件导出、clear、同一文件回导、刷新持久化与二次导出逐字段一致。
- 用户确认曾在验收期间删除仅有的 BTC、ETH 两条交易；受控复验确认两条交易和持仓存在且刷新后仍完整，先前空页面观察已解释，不构成恢复缺陷证据。
- 本轮最终验证：23 个测试文件、239 项测试通过；lint、production build、diff-check 通过。Week 8 Gate 为 Go。

---

## 正在进行

- **Week 15 六批全部收口并合入本地 `main`。** 当前没有进行中的源码执行。
- 07 批产品定义 `07A` 与执行合同 `07B`（含开工前自查的修订 A）已产出，**可立即开工**。

---

## 下一步

### 06 批：已完成（2026-09-01 合入）

`06D` 判有条件通过，两项条件均已解除：`06C` 的两处表述已更正（S2-F 通电检查在最终提交态不成立的原因、V3 尾随空白构造的描述）；性能证据等级由产品负责人明确接受并留痕。14 笔以 fast-forward 合入，合入后在 `main` 上复跑全量 1174、typecheck、lint、冻结快照 7/7 全绿。

**留下一条今后适用的教训**：分阶段实施的批次中，早期阶段取得的通电检查结论**不会自动在最终提交态继续成立**——后续阶段可能增加新的兜底路径，使原先的单点破坏不再充分。今后跨阶段的硬测试，通电检查须在最终提交态重做一次，或明确标注其取得于哪个提交、未在最终态复验。

### 07 批：多语言机制（产品负责人 2026-08-31 重新编号）

**只搭机制，不搬文案**：一个语言开关、一张 key→文案表、挑一个页面做示范、定下测试怎么写、同步扩大 `interfaceWording` 守卫的扫描范围。规模只有几个文件，但要消掉全部未知（方案是否适配架构、821 处中文测试断言怎么处理）。方案错了只浪费一个页面。

**待定一：用现成的 i18n 库还是自研一个小的。** 产品负责人尚未决定，起草 `07A` 时须把两种方案在本仓库的实际形态、各自要改哪些文件写出来对比。倾向自研：两个真正需要库的能力——数字本地化与日期本地化——已分别由「统一空格」与「ISO 日期」两项决定主动规避，剩下只有英文复数（`1 item / 2 items`）这一类小问题。

**待定二：文案表与 `t()` 放在哪一层。** 倾向 `src/ui/`：该目录已承载 `formatLedgerNumber.ts` 等纯展示逻辑，`@/ui` 是结构守卫认可的稳定入口，`app` 与 `features` 均可引用，无需新增源码区。

**⚠️ 2026-08-31 查明的架构事实：本仓库当前没有任何 React Context**（全仓 `createContext` 零命中），状态一律靠 props 逐层传递。07 若采用 Context 提供 `t()`，将是本项目第一个 Context。这是一个需要在 `07A` 中明确论证的架构决定，不得默认引入——替代方案是模块级单例加顶层一次强制重渲染，但那不符合 React 的数据流习惯。

**⚠️ 2026-08-31 核实：多语言／国际化在毕业论文原方案 `04-毕业论文项目方案.md` 与范围调整 `04A` 中一次都没有出现**——既不在论文范围内，也不在 Future Work 里。所谓「论文提交前硬性前置」是建立 `CS2026` 英文 worktree 时形成的工程判断，**不是论文文档记载的要求**。它仍然值得做（论文截图与答辩演示需要评审读得懂界面），但性质是**产品负责人的判断，不是论文的硬约束**，此前表述已过强，据此更正。

**数字表示法已定**：全部语言统一用空格作千分位、点作小数点（`1 234 567.89`），不随语言切换。

**语言偏好存放位置待定**，但有一条硬约束：**不得存进账本文件**。界面语言是「这台电脑的偏好」而非「账本的数据」，存进去会迫使 `ledgerSchemaVersion` 为一个 UI 设置而升版，并**当场使 2026-08-28 那份 B 文件失效**（见风险栏）。

### 08 批：拆大文件、重构、整理目录

`src/app/` 现为 40 个文件、23,583 行，其中 `usePersistentLedger.ts` 1,904 行、`DashboardShell.tsx` 1,575 行、`LedgerAccessGate.tsx` 1,276 行。**重点是拆大文件，不是建文件夹**——分进文件夹后单文件仍然过长，找东西照样难。顺序：先看清大文件在干几件事 → 一件一件拆出去（拆一件、测一次、提交一次）→ 拆完再决定分几个目录。

**硬边界：08 批不得改变任何外部行为。** 删死代码可以，合并重复逻辑可以，但不得「顺手优化这个算法」或「这里逻辑好像有问题我改一下」。08 的价值就在于它的 diff 可以被机械验证；一旦混进主观改进，验收者就无从判断。发现真问题记下来另开批次。

两条已查明的约束：`src/app/` 是 Next.js App Router 的路由目录，子文件夹会被当作网址路径，需用下划线前缀规避（动手前须验证）；源码结构守卫把 `app` 视为单一扁平区域、只有 `@/app` 一个稳定入口，外部 11 处引用全部合规——**扁平是为防止循环依赖的设计，不是疏忽**。守卫管的是 import 路径而非物理目录，故加子目录本身不违规。

### 09 批：把文案搬进文案表

约 1,260 行界面文案（`features` 900／`app` 360）搬进 07 建好的文案表。纯体力活，可按页面分批交付、随时中断随时继续。

**其中唯一要动逻辑的是 `src/core` 里 11 处字符串中的中文**（如 `ledgerFactPolicy.ts:127`）：纯计算层不应生成用户可见文案，应只吐错误码由界面层翻译。该模式项目里已有先例（5 处 `*ErrorCode` 类型定义），只是未贯彻到底。**是否把这 11 处并入 09 还是单独立项，待定。**

**测试断言大概率一行不用改**：文案搬表后界面渲染出的仍是同样中文，只要在测试初始化里把语言钉死为中文，`getByText("交易列表")` 照样命中。做法为「一行 setup + 另写十余条专测语言切换的测试」。

### 待排：14 处排序确定性小批次

`localeCompare` 未指定语言共 14 处（另 3 处显式 `"en"`），其中 `cashReplay.ts:102`、`ledgerDate.ts:83`、`activityService.ts:81` 在派生计算路径上。产品负责人已决定在 06 批之后作为独立小批次修复，修复时必须跑 04 批冻结派生快照确认排序结果一字未变。

### 论文侧（产品负责人 2026-08-31 决定：排到 07／08／09 全部完成之后）

**产品负责人明确裁决：先把软件做好，再处理论文事务。** 优先序为 实验证据 → 写论文正文 → 导师确认邮件 → NLP 模块。

一项反对意见已提出并被驳回，记录在案：**导师确认邮件的回复周期不由用户控制，且后续实验与三章论文都建立在「存储架构由 IndexedDB 改为 `.lftl` 加密文件」这一偏离被接受的前提上**；若导师最终不接受，基于该前提完成的工作将失效。产品负责人已知悉该风险并选择承担。**此后不再重复提出。**

以下条目保持登记，等待产品负责人另行启动：

1. 更新 `04A` 那张已过期的「原方案承诺 vs 当前执行口径」对照表，补上存储架构由 IndexedDB 改为 `.lftl` 文件这一架构级偏离，以及图表库 Recharts→ECharts、第三张图变为交易热力图、benchmark 量级超额完成。
2. 依 `04A` 的「导师确认点」与新增偏离，整理一份待确认清单交导师。**未经确认，不把工程调整表述为导师批准的论文范围变更。**
3. 论文素材已留存两条「考虑过但没选」：SQLite-WASM 作为存储层（见 `99_W15` 2026-08-31 日志二），以及 event sourcing／只追加账本（见 `06A`）。两条都要注意**否决理由是性能不是安全**。

### 长期边界（不由上述批次自动扩权）

真实数据操作、NLP 与桌面端路线进入新任务时另行确认。远端 `origin/CS2026` 的删除与 `main` 的推送均属 push 操作，只能由用户本人执行。

---

## 阻塞与风险

- Week 14 没有新的独立 R2 复验；产品负责人已接受现有开发证据并批准发布，但该决定不得替代或冒充独立 `PASS`。
- 纯浏览器没有操作系统级同目录临时文件、稳定文件锁、原子替换、目录同步和进程死亡后的启动恢复闭环；这些是已接受的平台上限，不冒充已修复缺陷。
- 当前保存合同只能承诺：正常成功必须完整复读；仅在能证明安全时补偿；无法证明磁盘结果时停止后续写入、明确提示结果无法确认，且绝不显示成功或继续盲写。
- USDT、含费 P&L、版本 2、旧整账退役、平台字段和固定 / 百分比手续费规则均已进入 `main`；本地 Agent 仍未实现。
- V2 核心账本在开发侧已经可用；在集中独立复验前，重要数据需保留独立备份，不宣称已经取得独立认证。
- Week 12 开发侧已经取得双标签、权限、恢复、重连、清空和 raw IndexedDB 证据；Week 11 `02D BLOCKED` 只保留为历史原判。
- 将来的集中独立 PASS 只能证明当时最新 `main`，不能追溯改写 Week 11 02D 或此前延期状态。
- NLP 不再依赖旧 `USE-001`，但模型超时、歧义未清、revision 变化和重复确认都必须整批零写入。
- 比赛、课程、产品和论文证据不得互相冒充；`CS2026` 当前暂停且本轮未进入其 worktree。

---

## 关键入口文件

```text
当前 Week 13 入口：
01一些进度/日志/week13_260811/01A_W13-main-源码目录重构执行计划.md
01一些进度/日志/week13_260811/01B_W13-main-源码目录重构执行报告.md
01一些进度/日志/week13_260811/01C_W13-main-源码目录重构独立审计问题与修复清单.md
01一些进度/日志/week13_260811/01D_W13-main-源码目录重构R1修复报告.md
01一些进度/日志/week13_260811/01E_W13-main-源码目录重构主干发布记录.md
01一些进度/日志/week13_260811/02A_W13-main-UI重构已确认设计记录.md
01一些进度/日志/week13_260811/02C_W13-main-UI重构执行与测试报告.md
01一些进度/日志/week13_260811/99_W13-第十三周日志.md

Week 12 历史入口：
01一些进度/日志/week12_260802/000_W12-规划文件关系与批次命名规则.md
01一些进度/日志/week12_260802/00A_W12-网页优先产品共识与架构边界.md
01一些进度/日志/week12_260802/00B_W12-总需求快照与待办清单.md
01一些进度/日志/week12_260802/00C_W12-main-网页与AI开发批次路线.md
01一些进度/日志/week12_260802/00D_W12-已知问题与验收缺口清单.md
01一些进度/日志/week12_260802/01A_W12-main-手续费进入成本与净盈亏执行文档.md
01一些进度/日志/week12_260802/01B_W12-main-手续费进入成本与净盈亏执行报告.md
01一些进度/日志/week12_260802/01C_W12-main-手续费进入成本与净盈亏独立审查执行文档.md
01一些进度/日志/week12_260802/01D_W12-main-手续费进入成本与净盈亏独立审查报告.md
01一些进度/日志/week12_260802/01R1A_W12-main-旧账Binance映射静默写回修复执行文档.md
01一些进度/日志/week12_260802/01R1B_W12-main-旧账Binance映射静默写回修复报告.md
01一些进度/日志/week12_260802/01R1C_W12-main-旧账Binance映射静默写回独立复验执行文档.md
01一些进度/日志/week12_260802/01R1D_W12-main-旧账Binance映射静默写回独立复验报告.md
01一些进度/日志/week12_260802/02A_W12-main-版本2与手续费规则执行文档.md
01一些进度/日志/week12_260802/02B_W12-main-版本2与手续费规则执行报告.md
01一些进度/日志/week12_260802/02C_W12-main-版本2与手续费规则独立审查执行文档.md
01一些进度/日志/week12_260802/99_W12-第十二周日志.md

Week 11 历史入口（不作为当前执行计划）：
01一些进度/日志/week11_260727/000_W11-规划文件关系与批次命名规则.md
01一些进度/日志/week11_260727/00A_W11-产品战略重构已确认共识.md
01一些进度/日志/week11_260727/00B_W11-总需求快照与待办清单.md
01一些进度/日志/week11_260727/00C_W11-NLP前预计开发批次清单.md
01一些进度/日志/week11_260727/00D_W11-已知缺陷与延期处理清单.md
01一些进度/日志/week11_260727/01A_W11-C文件合同与安全保存执行文档.md
01一些进度/日志/week11_260727/01B_W11-C文件合同与安全保存执行记录.md
01一些进度/日志/week11_260727/01C_W11-C文件合同与安全保存独立测试审查执行文档.md
01一些进度/日志/week11_260727/01D_W11-C文件合同与安全保存独立测试审查报告.md
01一些进度/日志/week11_260727/02A_W11-C文件上一版恢复与单写入者执行文档.md
01一些进度/日志/week11_260727/02B_W11-C文件安全闭环与正式接管合并执行记录.md
01一些进度/日志/week11_260727/02C_W11-C文件上一版恢复与单写入者独立测试审查执行文档.md
01一些进度/日志/week11_260727/02D_W11-C文件安全闭环与正式接管合并独立测试审查报告.md
01一些进度/日志/week11_260727/03A_W11-B历史导入预检与整本原子替换合并执行文档.md
01一些进度/日志/week11_260727/03B_W11-B历史导入预检与整本原子替换合并执行记录.md
01一些进度/日志/week11_260727/03C_W11-B历史导入预检与整本原子替换合并独立测试审查执行文档.md
01一些进度/日志/week11_260727/99_W11-开发日志_260727.md
01一些进度/日志/week4_260703/00_W4-Week4-Checklist.md
01一些进度/日志/week4_260703/00_W4-Week4每日执行清单.md
01一些进度/日志/week4_260703/01A_W4-React状态与Service职责拆解.md
01一些进度/日志/week4_260703/01B_W4-useReducer状态地基执行与验收标准.md
01一些进度/日志/week4_260703/01C_W4-总结构图新版相较旧版优势.md
01一些进度/日志/week4_260703/02A_W4-Gate1-reducer状态地基实现记录.md
01一些进度/日志/week4_260703/99_Week4日志_260705.md
01一些进度/日志/week5_260710/00-Week5每日执行清单.md
01一些进度/日志/week5_260710/00-Week5-Checklist.md
01一些进度/日志/week5_260710/06A_W5-Day6-真实交易列表前置审查与开发计划.md
01一些进度/日志/week5_260710/06B_W5-Day6-代码风险审查与后续补漏.md
01一些进度/日志/week5_260710/07A_W5-前五周开发复盘与Week6-7风险补漏清单.md
01一些进度/日志/week5_260710/07B_W5-07A风险补漏解决报告.md
01一些进度/日志/week5_260710/99_Week5日志_260710.md
01一些进度/日志/week6_260717/00-Week6每日执行清单.md
01一些进度/日志/week6_260717/00-Week6-Checklist.md
01一些进度/日志/week6_260717/01A_W6-全周一次性收口前置审查与执行文档.md
01一些进度/日志/week6_260717/01B_W6-全周开发与Gate验收记录.md
01一些进度/日志/week6_260717/99_Week6日志_260717.md
01一些进度/日志/week7_260724/00-Week7每日执行清单.md
01一些进度/日志/week7_260724/00-Week7-Checklist.md
01一些进度/日志/week7_260724/01A_W7-持久化Gate收口与Week8衔接执行计划.md
01一些进度/日志/week7_260724/01B_W7-持久化Gate验收记录.md
01一些进度/日志/week7_260724/99_Week7日志_260724.md
01一些进度/日志/week8_260731/01A_W8-完整账本备份单日压缩执行计划.md
01一些进度/日志/week9_260807/01A_W9-IndexedDB静态加密执行清单.md
01一些进度/日志/week9_260807/01B_W9-IndexedDB静态加密执行验收记录.md
01一些进度/日志/week9_260807/02A_W9-静态加密收口问题清单.md
01一些进度/日志/week9_260807/02B_W9-静态加密收口问题解决方案.md
01一些进度/日志/week9_260807/99_W9-加密收口开发日志_260724.md
01一些进度/日志/week10_260814/00-Week10-Checklist.md
01一些进度/日志/week10_260814/00-Week10每日执行清单.md
01一些进度/日志/week10_260814/01C_W10-三图与Binance行情执行计划.md
01一些进度/日志/week10_260814/02A_W10-三图与Binance行情执行验收记录.md
01一些进度/日志/week10_260814/02B_W10-三图与Binance行情独立测试标准.md
01一些进度/日志/week10_260814/02C_W10-三图与Binance行情独立测试结果.md
01一些进度/日志/week10_260814/02D_W10-三图与Binance行情不通过项通俗说明.md
01一些进度/日志/00-账本核心架构与数据流图-当前版.canvas
01一些进度/日志/00-W2-W12总路线图与执行说明.md
00开始前的一些准备文件/04-毕业论文项目方案.md
00开始前的一些准备文件/04A-毕业论文执行范围调整-2026-07-10.md

当前产品源码入口：
01一些进度/产出/LocalFirstTradingLedger/README.md
01一些进度/产出/LocalFirstTradingLedger/src/README.md
01一些进度/产出/LocalFirstTradingLedger/src/app/DashboardShell.tsx
01一些进度/产出/LocalFirstTradingLedger/src/app/LedgerWorkspaceFrame.tsx
01一些进度/产出/LocalFirstTradingLedger/src/app/HomeWorkspace.tsx
01一些进度/产出/LocalFirstTradingLedger/src/app/RecordWorkspace.tsx
01一些进度/产出/LocalFirstTradingLedger/src/app/TransactionsWorkspace.tsx
01一些进度/产出/LocalFirstTradingLedger/src/app/TransferWorkspace.tsx
01一些进度/产出/LocalFirstTradingLedger/src/app/SettingsWorkspace.tsx
01一些进度/产出/LocalFirstTradingLedger/src/app/useLedgerWorkspaceSession.ts
01一些进度/产出/LocalFirstTradingLedger/src/app/usePersistentLedger.ts
01一些进度/产出/LocalFirstTradingLedger/src/core/models/types.ts
01一些进度/产出/LocalFirstTradingLedger/src/core/state/ledgerReducer.ts
01一些进度/产出/LocalFirstTradingLedger/src/features/trades/TradeForm.tsx
01一些进度/产出/LocalFirstTradingLedger/src/features/trades/TradeTable.tsx
01一些进度/产出/LocalFirstTradingLedger/src/platform/files/ledgerFileRepository.ts
01一些进度/产出/LocalFirstTradingLedger/src/test-support/sourceLayout.test.ts
01一些进度/产出/LocalFirstTradingLedger/vitest.config.ts
```

---

## 最近验证结果

源码测试最近验证时间：2026-08-22 的既有 R2 记录（定向 1 / 10，Week 14 闭集 58 / 739，源码变化后全量两轮及 README 后最终全量均为 85 / 919）；本次发布收口未重跑
源码 lint / build 最近验证时间：2026-08-21 的既有记录（typecheck、lint、Next 15.5.22 production build、两类 diff-check 全绿）；本次未重跑
源码生产浏览器验证时间：2026-08-21 的既有记录（Guest Profile、production origin、macOS 原生 picker 和虚构 `W14-R2-DEV-FAKE` 账本）；本次未重跑
源码仓库当前分支：`main`
源码仓库状态：`9855f842f172d792582c9b0f5d40367d2bedfe46`，tree `6ea62e2d25b865be1535c2a458328352ad8ebd08`；工作树 clean，与 `origin/main` 为 `0/0`。本地及远端只保留 `main` 与 `CS2026`。

Week 13 UI 与首页交易活动区最终开发／发布验证：

```text
第一轮 UI 定向   -> 阶段 1—7 与两个浏览器修复组全部 PASS
首页打磨定向     -> 9 files / 86 tests PASS
全量测试         -> 第一轮 UI 72 / 785；首页打磨候选两轮 73 / 797；合入 main 后新鲜复跑 73 / 797，全部 PASS
typecheck / lint -> 合入后 exit 0；0 warning / error
production build -> 合入后 Next 15.5.22；5 个静态页面；route / 319 kB；first load 422 kB
whitespace / diff-> PASS
真实 Chrome      -> 两份虚构 V2 .lftl、五页导航、保存锁定重开、草稿、筛选、安全删除、整本恢复、设置、清空；首页打磨 1280 / 1440 / 1100 / 390、Tooltip、日期定位 PASS
Git 发布          -> PR #8 合入第一轮 UI；76213d4 合入首页打磨；0d0cb55 同步 README；main 已推送，功能分支已清理
开发侧结论       -> 02C、03B 均为 PASS
独立状态         -> 未执行覆盖当前 main 的集中独立验收
```

Week 12 第二批最终开发验证：

```text
定向测试         -> 10 files / 144 tests；12 files / 282 tests PASS
全量测试         -> 58 files / 727 tests PASS
typecheck / lint -> exit 0；0 warning / error
production build -> Next 15.5.22；route / 303 kB；first load 405 kB
whitespace / 残留 -> PASS；无 .only、.skip、debugger、console.log/debug、冲突标记或手续费原生浮点公式
真实 Chrome      -> V2 新建 / 重开、FeeRule、备份 / 恢复、V1 拒绝、权限、双标签、外部 revision、删除 / 重选、raw IndexedDB、390 / 1280 视口 PASS
开发侧结论       -> 02B PASS；已合入 main
独立状态         -> 02C 未执行；无 02D；等待未来集中复验
```

Week 12 第一批 R1 最终验证：

```text
定向测试         -> R1 170 / 文件安全 242 / PNL 与图表 160 tests PASS
全量测试         -> 58 files / 730 tests PASS
typecheck / lint    -> 0 error；0 warning / error
production build   -> Next 15.5.22 编译成功；5 个静态页面生成成功
whitespace / debug -> PASS；无 .only、.skip、debugger、console.log/debug
固定金额           -> 6505 / 2797 / 2602 / 195 / 3903 PASS
真实 Chrome       -> 独立 absent / null / explicit / 新 USDT 专用虚构 .lftl；逐字段导出、真实 Binance、固定 PNL、缺价、三图、响应式与锁定重开均 PASS
控制台             -> 0 error
开发侧结论       -> 原 01B PASS；原 01D P0 后 01R1B PASS
独立结论         -> 原 01D FAIL 历史保留；01R1D PASS；PNL-001～003 已勾选
```

Week 11 第三批开发与真实成功链验证：

```text
Stage 2 定向组合  -> 13 files / 357 tests PASS
01 / 02 回归组合  -> 185、223、210、187 tests 全部 PASS
repository 恢复边界 -> 2 files / 79 tests PASS
lifecycle 定向    -> 3 files / 82 tests；独立复审补跑 3 files / 89 tests PASS
最终全量          -> 55 files / 698 tests PASS
typecheck / lint  -> 0 error；0 warning / error
production build  -> Next 15.5.22 编译成功；5 个静态页面生成成功
whitespace / debug -> PASS；无 .only、.skip、debugger、console.log/debug
真实 Chrome       -> macOS picker、新 C、300 笔 B、hash/revision、锁定重开正常链 PASS
最终结论          -> 03B BLOCKED；03C 后来在执行前作废；03D 不生成；Week 11 00B 不回写
```

Week 11 第一批独立验证：

```text
定向测试         -> 4/43、3/11、4/38、3/79、4/39 全部通过
全量测试         -> 49 files passed, 445 tests passed
typecheck / lint -> 0 error；无 warning / error
production build -> Next 15.5.22 编译成功；5 个静态页面生成成功
diff-check       -> tracked 与两个新增文件均无 whitespace error
F-01～F-04       -> salt、Hook、stale picker、KDF 版本解耦全部 PASS
T8 Chrome        -> 系统 picker、宿主可见文件、错密/正密、取消、300/301/302 全部 PASS
同名替换         -> 系统提示后用户主动确认属于正常行为，不是 P0
最终结论         -> 01D-6 PASS；P0/P1/P2/P3/BLOCKED 均为 0
历史结论         -> 原 01D / 01D-3 的 FAIL 保留，但不代表当前第一批状态
```

Week 10 验证：

```text
npm test         -> 42 files passed, 383 tests passed
npm run typecheck -> 0 error
npm run lint     -> No ESLint warnings or errors
npm run build    -> Compiled successfully；5 个静态页面生成成功
git diff --check -> 源码与外层仓库均通过
真实 Binance     -> BTC / ETH / ADA 更新 3 项、失败 0 项
自动/手动选价    -> 同日 Binance 优先与手动优先模式均通过
三图             -> 饼图、阶梯曲线、365 天热力图及五档区间通过
缺价降级         -> 全缺价、部分缺价、单资产与曲线断点通过
极端热力/清仓    -> 单日 8 笔对比 1 笔、五档标签与清仓历史保留通过
行情失败边界     -> 删除 ADA 映射后旧 API 价保留；真实网络超时由确定性合同测试覆盖
恢复主链         -> 导出、clear、无刷新导入、刷新解锁恢复通过
日期边界         -> production 拒绝 2099-01-01；旧未来事实隔离、整体恢复和按 ID 逐条删除已实现
390 / 1280       -> 页面级无横向溢出，宽表只局部滚动
浏览器控制台     -> 0 warning / 0 error
独立 T2          -> timeout / offline / 418 / 429 / 500 / partial 全部通过
独立 T3          -> 未来事实隔离、逐条删除、删除全部、合法替换与 clear 主场景通过
独立 T4          -> mapping / 整账替换 / 并发普通交易的旧响应保护通过
独立 T5          -> 响应式、raw V2 envelope、真实备份和重新解锁恢复通过；部分 harness 子项 BLOCKED
独立 T6          -> Next 15.5.22 与 loopback 已确认；正式 audit 重跑被审批层 BLOCKED
独立总判定       -> 03D 形式不通过；产品负责人接受剩余低风险并授权主线收口
```

Week 7 验证：

```text
npm test         -> Storage Gate 基线 19 files / 169 tests；B 批次后 19 files / 188 tests；S-07 后 20 files / 195 tests
npm run lint     -> No ESLint warnings or errors
npm run build    -> Compiled successfully
git diff --check -> 通过
production 主链 -> BTC/ETH 新增、价格、删除、刷新、clear、首次新写入恢复通过
浏览器控制台     -> 无 warning / error
故障注入         -> load/save/clear、排队写、重复 clear、切 Repository、卸载通过
production DevTools -> G-01 写入后 envelope、G-02 clear 后 record 不存在均已直接确认
Gate             -> Go；Week 8 单日压缩执行计划已建立，源码待实施
```

Week 6 验证：

```text
npm test         -> 19 files passed, 154 tests passed
npm run lint     -> No ESLint warnings or errors
npm run build    -> Compiled successfully
git diff --check -> 通过
生产 UI          -> golden、价格、超卖、两类删除通过
宽窄屏           -> 390 / 1280 页面级无横向溢出
浏览器控制台     -> 无 warning / error
边界扫描         -> UI 无公式；fixture 不进生产；Position[] 不进 state / 存储
```

07A 补漏验证：

```text
npm test       -> 18 files passed, 152 tests passed
npm run lint   -> No ESLint warnings or errors
npm run build  -> Compiled successfully
git diff --check -> 通过
边界扫描       -> IndexedDB 只存在于 Adapter；具体实现只在唯一组装点实例化
重挂载回归     -> 真实组装链可恢复交易和价格
```

Day 6 提交：

```text
c5383a9 功能：接入第五周第六天真实交易列表
b633de9 文档：同步第五周第六天交易列表状态
a7ac21c 合并第五周第六天真实交易列表
```

Day 6 验证：

```text
Dashboard 定向测试   -> 1 file passed, 7 tests passed
npm test -- --run    -> 8 files passed, 90 tests passed
npm run lint         -> No ESLint warnings or errors
npm run build        -> Compiled successfully
git diff --check     -> 通过
浏览器初始空状态冒烟 -> 页面 200、六列表格正常、旧占位交易消失、控制台无 warning/error
独立复审             -> 无阻断性代码缺陷；4 项后续风险已记录
```

Day 5 提交：

```text
ea08b0e 功能：补强交易时间线与币种校验
81de777 功能：实现安全交易创建服务
b53b692 文档：同步第五周第五天交易服务
```

Day 5 合并与发布：

```text
7ed6ca5 合并第五周第五天安全交易服务
origin/main -> 7ed6ca5
```

Day 5 验证：

```text
npm test -- --run  -> 8 files passed, 85 tests passed
四组定向回归        -> 4 files passed, 63 tests passed
npm run lint       -> No ESLint warnings or errors
npm run build      -> Compiled successfully
git diff --check   -> 通过
独立代码复审         -> 无阻断、高风险或中风险问题
生产边界扫描         -> 通过
```

Day 3 提交：

```text
69ae742 功能：接通仪表盘真实持仓
7c633ac 功能：增加仪表盘空持仓状态
fef40d1 测试：强化仪表盘持仓断言
8d1f955 文档：同步仪表盘真实持仓状态
```

Day 4 提交：

```text
c68b3e7 功能：建立生产内置资产目录
47b1502 功能：初始账本接入内置资产
6235a69 测试：锁定资产初始化与账本重置边界
a86c6ac 测试：同步仪表盘初始资产预期
895c026 文档：记录第五周第四天内置资产结果
```

Day 4 合并与发布：

```text
1cc9acb 合并第五周第四天生产内置资产
37030c8 文档：同步第五周第四天合并状态
origin/main -> 37030c8
```

Day 4 验证：

```text
npm test -- --run  -> 7 files passed, 57 tests passed
npm run lint       -> No ESLint warnings or errors
npm run build      -> Compiled successfully
git diff --check   -> 通过
生产代码 fixture / 网络 / 动态时间 / 随机 ID 边界扫描 -> 通过
```

Day 3 验证（保留历史基线）：

```text
npm test -- --run
Test Files  6 passed (6)
Tests       53 passed (53)
```

```text
npm run lint
No ESLint warnings or errors

npm run build
Compiled successfully
```

边界扫描：`src/state/` 未发现 `validateTradeDraft`、`calculatePositions`、`localStorage`、`indexedDB`、`Position`、`PriceSnapshot`、`TradeDraft`。

边界扫描：`src/services/positionService.ts` 未发现 React、`useReducer`、Validator、Repository、`localStorage`、`IndexedDB`、成本/盈亏公式或 `try/catch`。

边界扫描：`DashboardShell.tsx` 未发现 `summaryRows`、`calculatePositions`、`localStorage`、`IndexedDB`、`test/fixtures`、`dangerouslySetInnerHTML` 或独立 `Position[]` state。

---

## 维护规则

- 只记录当前有效状态，不累计历史流水。
- 实质进展、里程碑、下一步、风险、关键入口文件或验证结果变化后更新。
- 普通讨论、只读检查或没有状态变化时不更新。
- 历史过程写入对应 Week 日志。
