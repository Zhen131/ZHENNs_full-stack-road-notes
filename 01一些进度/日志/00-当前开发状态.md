# 当前开发状态

更新时间：2026-06-27
当前阶段：Week 3 Day 2，认知债务偿还周
当前里程碑：核心账本能够根据交易和价格快照计算持仓、成本、已实现盈亏、市值和未实现盈亏，能够拒绝非法交易，并由自动化测试证明结果。

## 已完成

- 建立 `models / calculators / validators / utils / services / repositories / adapters` 分层骨架。
- 使用 `DecimalString → decimal.js → 展示格式化` 处理数量和金额精度。
- 实现 `decimalMath` 统一运算入口。
- 实现 `positionCalculator`：
  - 按时间排序和按资产聚合。
  - 买入、部分卖出、剩余成本和加权平均成本。
  - `realizedPnl`。
  - 同资产、同币种按 `recordedAt` 选择最新 `PriceSnapshot`。
  - `recordedAt` 相同时使用数组中后出现的快照。
  - 推导 `latestPrice`、`marketValue` 和 `unrealizedPnl`。
  - 无匹配快照时三个可选字段保持 `undefined`。
  - 零仓位卖出和超卖防御。
- 实现 `tradeValidator`：
  - 类型、资产、数量、价格、总额和手续费校验。
  - `quantity × price` 与 `totalValue` 容差校验。
  - 无持仓卖出、超卖和等量清仓校验。
- 将核心测试统一到 Vitest。

## 正在进行

- Week 2 Step 9–10 已通过 PR #5 合并到源码仓库 `main`。
- Week 3 已调整为认知债务偿还周，暂停真实功能开发。
- 原 Week 3 页面内存状态与 Service 接线顺延到新 Week 4（2026-07-03 开始）。

## 下一步

1. 复盘 Week 1 项目边界、第一版功能和不做事项。
2. 复盘 Week 2 数据模型、计算器、校验器和测试。
3. 用自然语言、手算和伪代码讲清 `Trade → Validator → Trade[] → Calculator → Position[]`。
4. 通过理解验收后，在 Week 4 恢复页面内存状态与 Service 接线。

## 阻塞与风险

- 页面现有资产和交易是占位数据，不能视为真实账本状态。
- `PriceSnapshot` 当前只作为结构化输入参与核心测试；价格表单、价格校验和内存状态接入已顺延到 Week 4。
- 最新时间目前按 ISO 日期/时间字符串比较；输入规范化和日期合法性由后续 `priceSnapshotValidator` 负责。
- Week 4 开发前必须先完成 Week 1–2 的理解验收，避免继续堆积黑盒代码。

## 关键入口文件

```text
01一些进度/产出/LocalFirstTradingLedger/README.md
01一些进度/产出/LocalFirstTradingLedger/src/models/types.ts
01一些进度/产出/LocalFirstTradingLedger/src/calculators/positionCalculator.ts
01一些进度/产出/LocalFirstTradingLedger/src/calculators/positionCalculator.test.ts
01一些进度/产出/LocalFirstTradingLedger/src/validators/tradeValidator.ts
01一些进度/产出/LocalFirstTradingLedger/src/validators/tradeValidator.test.ts
01一些进度/日志/week3_260626/00_W3-Week3每日执行清单.md
01一些进度/日志/week3_260626/01_W3-Week3-Checklist.md
01一些进度/日志/week2_260619/00_W2_每日执行清单.md
```

## 最近验证结果

验证时间：2026-06-26
源码仓库分支：`main`
源码仓库状态：干净，PR #5 已合并，合并提交 `9bb5aa7`

```text
npm test
Test Files  3 passed (3)
Tests       40 passed (40)

npm run lint
No ESLint warnings or errors

npm run build
Compiled successfully
```

## 维护规则

- 只记录当前有效状态，不累计历史流水。
- 实质进展、里程碑、下一步、风险或验证结果变化后更新。
- 普通讨论、只读检查或没有状态变化时不更新。
- 历史过程写入对应 Week 日志。
