# 07B_W15-main｜多语言机制执行文档

- 日期：2026-08-31
- 轨道：长期账本产品 `main`
- 工作目录：`01一些进度/产出/LocalFirstTradingLedger/`
- 依据：`07A_W15-main-多语言机制产品定义.md`
- 执行者：外部 AI；产出 `07C` 执行报告，独立验收 `07D` 另行进行

## 结论

本文件是执行合同。本批**只搭机制，不搬文案**：修 14 处排序确定性，建立文案表与 `t()`，加语言开关，**只用首页一个页面做示范**。

**开工前置：06 批必须已完成独立验收（`06D`）并合入本地 `main`。** 未合入不得开工——本批要改 `formatLedgerNumber.ts` 与大量测试初始化，与 06 的存盘改动并行会产生无谓冲突。

任何与 `07A` 产品决定冲突之处，**必须停止并在 `07C` 中记录，不得自行取舍**。特别地：任何「为了让某条文案能翻译而引入复数机制／引入第三方库／新增第二个 Context」的想法一律禁止，宁可判未完成。

---

# 零、Git 与执行边界

## 0.1 分支要求

| 项目 | 要求 |
| --- | --- |
| 仓库 | `01一些进度/产出/LocalFirstTradingLedger/`。这是唯一源码轨道 |
| 起点 | 从合入 06 批后的 `main` 最新提交切出。开工前核对分支、HEAD 与工作树 clean |
| 新分支名 | `zhennn/w15-main-i18n-mechanism` |
| 提交粒度 | 第零步（排序）独立成一笔或数笔；文案表与 `t()`、语言开关、首页示范、数字格式各自独立提交 |
| 提交信息 | 英文，命令式语气 |
| 禁止 | 不得合并回 `main`，不得 `push`，不得 rebase，不得使用破坏性 git 命令 |
| 收尾 | 分支停在最后一个提交上，工作树 clean |

根文档仓库与源码仓库分别检查、分别提交，不得混成一个提交。`07C` 属根文档仓库。

## 0.2 禁止事项

| 编号 | 约束 |
| --- | --- |
| B-01 | **不得引入任何 i18n 第三方库**，包括但不限于 `next-intl`、`react-i18next`、`formatjs` |
| B-02 | **不得引入第二个 React Context。** 本批只允许新增语言这一个 |
| B-03 | **不得使用任何依赖语法数（复数）的文案。** 一律采用「标签＋数值」写法（`07A` 2.3） |
| B-04 | **不得让数字或日期随语言变化。** 数字统一空格千分位＋点小数点，日期统一 ISO |
| B-05 | **不得修改既有测试的断言或阈值。** 允许新增测试；若既有测试因本批必须调整，停止并申报 |
| B-06 | **不得把语言偏好写入账本文件**，也不得写入任何与账本数据同生命周期的存储 |
| B-07 | **不得让 `src/core` 或 `src/platform` 引用 `@/ui`**，不得在这两层新增任何用户可见文案 |
| B-08 | **不得搬运首页以外的任何文案**，不得处理 `core` 已有的 11 处中文 |
| B-09 | **不得改动 `src/app/` 的目录结构，不得拆分任何大文件** |
| B-10 | **不得改动任何文件格式、版本号、加密参数或派生计算** |
| B-11 | **不得实现语言自动探测**（读取浏览器语言）。默认中文 |
| B-12 | 不得读取、引用或复制 `~/Downloads/history_OKX/`；不得打开任何真实 `.lftl` 或真实 B 文件 |

## 0.3 必须停止并申报的情形

| 编号 | 情形 |
| --- | --- |
| H-1 | 某条既有文案在不引入语法数的前提下无法翻译 |
| H-2 | 第零步改排序后，04 批冻结派生快照出现任何一格不符 |
| H-3 | 发现必须修改既有测试断言才能继续 |
| H-4 | 发现必须引入第三方库或第二个 Context 才能达成 `07A` 的要求 |
| H-5 | 结构守卫因文案表位置而无法通过，且调整位置也解决不了 |

---

# 一、阶段〇：14 处排序确定性（必须最先，独立提交）

## 1.1 清单

按 `07A` 1.2 的两档处理。清单以 `07A` 为准；**开工时须自行重新检索并在 `07C` 中给出检索命令与原始输出**（口径自证，同 06 批修订 B 的 B-11）。若实测数量与 `07A` 不一致，**以你的实测为准**并说明差异，不得去凑 `07A` 的数字。

## 1.2 改法

首选直接用 `<` / `>` 比较（比较对象均为纯 ASCII 的 ISO 日期串、资产代号、技术 id）；若某处确需按人类习惯排序，显式传 `"en"`。

## 1.3 闸门

| 编号 | 要求 |
| --- | --- |
| L-1 | **必须跑 04 批冻结派生等价性快照，7/7 通过**，确认排序结果一字未变。命令：`npx vitest run --config vitest.benchmarks.config.ts benchmarks/measure/derivedSnapshot.contract.ts` |
| L-2 | 本阶段独立提交，不与后续多语言改动混在同一笔 |
| L-3 | 任何一格快照不符即触发 H-2，**停止申报，不得自行调整快照** |

---

# 二、阶段一：文案表、`t()` 与语言 Context

## 2.1 放置

文案表与 `t()` 放 `src/ui/`，经 `@/ui` 导出。**不新增源码区**，结构守卫的六区不变。

## 2.2 命名规则（硬要求）

**本批必须确立并在 `07C` 中写明 key 的命名规则**，09 批将照此搬运一千余条文案。规则须能回答：区域怎么分、组件怎么标识、同一文案在多处复用时归谁。建议形如 `区域.组件.用途`，但由执行者选定并说明理由。

**命名规则一旦写明即冻结**，09 批不得另立一套。

## 2.3 行为合同

| 编号 | 要求 |
| --- | --- |
| M-1 | 三张表：中文（唯一完整表）、英文、匈牙利语。后两者允许缺项 |
| M-2 | `t(key)` 缺项时**回落到中文**，并在开发模式下告警；**不得显示空白或 key 本身** |
| M-3 | 语言 Context 的值应为稳定引用，避免每次渲染产生新对象 |
| M-4 | 语言偏好须能在刷新后保持；存放介质由执行者选定并在 `07C` 说明。**不得写入账本文件或任何与账本数据同生命周期的存储** |
| M-5 | 读取失败或值非法时**静默回落到中文默认**，不得阻塞应用启动 |

---

# 三、阶段二：语言开关与首页示范

| 编号 | 要求 |
| --- | --- |
| U-1 | 语言开关放设置页 `src/app/SettingsWorkspace.tsx`，三选一 |
| U-2 | **示范范围只有首页 `HomeWorkspace`。** 其余页面一律不动（B-08） |
| U-3 | 首页文案全部经 `t()`；三种语言均须给出译文，中文与现状逐字相同 |
| U-4 | 匈牙利语与英语译文须为可用的真实译文；**不得留占位符，不得留明显错误的机器直译**。有疑问的条目在 `07C` 中列出等裁决 |
| U-5 | 首页若存在依赖语法数的文案，按 `07A` 2.3 改写为「标签＋数值」；改写后的中文表述须在 `07C` 中列出 |

---

# 四、阶段三：数字格式与守卫

| 编号 | 要求 |
| --- | --- |
| F-1 | `src/ui/formatLedgerNumber.ts` 改为**空格千分位 + 点小数点**（`1 234 567.89`）。仍为手写实现，**不得改用 `toLocaleString` 或 `Intl`**，不得依赖系统语言 |
| F-2 | 该改动会影响既有断言中的数字文本。**这属于 B-05 的既有断言修改，须停止申报**（H-3），由产品负责人裁决后再改 |
| F-3 | 扩大 `src/test-support/interfaceWording.test.ts` 的扫描范围，使其覆盖文案表所在位置。**否则文案迁出后该守卫会静默失效** |

**F-2 是本批已知的、几乎必然触发的申报点。** 请在阶段三开始时先做检索、评估影响面，一次性申报，不要边改边问。

---

# 五、阶段四：测试

## 5.1 既有断言不改

| 编号 | 要求 |
| --- | --- |
| T-1 | 在测试初始化中把语言钉死为中文，**使既有断言无需改动** |
| T-2 | 不得修改任何既有测试断言（B-05）。除 F-2 已知的数字格式影响外，若还有其他必须改的，逐条停止申报 |
| T-3 | `07C` 须给出改动前后的全量用例数对比 |

## 5.2 新增测试（`07A` 5.2 的 T-A1～T-A8）

八条全部实现，**每条都须做通电检查**（临时破坏实现确认变红、恢复后变绿），过程与结果写入 `07C`；破坏后必须确认文件已还原。

**T-A8 最重要**：切换语言前后 `.lftl` 字节完全一致，守的是「语言偏好绝不进账本文件」这条绝对禁令。

---

# 六、通过线

| 编号 | 闸门 |
| --- | --- |
| Q-1 | 04 批冻结派生快照 7/7 |
| Q-2 | T-A1～T-A8 全过并完成通电检查 |
| Q-3 | 既有用例数不减少，无既有断言被修改（F-2 经裁决者除外） |
| Q-4 | 默认全量、typecheck、lint、production build、结构守卫、两项 whitespace 检查全过 |
| Q-5 | **四个版本号全部不变** |
| Q-6 | `core` 与 `platform` 均未引用 `@/ui`，未新增用户可见文案 |

不设性能通过线，但须给出一次冷启动与页面切换的对照，确认无可测量退化。

---

# 七、`07C` 报告要求

| 编号 | 必答 |
| --- | --- |
| R-1 | 阶段〇的自证：检索命令、原始输出、实际处理的位置清单、与 `07A` 的差异说明 |
| R-2 | 冻结派生快照结果 |
| R-3 | 文案表与 `t()` 的放置位置，以及**冻结的 key 命名规则**及其理由 |
| R-4 | 语言偏好的存放介质与选定理由；如何证明它没有进入账本文件 |
| R-5 | Context 的实现方式，以及如何保证值为稳定引用 |
| R-6 | 首页三语译文清单；U-4 中有疑问的条目；U-5 中改写过的文案 |
| R-7 | F-2 的申报内容与裁决结果；实际改动的既有断言逐条列出 |
| R-8 | T-A1～T-A8 的通电检查过程与结果 |
| R-9 | 改动前后全量用例数对比 |
| R-10 | 性能对照（冷启动、页面切换） |
| R-11 | 四个版本号的最终值 |
| R-12 | H-1～H-5 是否触发；若触发，申报了什么、如何裁决的 |
| R-13 | 强制否定性声明：未引入第三方库、未新增第二个 Context、未使用复数文案、未让数字或日期随语言变、未搬首页以外文案、未动 `src/app/` 结构、未改版本号或加密参数、未读私有数据区、未 merge／push／rebase |

**任何一项没做或没取得，如实写「未取得」并说明原因——不得留白、不得推断、不得用别的数字顶替。**

`07C` 只是开发执行报告，**不替代独立验收 `07D`，不授权合入 `main`**。
