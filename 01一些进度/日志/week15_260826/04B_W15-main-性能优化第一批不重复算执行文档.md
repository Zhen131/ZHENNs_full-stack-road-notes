# 04B_W15-main｜性能优化第一批「不重复算」执行文档

- 日期：2026-08-29
- 轨道：长期账本产品 `main`
- 工作目录：`01一些进度/产出/LocalFirstTradingLedger/`
- 依据：`04A_W15-main-性能优化第一批不重复算产品定义.md`
- 执行者：外部 AI；产出 `04C` 执行报告，独立验收 `04D` 另行进行

## 结论

本文件是执行合同。三个阶段按顺序完成：**固定证据 → 不重复算 → 复测对照**。

**阶段一与阶段二之间有一道强制闸门。** 阶段一结束后必须停止执行、交付 `04C-1` 阶段报告、等待产品负责人明确放行，方可进入阶段二。未获放行擅自进入阶段二，本批直接判失败。

任何与 `04A` 产品决定冲突之处，必须停止并在 `04C` 中记录，不得自行取舍。**特别地：任何"为了让指标达标而修改测试、放宽阈值、改写快照或降低精度"的想法一律禁止，宁可判未完成。**

---

# 零、Git 与执行边界

## 0.1 分支要求

| 项目 | 要求 |
| --- | --- |
| 仓库 | `01一些进度/产出/LocalFirstTradingLedger/`。同级的 `LocalFirstTradingLedger-CS2026/` 是论文轨道，本批完全不碰 |
| 起点 | 从 `main` 最新提交切出。开工前核对实际路径与 `git branch --show-current` 相符 |
| 新分支名 | `zhennn/w15-main-perf-no-recompute` |
| 提交粒度 | 每个阶段至少一个提交；阶段二的 P-1～P-4 四处修改必须各自独立提交 |
| 提交信息 | 英文，命令式语气 |
| 禁止 | 不得合并回 `main`，不得 `push`，不得 rebase 已有历史，不得使用破坏性 git 命令 |
| 收尾 | 分支停在最后一个提交上，工作树 clean |

根文档仓库与源码仓库分别检查、分别提交，不得混成一个提交。`04C` 属于根文档仓库。

## 0.2 禁止事项

| 编号 | 约束 |
| --- | --- |
| B-01 | 不得改动任何文件格式、版本号、加密参数、KDF 或安全边界 |
| B-02 | 不得把派生值写入 `.lftl` 或 B 文件；缓存只允许存在于内存 |
| B-03 | 不得实现虚拟列表、分页或分块存储（分别属于 05、06 批） |
| B-04 | 不得读取、引用或复制 `~/Downloads/history_OKX/`；不得打开任何真实 `.lftl` 或真实 B 文件 |
| B-05 | 不得修改既有测试的断言或阈值。允许新增测试；若既有测试因本批而必须调整，停止并在 `04C` 中申报，等待裁决 |
| B-06 | 不得修改或删除阶段一冻结的派生结果快照 |
| B-07 | 不得为了快而降低精度、改用浮点、四舍五入或跳过校验 |
| B-08 | 不得在产品中加入测试模式开关、环境变量后门，或任何跳过密码校验、加密、版本校验的分支 |
| B-09 | 不得把生成出来的账本文件、量尺结果文件提交进 Git |
| B-10 | 不得下调档位、缩短时间跨度或简化账本结构以美化数字 |
| B-11 | 阶段一除 `04A` 1.2 明确列出的常量外，不得修改任何其他产品文件 |
| B-12 | 不得跨阶段提前动手：阶段一进行中不得修改任何性能相关实现 |

## 0.3 开工前必须完成

1. 确认工作树路径与分支正确、工作树 clean。
2. 只读跑一次全量测试、`typecheck`、`lint`、production build，记录基线数字写入 `04C`。
3. 记录运行环境：操作系统与版本、浏览器与版本、CPU 型号、内存大小、是否插电、Node 版本。全部写入 `04C`。
4. 记录 `03C` 的运行环境作为对照。**若本次环境与 `03C` 不同（换机器、换浏览器版本、未插电），必须在 `04C` 显著位置写明，并且不得直接引用 `03C` 的数字作为改动前基线——这正是阶段一要求重跑 10²～10⁴ 的原因。**

---

# 阶段一：固定证据

## 1.1 步骤顺序（不可调换）

```
① 放宽上限之前，复跑 10² / 10³ / 10⁴ 三档全部指标        → E-1
② 生成并冻结 10² / 10³ / 10⁴ 三档的派生结果快照           → E-5
③ 修改资源上限常量，只改 04A 1.2 表中列出的那几个数字
④ 放宽上限之后，再跑一次 10² / 10³ / 10⁴ 三档全部指标      → E-2
⑤ 跑 10⁵ 档全部指标                                      → E-3
⑥ 跑 10⁶ 档单次探测                                      → E-4
⑦ 写 04C-1，停止，等待放行
```

②必须在③之前完成，理由：快照是"改动前的正确答案"，必须在任何产品文件被触碰之前取得。

## 1.2 允许修改的文件（阶段一唯一白名单）

| 文件 | 允许的改动 |
| --- | --- |
| `src/core/validation/resourcePolicy.ts` | 只改 `DEFAULT_LEDGER_RESOURCE_LIMITS` 中 `fileBytes`、`trades`、`cashEvents`、`assetTransfers`、`priceSnapshots` 五个数值 |
| `src/platform/files/ledgerFileContract.ts` | 只改 `MAX_LEDGER_FILE_V2_BYTES` 一个数值 |
| 测试夹具目录 | 新增派生结果快照文件 |
| `benchmarks/` | 新增快照生成与比对所需的工具代码 |

新数值见 `04A` 1.2。`assets` 保持 500 不变。`maximumCiphertextBytes` 由 `fileBytes` 派生，不单独改。

**除上表外，阶段一不得修改 `src/` 下任何文件。** 收尾时对 `src/` 的 diff 必须只包含上述两个文件的数值改动。

## 1.3 快照的具体要求

对 10²／10³／10⁴ 三档，各生成一份派生结果快照，覆盖：

| 派生 | 说明 |
| --- | --- |
| 持仓重放 | 全部资产的最终持仓、成本、位置分布 |
| 现金重放 | 最终余额与全部现金事件的累积结果 |
| 盈亏摘要 | 首页四项摘要的全部字段 |
| 资产分配 | 分配饼图的全部数据点 |
| 历史曲线 | 1d／7d／30d／365d／all 五个区间各一份完整序列 |
| 交易热力图 | 365 日全部格子 |

| 项目 | 要求 |
| --- | --- |
| 种子 | 使用 `benchmarks` 默认种子 `w15-main-perf-baseline-v1`，不得更换 |
| 数据 | 全部虚构，不得含任何真实账户数字 |
| 命名 | 文件名写明种子与档位 |
| 形式 | 10⁴ 档允许只存内容哈希加抽样字段以控制体积；具体形式由执行者决定并在 `04C-1` 中说明取舍与覆盖率 |
| 冻结 | 一经提交不得修改。若阶段二发现快照本身有误，停止并在 `04C` 中报告，等待裁决 |

## 1.4 阶段一测试合同

| 编号 | 断言 |
| --- | --- |
| T1-01 | 放宽上限前后，10²／10³／10⁴ 三档的 M-2 中位数差异在 ±10% 以内，证明放宽本身不影响性能 |
| T1-02 | 放宽上限后，10⁵ 档的合成 B 文件能通过产品自身的导入预检并完成导入 |
| T1-03 | 快照比对工具在账本未改变时，对同一档位连续两次比对均判定相同 |
| T1-04 | 快照比对工具在故意改动一笔事实后能准确判定不同（负向验证，改动不入库） |
| T1-05 | 默认全量测试用例数不低于 0.3 记录的基线，全部通过 |
| T1-06 | 资源上限的既有测试仍然通过；若既有测试写死了 8 MiB 等旧数值而失败，停止并在 `04C-1` 申报，不得自行改写断言 |

T1-06 大概率会触发（`resourcePolicy.test.ts` 中有"恰好 8 MiB 通过、多一字节拒绝"的用例）。**这属于合同预见到的情况：允许同步更新该测试中的数值使其对应新上限，但必须在 `04C-1` 中逐条列出改了哪些断言、从什么改成什么。禁止删除测试或放宽其结构。**

## 1.5 阶段一交付：`04C-1`

写入 `01一些进度/日志/week15_260826/04C-1_W15-main-性能优化第一批阶段一证据报告.md`，中文，提交到根文档仓库 `main`。必须包含：

1. 运行环境与开工前基线数字，以及与 `03C` 环境的异同
2. E-1 全部数据表（放宽前，三档）
3. E-2 全部数据表（放宽后，三档）及 T1-01 判定
4. E-3 全部数据表（10⁵ 档，Node 与浏览器 dev／production 分开）
5. E-4 10⁶ 探测结果与失败点
6. 快照的形式、覆盖范围、文件清单与取舍说明
7. T1-01～T1-06 逐条结果
8. 阶段一实际改动的文件清单与提交哈希
9. `git diff main...HEAD -- src/` 的完整内容（应只有两个文件的数值改动）

**交付 `04C-1` 后停止。等待产品负责人明确放行，方可进入阶段二。**

---

# 阶段二：不重复算

## 2.1 四处修改，各自独立提交

按此顺序执行，每处完成后立即跑等价性测试与定向指标：

### P-1 历史曲线（收益最大，先做）

现状：按区间内每一天，重放一次截至该日的账本前缀，另加每天约 0.11 倍全量重放的固定开销。10⁴ 档单独耗时 1,384 ms。

要求：改为**单次顺序扫描**——按时间顺序遍历事实一遍，边遍历边记录每日收盘状态，区间筛选在扫描结果上完成。目标复杂度与账本规模成线性，与区间天数无关。

通过线：10⁴ 档全部五个区间的 M-7 中位数，与同档 M-8 中位数处于同一量级（不超过 3 倍）。

### P-2 写入后的增量更新

现状：记一笔新事实后全量重放，10⁴ 档写入 7,652 ms。

要求：新增一笔事实后，只更新受该事实影响的部分。若因确定性排序要求无法完全增量，允许"追加式重放"（从受影响的最早时间点起重放），但必须在 `04C` 中写明退化到全量重放的具体条件。

通过线：10⁴ 档 M-3 中位数 ≤ 300 ms。

### P-3 输入草稿下沉

现状：价格输入框每敲一个字符触发整本账本重放约 30 遍，10⁴ 档单字符输入 1,535 ms。

要求：输入过程中的草稿状态与账本派生完全解耦。**敲字期间不得触发任何账本重算**，派生只在用户提交时发生。

通过线：10⁴ 档 M-6 输入与删除中位数均 ≤ 100 ms。

### P-4 派生结果缓存

现状：切换页面重新计算全部派生，10⁴ 档页面切换 1,686～2,409 ms。

要求：缓存派生结果，只在事实真正变化时失效。

| 约束 | 说明 |
| --- | --- |
| 失效条件 | 必须由事实变化驱动。**不得依赖定时器、轮询或手动刷新** |
| 存放位置 | 只在内存。不得写入 `.lftl`、B 文件、IndexedDB 或任何持久层 |
| 会话边界 | 账本锁定、切换文件、重新解锁时缓存必须整体丢弃 |
| 必须有测试 | 至少覆盖：新增一笔后缓存失效、删除一笔后缓存失效、撤回删除后缓存失效、切换账本后缓存不串 |

通过线：10⁴ 档 M-5 四个方向中位数均 ≤ 200 ms。

## 2.2 每一处修改后必须立即执行

1. 等价性测试：三档派生结果与阶段一快照逐字段比对，必须完全相同
2. 该处对应的定向指标，10⁴ 档，production 模式
3. 默认全量测试

**任何一处修改导致等价性测试不通过，立即停止该处修改并在 `04C` 中记录，不得继续往下做。**

## 2.3 阶段二测试合同

| 编号 | 断言 |
| --- | --- |
| T2-01 | 三档等价性测试全绿：派生结果与阶段一快照逐字段相同 |
| T2-02 | 缓存失效测试全绿：新增、删除、撤回删除、切换账本四种情形 |
| T2-03 | 敲字期间不触发账本重算，有可观测证据（调用计数或等价手段） |
| T2-04 | 默认全量测试用例数不低于阶段一，全部通过 |
| T2-05 | 派生值未进入任何持久层：`.lftl`、B 文件与 IndexedDB 的内容结构与阶段一相同 |
| T2-06 | 文件格式与四个版本号未发生任何变化 |

---

# 阶段三：复测与对照

## 3.1 必须跑完的组合

与 `03B` 4.1 相同的组合，加上 10⁵ 档：

| 档位 | Node 层 | 浏览器 dev | 浏览器 production |
| --- | --- | --- | --- |
| 10² | 10 次 | 10 次 | 10 次 |
| 10³ | 10 次 | 10 次 | 10 次 |
| 10⁴ | 10 次 | 10 次 | 10 次 |
| 10⁵ | 3 次 | 3 次或记超时 | 3 次或记超时 |
| 10⁶ | 1 次探测 | 1 次探测或记无法完成 | 可省略 |

种子、采样规则、丢弃首次运行的规则与 `03B` 完全一致。**不得更换种子或调整采样方式**，否则前后不可比。

## 3.2 对照表要求

每个指标一张表，行是档位，列依次为：阶段一中位数、阶段三中位数、倍数、是否达标。

- 开发模式与 production 分成两组，不得合并
- 明确标出超时、无法完成或未取得的格子，不得留空
- 倍数按"阶段一 ÷ 阶段三"计算，大于 1 表示变快

## 3.3 必须回答的五个问题

1. `04A` 2.2 的必达通过线，逐格是否达标？未达标的逐条说明差距与原因。
2. 等价性测试是否全绿？若有任何一格不同，具体是哪个派生的哪个字段？
3. P-1～P-4 四处修改各自带来多大改善？请用只做该处修改时的定向数据回答。
4. 10⁵ 档在优化后处于什么状态？哪一项仍是主要瓶颈？该瓶颈属于 05 批还是 06 批？
5. 10⁶ 档的失败点是否与 `03C` 一致？若不同，具体变成了哪一步？

---

# 质量门

- 阶段一测试合同全部通过
- 阶段二测试合同全部通过
- `04A` 2.2 的必达通过线全部达标
- 等价性测试全绿
- 默认全量自动测试通过，用例数不低于开工基线
- `typecheck` 通过
- `lint` 通过
- production build 通过
- `git diff --check` 通过
- `git status` 中不存在任何生成出来的账本文件或量尺结果文件
- 四个版本号未发生任何变化

任一项未通过即为失败，不得以"大部分通过"结案。

---

# `04C` 交付要求

执行报告 `04C_W15-main-性能优化第一批不重复算执行报告.md` 写入 `01一些进度/日志/week15_260826/`，中文，提交到根文档仓库 `main`。必须包含：

1. 分支名、每个阶段的实际改动文件清单与提交哈希
2. 阶段一已在 `04C-1` 交付的内容可引用，不重复全文，但必须给出放行时间与放行人
3. 阶段二 P-1～P-4 逐处的改法说明：改了什么、为什么这样改、退化条件是什么
4. 阶段二与阶段三测试合同逐条的通过或失败结果；失败项写明原因，不得省略
5. 3.1 全部组合的实测数据与 3.2 的对照表
6. 3.3 五个问题的明确回答
7. 全量测试、`typecheck`、`lint`、build、`git diff --check` 的实际数字，并与开工基线对比
8. 与 `04A` 产品决定不一致之处（若有）及处理方式
9. 明确声明：是否改动过任何文件格式或版本号（应为否）；是否把派生值写入过持久层（应为否）；是否修改过既有测试的断言或阈值（阶段一的资源上限测试除外，且须逐条列出）；是否修改过阶段一冻结的快照（应为否）；是否读取过 `~/Downloads/history_OKX/`（应为否）；是否执行过 merge 或 push（应为否）

开发侧全绿只代表开发执行候选通过，不等于独立验收通过。独立验收 `04D` 由另一执行者另行进行。

---

# 修订 A（2026-08-29，阶段一执行中追加）

## A.1 背景

执行机为无风扇的 MacBook Air。持续满载十分钟以上必然进入热降频，**越晚跑的档位越慢**，且慢的原因是机器发热而不是账本变大。若不加控制，阶段一与阶段三的对照会被热漂移污染，得出的"快了几倍"不可信。

本修订不要求中断或重跑任何已完成的测量，只增加记录与复现要求。

## A.2 追加要求

| 编号 | 要求 |
| --- | --- |
| A-1 | 每一条量尺命令，记录其**开始时刻（本地时间，精确到分）与在本轮中的执行序号**。已完成的档位按可得信息补记，无法补记的如实写"未记录" |
| A-2 | `04C-1` 必须给出**完整的执行顺序清单**：先跑了哪个档位哪个模式，再跑哪个，中间是否有间隔 |
| A-3 | 阶段三复测必须**按与阶段一完全相同的顺序**执行，使前后受到的热影响尽量一致。顺序改变必须在 `04C` 中申报并说明理由 |
| A-4 | 每个档位的浏览器层测量结束后，**静置至少 5 分钟**再开始下一个档位，降低累积热量。该静置时间不计入任何指标 |
| A-5 | 若同一档位同一指标的最大值超过中位数 2 倍，`04C-1` 必须单独标出该格并注明可能受热降频影响，不得直接取中位数了事 |

## A.3 不做的事

- 不因发热而缩短采样次数、下调档位或简化账本结构（B-10 仍然有效）
- 不安装或调用任何温控、跑分或系统调优工具
- 不因单次数字难看而重跑；重跑必须整档重跑并在 `04C-1` 中写明两次结果
