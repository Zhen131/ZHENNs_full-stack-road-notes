# 02D_W15-main｜首页持仓表用词统一与成本列校正独立验收报告

- 日期：2026-08-28
- 轨道：长期账本产品 `main`
- 受验对象：`zhennn/w15-main-holdings-wording@bbaafa7`，4 个提交，12 文件，+554 / -92
- 依据：`02A` 产品定义、`02B` 执行合同（含 B-09 修订）

## 结论

**有条件通过。** 代码层逐项独立复核，`02A` 的产品决定与 `02B` 的全部禁止事项均成立，未发现与合同冲突之处，也未发现受验方自报与代码事实不符之处。

本结论**不覆盖测试执行结果与真实 Chrome 证据**。受验方自报的 93 文件 / 1092 项测试、lint、production build 与四项 Chrome 只读确认**均未被独立复跑**，原因见第一节，因此这些项在本报告中既不判通过也不判失败。

发现三项观察，均非缺陷，不阻塞合并，登记供后续批次处理。

## 一、证据边界（必须先读）

受验仓库的工作目录当前已由另一执行者切出并占用，正在执行 Week 15 第三批（`zhennn/w15-main-perf-baseline`）。一个工作目录同一时刻只能停在一条分支上，切换到受验分支会在对方运行期间替换其正在读取的文件，破坏其测试结果。

因此本次独立验收全程采用**不切换分支的只读方式**，仅通过 Git 对象直接读取受验分支内容。证据范围如下：

| 项目 | 是否独立取得 |
| --- | --- |
| 受验分支全量 diff 逐行复核 | 是 |
| 受验分支文件内容逐处读取 | 是 |
| 受保护实现文件是否被改动 | 是 |
| 旧叫法独立扫描（范围比合同更宽） | 是 |
| Git 提交边界与分支状态 | 是 |
| 全量自动化测试 | **否**，工作树被占用，未复跑 |
| `typecheck` / `lint` / production build | **否**，同上 |
| 真实 Chrome 四项只读确认 | **否**，未独立复现 |

受验方自报结论保留在 `02C`，不因本报告的未复跑项而被改写，也不因其自报通过而被本报告采信为独立证据。

## 二、产品决定复核

逐项核对 `02A` 的产品决定与实现代码：

| 编号 | 决定 | 结论 | 证据 |
| --- | --- | --- | --- |
| V-01 | 首页持仓表八列，顺序为 币种／当前价格／持仓均价／相对均价涨跌／未实现盈亏／持仓量／剩余持仓成本／当前市值 | 通过 | `HoldingsOverview.tsx` 表头数组逐字符与 `02A` 3.2 一致，列数仍为八 |
| V-02 | 第七列数据来源由 `buyOutflowByAsset` 改为 `Position.costBasis` | 通过 | 第七格改读 `position.costBasis`；`buyOutflowByAsset` 属性、`ZERO_BUY_OUTFLOW` 常量与 `SummaryMetric` 引用一并从该组件移除，无残留 |
| V-03 | 卡片标题、说明文字与无障碍名称按 `02A` 更新 | 通过 | 标题为「当前持仓」，说明文字与 `02A` 3.2 逐字符一致，`aria-label` 为「前五持仓」 |
| V-04 | 统一词表在全部界面生效 | 通过 | 见第三节独立扫描 |
| V-05 | 累计买入流出移入完整持仓详情，不删除 | 通过 | `HoldingsDetails.tsx` 新增第十二列，数据来自 `pnlSummary.buyOutflowByAsset`；`HomeWorkspace.tsx` 将该属性由概览组件改传详情组件 |
| V-06 | 该列须有视觉分隔与说明文字 | 通过 | 表头与单元格对最后一列施加左边框（窄屏为上边框）；面板头部新增说明「累计买入流出是历史上买入一共支出的现金，不与本表其他任何列相减。」 |
| V-07 | 不新增已实现盈亏列到首页持仓表 | 通过 | 首页表仍为八列，无该列 |
| V-08 | 不实现累计净盈亏、不展示白拿计入价值 | 通过 | 无相关新增 |
| V-09 | 不调整首页布局与最小宽度 | 通过 | `min-w-[1120px]` 与四行布局无 diff |

## 三、旧叫法独立扫描

独立执行，**扫描范围扩大到整个 `src/`**，比 `02B` T-12 要求的 `src/app` 与 `src/features` 更宽：

| 旧叫法 | 全 `src/` 命中文件数 | 命中位置 |
| --- | ---: | --- |
| 总花费 | 1 | 守卫文件自身 |
| 盈亏金额 | 1 | 守卫文件自身 |
| 平均购价 | 1 | 守卫文件自身 |
| 涨跌幅 | 1 | 守卫文件自身 |
| 含费平均成本 | 1 | 守卫文件自身 |
| 剩余含费成本 | 1 | 守卫文件自身 |
| 已实现净盈亏 | 1 | 守卫文件自身 |
| 未实现净盈亏 | 1 | 守卫文件自身 |
| 累计买入总支出 | 1 | 守卫文件自身 |

九个旧叫法在整个 `src/` 中的唯一残留是扫描守卫 `src/test-support/interfaceWording.test.ts` 自身的禁用词清单，属于必然存在，不构成遗漏。

新叫法「相对均价涨跌」不包含被禁字符串「涨跌幅」，守卫不会误伤。

## 四、禁止事项复核

| 编号 | 约束 | 结论 | 证据 |
| --- | --- | --- | --- |
| B-03 | 不得修改 `positionReplay.ts` | 通过 | `git diff main...bbaafa7 -- src/core/` 为空 |
| B-04 | 不得修改 `pnlSummaryService.ts` 计算逻辑 | 通过 | 该文件相对 `main` 无 diff |
| B-05／B-06 | 不改精度、不改安全与文件逻辑 | 通过 | `src/core/`、`src/platform/`、`src/ui/` 三个目录相对 `main` 全部无 diff |
| B-07 | 不改首页布局、列数与最小宽度 | 通过 | 同 V-09 |
| B-09 | 输入夹具冻结，预期输出可更新且须登记 | 通过 | 受验方登记一条（`HoldingsOverview.test.tsx` 第七格 `6505 → 3903`）。独立核对全部测试 diff，未发现任何输入夹具数值被改动 |
| — | 不得触碰论文轨道 | 通过 | 无 `CS2026` 相关改动 |
| — | 未合并、未推送 | 通过 | 分支停在 `bbaafa7`，与 `main` 无合并关系 |

## 五、测试合同抽查

未复跑，但逐条核对测试代码是否真实存在且断言正确：

| 编号 | 抽查结论 |
| --- | --- |
| T-01～T-04 | 存在。BTC 虚构样例断言持仓均价 `33,333.33`、未实现盈亏 `1,166.67`、剩余持仓成本 `3,333.33`，四个自校验等式成立 |
| T-05 | 存在，且额外断言旧值 `5000` **不出现在该行**——验证的是移除而不只是新增，强于合同要求 |
| T-06 | 存在。ETH 外部转出样例断言未实现盈亏 `400.00`、当前市值 `2,400.00` |
| T-07 | 存在。SOL 白拿样例断言未实现盈亏 `-50.00`、剩余持仓成本 `320.00`，并断言 `120.00` 不出现在该行 |
| T-08 | 存在。手续费不可靠时断言第 2、3、4、6 四格**全部**显示「不可可靠计算」，覆盖 `02B` 2.3 新增的成本列要求 |
| T-13 | 存在且写法可靠。分别在指标卡、概览表与详情面板按无障碍角色查找同一批词 |
| T-18 | 独立证实。`positionReplay.ts`、`pnlSummaryService.ts` 及其既有测试文件均不在本批改动清单内 |
| T-12、T-14～T-17、T-19、T-20 | 测试代码存在，断言内容与合同一致 |

## 六、观察项（非缺陷，不阻塞）

| 编号 | 观察 | 影响 | 建议 |
| --- | --- | --- | --- |
| O-1 | 扫描守卫只覆盖 `src/app` 与 `src/features`，`src/ui`、`src/core`、`src/platform` 不在范围内 | 当前那三处无界面文案，实际无残留（第三节已独立证实）。但守卫留有口子，将来界面文案若出现在 `src/ui` 不会被拦住 | 后续批次把守卫范围扩到整个 `src/`，并显式排除守卫文件自身 |
| O-2 | `HoldingsDetails.tsx` 的列名清单存在两份副本（内联表头数组与 `holdingLabels` 常量），靠位置对齐 | 本批把两份同步扩到 12 项，当前一致。但任一方单独增删会造成静默错位，且无测试能发现 | 后续合并为单一来源 |
| O-3 | `02A` A-04（顶部指标卡剩余持仓成本总数等于各资产之和）无对应测试 | 风险低，两处读同一份派生数据 | 可在后续批次补一条跨组件一致性断言 |

## 七、待补证据

以下项目必须在工作目录释放后由独立方补齐，才能把本报告由「有条件通过」升级为完整通过：

1. 在受验分支上独立复跑全量自动测试，核对是否为 93 文件 / 1092 项、零失败
2. 独立复跑 `typecheck`、`lint`、production build
3. 独立复现真实 Chrome 四项只读确认，全程只读，只使用虚构账本

在此之前，本批的开发候选 `PASS`（`02C`）与本报告的代码层「有条件通过」并存，两者都不构成完整的独立验收通过。

## 八、去向建议

代码层未发现阻塞项。按用户决定，本分支与 `zhennn/w15-main-perf-baseline` 均保持不合并、不推送，等第三批完成并各自验收后再统一决定合并顺序。

两批改动文件集合无交集（本批全部在 `src/`，第三批 `src/` 只读），合并时不会产生冲突，先后顺序不影响结果。
