# 2026-08-02 日志：把长期账本和毕业论文重新拆开

日期：2026-08-02
状态：已完成并推送
范围：论文差距盘点、`main`／`CS2026` 双轨建立、论文版英文迁移、规则与 Skill 固化

## 昨天最重要的结果

我昨天真正解决的并不是某一个新功能，而是把之前缠在一起的长期账本和毕业论文重新拆开了。

原来我一直把它们当成同一个项目去推进，所以我一方面想让账本满足自己长期真实使用，另一方面又想让它尽快回到论文方案和导师能够检查的范围。两个目标本身都没有错，但是放在同一个分支里以后，我每做一个决定都要同时考虑长期产品、论文、比赛和课程，结果就是东西越做越多，下一步反而越来越不清楚。

现在我把这件事情定下来了：`main` 继续做我长期会用的真实账本，`CS2026` 专门完成毕业论文。两条线共享同一个历史起点，但是以后各走各的，不互相合并，也不让 AI 自动把一边的修改搬到另一边。这样以后我坐到电脑前，不需要再同时处理四五件事情，只需要先判断今天是在做账本，还是在做论文。

## 昨天完成了什么

### 1. 先把论文方案和当前源码做了对照

我先把原毕业方案和当前源码逐项对照，整理出了三个表：

- 原方案核心功能哪些已经有、哪些只有一部分、哪些还没有。
- 原技术栈和当前真实技术栈发生了哪些变化。
- 原方案之外，我已经额外做出了哪些能力。

这份对照保存在 `000-截至2026-08-02的毕业论文原方案与当前源码对照.md`。它的意义不是证明我做了很多，而是让我以后能够很直观地看见：论文最低要求还差什么，哪些长期产品功能不应该继续塞进论文主线。

### 2. 固定共同基线，建立两个长期 worktree

我把源码提交 `084ae7d` 固定成 annotated tag：

```text
CS2026-baseline-2026-08-02
```

然后建立了两个长期目录：

| 方向 | 本地目录 | 分支 | 用途 |
| --- | --- | --- | --- |
| 长期账本 | `LocalFirstTradingLedger/` | `main` | 我自己长期真实使用的产品 |
| 毕业论文 | `LocalFirstTradingLedger-CS2026/` | `CS2026` | 论文实现、实验和导师检查 |

它们是同一个源码仓库的两个 worktree，不是两个互相复制的仓库。两个分支都已经推送到 GitHub，默认分支继续保持 `main`。

### 3. 把 main 的公开说明改成英文

`main` 仍然允许保留中文产品界面，因为这是我自己长期使用时的产品选择。但是 README、模块 README、`AGENTS.md` 和今后的提交标题统一使用英文，让源码仓库面对导师或公开读者时不会再显得中英文规则混乱。

这次没有为了形式去翻译 `main` 的界面、测试和源码注释，也没有改变任何业务逻辑。

### 4. 把 CS2026 当前工作树迁移成全英文

论文 worktree 的要求更严格：当前受 Git 管理的文档、界面、按钮、表格、图表、无障碍名称、错误信息、安全警告、源码注释、测试和永久夹具全部改成英文。

三个危险操作的确认文本也固定成了：

```text
CLEAR CURRENT C LEDGER
CLEAR LOCAL LEDGER
DELETE LEGACY BROWSER LEDGER
```

这次只做语言和内部命名迁移，没有改变 `LedgerData` schema、备份格式、`.lftl` 格式、错误码和业务计算规则。旧账本中的中文 `rawText` 仍然必须能够读取和重新导出，所以兼容测试使用 Unicode escape 保留了这条能力，而不是简单地把中文数据支持删掉。

### 5. 完成自动化验证，但没有冒充完整人工验收

`CS2026` 最终验证结果是：

- 受 Git 管理的当前工作树中文字符扫描为零。
- 55 个测试文件、698 项测试全部通过。
- typecheck、lint、production build 和 whitespace 检查全部通过。
- 浏览器检查了 Select/Create C 入口、新建 C 表单、密码不一致提示和无障碍名称，显示均为英文，控制台没有错误。

但是原生文件选择器之后的内部 Dashboard 没有取得完整人工巡检证据。自动化测试通过不能代替这条人工证据，所以我把它保留为待补项，没有写成“全部人工验收完成”。同样，原来的 `02D` 和 `03B` 仍然是 `BLOCKED`，不会因为建了论文分支就自动变成通过。

### 6. 把以后怎么维护写成了正式规则

我更新了根 `AGENTS.md`、当前开发状态、AI 使用指南、账本上下文说明和 `ledger-workflow` Skill，并新建了 `001_W12-main与CS2026双分支维护规则.md`。

以后默认路由是：

- 我说“继续论文”、提到“论文”或 `CS2026`，只进入论文 worktree。
- 我说“继续账本”、只说“账本”或没有提论文，只进入 `main`。
- 写入前必须同时检查目录和实际分支。
- 不创建 `main` 与 `CS2026` 之间的合并 PR。
- AI 不得自动 merge、rebase、cherry-pick、复制修复或同时修改两个分支。
- 如果一边发现的问题可能对另一边有用，只记录“可能值得参考”，等我明确批准后再处理。

Skill 的 `agents/openai.yaml` 已重新生成，官方 `quick_validate.py` 验证通过；“继续账本”“继续论文”“论文 Git”三种只读路由也都检查过。

## 昨天的 Git 结果

| 仓库／分支 | 最终提交 | 结果 |
| --- | --- | --- |
| 根文档仓库 `main` | `140c160` | 双分支规则、当前状态和 Skill 已提交并推送 |
| 源码 `main` | `279af4e` | 长期产品分支规则和英文公开文档已提交并推送 |
| 源码 `CS2026` | `a7d8b21` | 英文工作区、业务流程、界面、测试和夹具已分批提交并推送 |

昨天结束时，根文档仓库、源码 `main` 和源码 `CS2026` 都是干净状态，并分别与远端保持 `0/0`。源码远端长期分支只保留 `main` 和 `CS2026`，没有为了这次工作制造临时分支，也没有产生两个长期分支之间的合并提交。

## 我昨天明确没有做什么

- 没有开始合成交易生成器、Playwright、性能指标和论文统计。
- 没有创建 GitHub Project、Package 或 Release。
- 没有为了 GitHub contribution 绿格把 `CS2026` 合并回 `main`。
- 没有重写以前的中文 Git 历史。
- 没有把长期账本需求从 `main` 中删除，也没有让论文的短期范围反过来控制我的长期产品。

## 下一步入口

我觉得昨天做完以后，最重要的变化是我终于重新找到了线头。

接下来如果做论文，我就只看 `CS2026` 和论文差距表，先确定论文最低实现、benchmark 和向导师汇报的两套方案；如果做长期账本，我就回到 `main`，继续考虑真实使用、桌面端和严格事务能力。比赛、NLP 和其他想法可以继续存在，但它们不能再把当前主线重新搅成一团。

总之，昨天不是把论文做完了，也不是把账本做完了，而是把以后应该怎么做这两件事情彻底分清楚了。


# 2026-08-10 日志：含费 P&L 与 V2 手续费规则进入 main

日期：2026-08-10
状态：今日开发结束；源码与文档统一收口并推送
范围：Week 12 第 01 批独立闭环、第 02 批开发、验收节奏调整与双仓库 Git 收口

## 结论

今天完成了 01A / 01C 对应的含费 P&L 开发与独立闭环，也完成了 02A 的 V2 与手续费规则开发。第 02 批没有执行 02C，也没有生成 02D；用户决定把逐批独立审查改为若干功能完成后的集中复验，减少重复测试占用，把更多时间放回实际开发。

源码第 02 批九个阶段提交已 fast-forward 合入 `main`，README 以 `7481e78` 收口。根文档同步 00B、00C、00D、当前状态、02B / 02C 状态与本日志。源码远端长期只保留 `main` 和 `CS2026`。

## 今日完成

### 1. 第 01 批含费 P&L 走完开发与独立复验

- 01A 完成 USDT、成交金额、实际手续费、含费成本与净盈亏。
- 01C / 原 01D 发现旧 USD 账 absent Binance mapping 会被静默写回的 P0，没有用开发侧绿灯掩盖。
- R1 修复后由 01R1D 独立 PASS；58 个测试文件／730 项测试、typecheck、lint、build 和真实 Chrome 文件闭环通过。
- `PNL-001～003` 已进入 `main`，原 FAIL 与最新 PASS 分开保留。

### 2. 第 02 批完成 V2 与手续费规则开发

- `LedgerData.schemaVersion`、`.lftl`、明文备份和 current / previous 全部进入 V2。
- V1 `.lftl`、V1 备份和旧完整 IndexedDB 账本只识别并拒绝，不迁移、不回写、不自动删除。
- IndexedDB 正常路径只保留最小 connection record，正式整账只在用户选择的 `.lftl`。
- 新增 fixed / percentage FeeRule、平台 + 资产精确匹配、冲突拒绝、版本替换、停用和历史 fee 不重算。
- 页面新增规则管理、交易平台、候选手续费、实际值覆盖、现金影响和认证保存状态。
- 最终自动化为 58 files / 727 tests；typecheck、lint、production build、diff-check 和残留扫描通过。

### 3. 真实 Chrome 文件矩阵完成

02A 开发侧真实流程覆盖了：

- 系统 picker 新建和打开 V2 `.lftl`；
- 规则新增、版本、停用、匹配、冲突和用户覆盖；
- 保存、锁定、重开、clear、V2 备份预检和整本恢复；
- V1 文件 / 备份零写拒绝、旧 IndexedDB 整账退役；
- 权限撤销 / 重选、同一实际文件双标签争用、物理副本不误锁；
- 外部 revision 拒写、current 损坏 / previous 恢复、取消恢复零写；
- 文件删除后旧句柄 fail closed、显式选择重建文件、raw IndexedDB 与 390 / 1280 视口。

02B 因此判定开发侧 `PASS`。这是真实开发证据，但不是独立 02D。

## 关键决策：独立审查改为集中执行

之前每完成一个 A，就立即再跑一次 C / D。实际结果是大量时间重复运行全量测试、build、浏览器和文件矩阵，开发时间被明显挤压。

从今天起采用：

```text
NNA 开发合同
→ 实现 + 永久正式测试 + 适用真实流程
→ NNB 开发报告
→ 用户授权后可合入 main
→ 若干相关功能完成或重要发布前，再集中执行 NNC / NND
```

这项调整减少的是重复执行次数，不是删除安全门。每批 A 仍必须补永久测试、跑完整质量门并处理已知 P0 / 安全类 P1；C / D 未执行时必须写“待集中复验”，不能写成独立 PASS。

现有 02C 保留为历史输入，但不再立即执行。未来集中复验必须以当时最新 `main` 重新冻结范围，不能直接复用 `083b9f7` 冒充当前候选。

## 账本现在是否可以用

可以。当前 `main` 已经是一套可操作的 V2 网页本地账本：能新建和重开加密 `.lftl`，记录交易与价格，计算含费成本和净盈亏，管理手续费规则，备份、恢复并处理权限与冲突。

当前仍有三个使用边界：

1. 旧 V1 文件和备份不会自动迁移，必须新建 V2。
2. 02C 已延期，所以只能称“开发侧可用”，不能称“集中独立复验通过”。
3. 重要数据应保留独立备份，不把单一 `.lftl` 当成不可恢复的唯一副本。

## Git 收口

| 仓库 | 分支 | 收口结果 |
| --- | --- | --- |
| 产品源码仓库 | `main` | 第 02 批九个阶段提交 fast-forward 合入；README 收口为 `7481e78`；推送后与 `origin/main` 同步 |
| 根文档仓库 | `main` | 02A / 02B / 延期 02C、00 层状态、当前状态和本日志统一提交并推送 |
| 源码长期分支 | `main`、`CS2026` | 已合并本地功能分支清理；远端不保留临时候选分支 |

没有把 `main` 合并进 `CS2026`，也没有把产品改动自动复制到论文 worktree。

## 明天入口

今天到此结束。下一次先对现有 V2、FeeRule 和交易确认页面做小范围 UI 整理，让功能更顺手、更好看；UI 收口后再进入 `NLP-001`，不在同一任务里夹带 NLP、模型安装或账本合同改动。
