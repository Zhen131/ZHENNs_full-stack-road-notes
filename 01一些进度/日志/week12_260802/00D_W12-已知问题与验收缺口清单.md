# Week 12 已知问题与验收缺口清单

日期：2026-08-10
状态：当前有效；不阻塞 UI / NLP 开发或所有者谨慎使用新版 V2 账本，阻塞“已经独立认证”的发布表述
来源：独立审查报告、02B 开发与真实 Chrome 证据、当前源码核对和已确认平台边界

## 结论

当前没有未关闭的已证实产品缺陷。Week 12 02A 已在开发侧取得真实 Chrome、系统 picker、权限、双标签、宿主文件、恢复 / 清空、外部 revision 和 raw IndexedDB 的完整虚构 V2 证据，并以 58 个测试文件／727 项测试和全部质量门收口。产品因此已经具备可实际操作的新建 V2 本地账本核心能力。

尚未完成的是“由另一全新任务重新复现”的独立证据。用户于 2026-08-10 决定不立即执行 02C，而是在若干功能完成后集中复验。这个延期不阻塞 UI、NLP 或所有者在保留备份前提下谨慎使用 V2；但在集中 NND PASS 前，不得对外宣称已经独立认证，也不应把单一 `.lftl` 当成不可恢复的唯一副本。

Week 11 `03B BLOCKED` 保留为旧绝对恢复合同和浏览器能力上限的历史证据；`03C` 已在执行前作废，不再执行，也不生成或伪造 `03D`。

Week 12 第 01 批的原 `01D` 曾因旧 USD 账 absent Binance mapping 被静默物化而判 `FAIL`；R1 已由 [01R1D](01R1D_W12-main-旧账Binance映射静默写回独立复验报告.md) 判定 `PASS`，原 P0 现已关闭。第 02 批的 [02B](02B_W12-main-版本2与手续费规则执行报告.md) 则完整记录了 V2 与 FeeRule 开发证据；它把原本缺失的真实流程在开发侧补齐，但不冒充独立 02D。

## 当前统计

| 类别 | 数量 | 状态 |
| --- | ---: | --- |
| 未关闭已证实缺陷 | 0 | 无 |
| 待集中独立复验范围 | 1 组 | 不阻塞开发与谨慎使用；阻塞独立认证表述 |
| 已接受平台限制 | 1 组 | 未来桌面讨论输入，不计待修缺陷 |
| 已关闭 Finding | 2 | Week 11 README 陈述 1 项；Week 12 原 01D P0 1 项，均保留历史链接 |

## W12-EVID-001：V2 真实浏览器与存储闭环待集中独立复验

性质：独立验收证据缺口，不是已经证实的代码错误。
来源：[Week 11 02D 独立审查报告](../week11_260727/02D_W11-C文件安全闭环与正式接管合并独立测试审查报告.md)、[Week 12 02B 开发报告](02B_W12-main-版本2与手续费规则执行报告.md)
当前状态：Week 11 历史判定保持 BLOCKED；Week 12 开发侧证据完成，独立复现延期。

### 开发侧现有证据与剩余缺口

| 目标 | Week 12 02A / 02B 已取得 | 后续集中复验 |
| --- | --- | --- |
| `FILE-006` | current 单字节损坏、previous 有效、取消零写、确认恢复、复读与 revision 链 PASS | 由全新任务复现 |
| `FILE-007` | 同一实际 C 双标签互斥、副本不误锁、外部 revision 拒写 PASS | 由全新任务复现 |
| `FILE-008` | permission revoke / prompt / denied、移动 / 删除、正确与错误重选、connection record PASS | 由全新任务复现 |
| `FILE-009` | 错密零写、正密重开、锁定、session / lease 与 raw storage 边界 PASS | 由全新任务复现 |
| `FILE-003` | raw IndexedDB 只有三字段 connection record；旧整账只检测并拒绝，零迁移零删除 PASS | 由全新任务复现 |
| `FILE-010` | clear 双代、恢复、失败真实性和旧异步 fail closed PASS | 由全新任务复现 |

共同剩余项只有独立性：未来集中审查需要基于当时最新 `main` 重新运行，而不是直接继承 02B 的 PASS。

### 对路线的影响

| 路线 | 当前影响 |
| --- | --- |
| `01` 含费 P&L | 已由 01R1D 独立 PASS；原 01D FAIL 保留为 R1 修复来源 |
| `02` 版本 2 与手续费规则 | 开发 PASS 并合入；独立状态为待集中复验 |
| `03～05` AI 候选、审核、追问预览 | 不阻塞；这些批次无写账能力 |
| `06` AI 整批写入 | 开发与验收仍先使用专用虚构版本 2 Demo C；真实写入前再评估集中复验范围 |
| `07` 公平评估与 Notebook | 不阻塞；只使用固定虚构题目和账本外实验资料 |
| DEIK Demo | 不阻塞；不得冒充真实使用验收 |
| 所有者谨慎使用 V2 | 可用；保留独立备份，不使用 V1，不把单一文件当唯一不可恢复副本 |
| 独立认证 / 对外发布 Gate | 阻塞；必须先由集中独立审查取得适用证据 |

### 关闭条件

只有全新集中独立审查任务在当时最新的 `main` 上取得并记录以下证据，才能关闭 W12-EVID-001：

1. 真实 Chrome 系统 picker 与宿主可见版本 2 `.lftl`；
2. 权限、重连、恢复、双标签、清空和 revision 冲突的真实流程；
3. raw IndexedDB 证明只有最小 connection record，不含完整账本、密码或密钥；
4. 对应正式测试、全量测试和质量门继续通过；
5. 集中 NND 明确判定 PASS。

新的集中 `NND = PASS` 只关闭 `W12-EVID-001`，表示它所审查的当时 `main` 已取得独立复验。Week 11 `02D = BLOCKED` 永远保留为当时候选的历史判定，不追溯改写。02B、代码合入、自动化全绿和开发侧真实 Chrome 证明“开发可用”，但不能单独写成“独立认证通过”。

## 已接受平台限制

纯浏览器单文件模型不能保证：

- 页面或 Chrome 进程死亡后继续执行补偿；
- 文件权限永久丢失后仍能写回并复读旧 C；
- 外部原生程序抢先改写后，网页仍能回滚或提供 OS 级锁；
- 任一 post-close 异常最终都能证明磁盘恢复为旧版本。

仍然有效的合同：

```text
正常成功必须 write → close → readback → 解密 → 完整校验
→ 只有能证明磁盘仍是本事务 candidate 时才允许补偿
→ 无法证明安全结果时 fail closed，停止后续写入并如实提示
```

来源：[Week 11 03B 执行记录](../week11_260727/03B_W11-B历史导入预检与整本原子替换合并执行记录.md)。这些是已接受能力上限，不计入待修缺陷；未来重新讨论桌面端时再决定是否用原生事务能力改变合同。

## 历史项

- `W12-01D-P0`：合法 v1 / USD 旧账 absent Binance mapping 曾被运行期默认值静默物化；原 [01D](01D_W12-main-手续费进入成本与净盈亏独立审查报告.md) 判 `FAIL`，经 R1 修复后由 [01R1D](01R1D_W12-main-旧账Binance映射静默写回独立复验报告.md) 独立关闭。原报告不覆盖、不改写。
- `W11-DEF-001`：README 候选状态陈述过期，已由源码提交 `ceac405` 修正并复核关闭。历史见 [Week 11 缺陷清单](../week11_260727/00D_W11-已知缺陷与延期处理清单.md)。
- `03B BLOCKED`：保留旧绝对合同为什么无法由纯浏览器兑现的历史，不改写为 PASS，也不再作为 IMPORT 当前待办。
- `03C`：未执行的旧独立审查输入，已作废；只保留[历史原件](../week11_260727/03C_W11-B历史导入预检与整本原子替换合并独立测试审查执行文档.md)，不得继续执行或虚构 03D。
- 01D-6 以前已关闭的 salt / metadata 漂移、旧 picker Promise、KDF 解耦、双代和 close-readback 问题不重新登记，但永久回归测试不得删除。

## 登记与关闭规则

1. 开发任务通过 NNB 登记开发 Finding 与待集中复验范围；独立审查任务只生成 NND，不直接修改本文件。
2. NNB / NND 后由协调步骤把未关闭 Finding 和证据缺口去重登记。
3. 已证实缺陷、证据缺口和已接受平台限制必须分开统计。
4. P0、数据安全类 P1 或当前批强制通过项失败，立即阻塞相关路线。
5. 开发侧修复或开发侧真实流程可以标“开发 PASS / 待集中独立复验”；只有独立 PASS 才能关闭独立证据项。
6. 虚构 Demo、课程实验、论文 benchmark 和真实使用不能互相替代证据。

## 固定检查点

| 检查点 | 必须执行 |
| --- | --- |
| 每次生成 NNA 或集中 NNC 前 | 读取本文件，确认本批是否依赖未关闭项 |
| `06` 写入 Demo C 前 | 明确只使用虚构版本 2 数据，不宣称真实使用通过 |
| 把账本作为唯一重要副本或对外宣称独立认证前 | 重新冻结 `main` 并关闭 W12-EVID-001 的适用范围 |
| 讨论桌面端时 | 重新评估已接受浏览器限制是否需要原生能力改变合同 |
