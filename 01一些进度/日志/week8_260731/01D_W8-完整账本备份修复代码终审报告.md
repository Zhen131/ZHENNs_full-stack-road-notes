# Week 8：完整账本备份修复代码终审报告

日期：2026-07-23

审查对象：源码分支 `zhennn/week8-backup-roundtrip`

审查范围：修复提交 `06ab1af..7f15951`；完整 Week 8 差异 `main@655a87d..7f15951`

最终结论：**No-Go；不得合并 `main`，不得进入 Week 9。**

## 1. 结论

`01B` 的原 P0 已关闭；同文件重选、结构化错误、稳定错误码、候选摘要和完整 fixture Hook 往返已有实现与测试。

当前无 P0，仍有 4 个 P1、1 个 P2。阻断项集中在：

```text
production 完整验收缺失
关键失败/并发测试仍不完整
read-only 救援导出文案未满足契约
README / Checklist / 当前状态互相冲突
```

`223/223`、lint、build、diff-check 通过，不等于 `01C` 全部 Gate 通过。

## 2. Git 与验证事实

| 项目 | 结果 |
| --- | --- |
| 当前分支 | `zhennn/week8-backup-roundtrip` |
| 修复提交 | `2e3aca9`、`e51191d`、`7f15951` |
| Git 状态 | 源码干净；已本地提交；未 merge；未 push |
| 完整差异 | 14 个文件；新增 1595 行，删除 9 行 |
| 全量测试 | 23 个文件、223 项通过 |
| lint | 通过；0 warning / error |
| production build | 通过；`/` 与 `/_not-found` 静态生成成功 |
| `git diff --check main...HEAD` | 通过 |

“源码尚未提交”与仓库事实不一致；三个修复节点已经进入当前本地分支。

## 3. 已通过

### 3.1 P0 持久化生命周期

- 当前失败 snapshot 在 import 开始时不再清除。
- 自动保存 effect 会拦截同 generation/version 的失败 snapshot。
- 失败 import 不再触发第 3 次自动保存。
- import 失败保留页面账本、版本、dirty、error 和 retry 能力。
- active retry 期间 import 返回 `LEDGER_IMPORT_NOT_ALLOWED`。
- import 写失败统一返回 `LEDGER_REPOSITORY_WRITE_FAILED`。
- 真实 `DefaultLedgerRepository + 故障 StorageAdapter` 测试已覆盖 record 不变和两次 write 上限。

### 3.2 文件选择与导入确认

- 文件捕获后、取消后、成功后均重置 input value。
- 同一文件取消后重选、成功后重选测试通过。
- A/B 异步读取只接受最新 generation。
- 合法候选保留完整 `BackupEnvelopeV1`。
- 确认前展示 appVersion、exportedAt 和四组集合数量。
- 明确显示“完整覆盖、不合并数据”；dirty 和 recovery 文案已区分。

### 3.3 错误契约

- 已使用 `BACKUP_BAD_JSON`、`BACKUP_INVALID_ENVELOPE`、`BACKUP_UNSUPPORTED_FORMAT_VERSION`、`BACKUP_SCHEMA_VERSION_MISMATCH`。
- Validator、ResourcePolicy、Repository、Hook 错误归属已分层。
- 公开错误 union 保留 ResourcePolicy 的 `limit / actual`。
- UI 显示前 5 项 code、path、message，并保留总错误数。
- 旧错误码未残留在生产源码。

### 3.4 完整 fixture 与应用组装链

- fixture 包含自定义资产、5 条 golden trades、note、rawText、feeRuleId、2 条价格和 feeRule。
- fake IndexedDB 测试经过 Hook import、Hook clear、再次 import 和新 Repository 重挂载。
- Dashboard 确认前零写入；确认后交易和自定义资产视图更新。

## 4. 未通过

### P1-01：production Gate 未执行完整

`01C` 第 9 节要求的以下证据未形成：

- UI 新增 BTC / ETH 交易和 BTC 价格后导出。
- 导出文件名、字节数、版本、时间、集合数量和无 `Position[]`。
- clear 后直接确认 `ledger:v1` 不存在。
- import 后直接确认 record 一次性恢复。
- 刷新后交易、价格、持仓一致。
- 完整 fixture UI 导入、刷新、再导出、parse 后逐字段一致。
- hydration error recovery 主链。
- 坏 JSON、format v2、schema mismatch、悬空引用、超 8 MiB 五类拒绝。
- 所有场景页面与 record 不变、console warning/error = 0。

Week 8 目录没有新的 production 验收记录。按 `01C` 固定规则，任一 production 场景缺失即 No-Go。

### P1-02：关键自动化 Gate 仍不完整

现有测试未覆盖：

- hydration error recovery import 写失败并保留损坏 record。
- ready + read-only 调用 Hook import 返回 NOT_ALLOWED 且零写入。
- import 执行中 unmount、stale completion 不更新应用状态。
- Repository 切换后的旧 import 失败 completion。
- 取消后旧 `File.text()` resolve/reject。
- 组件卸载后旧 `File.text()` resolve/reject。
- 完整 fixture 在重挂载后重新 export、parse，并执行 canonical re-export 比较。
- serialized backup 恰好 8 MiB 通过、超 1 byte 拒绝且不创建 Blob。
- Dashboard import 失败、recovery import 成功/失败。
- importing 期间 export、import、clear、交易、价格、删除全部禁用的同一场景断言。

位置：

```text
src/hooks/usePersistentLedger.test.tsx:1281
src/hooks/usePersistentLedger.test.tsx:1344
src/components/backup/BackupControls.test.tsx:145
src/components/dashboard/DashboardShell.interaction.test.tsx:558
```

完整 fixture 测试证明了 import/clear/remount，但没有证明 re-export；Dashboard 测试只覆盖成功路径。

### P1-03：read-only 救援导出文案仍不符合契约

位置：

```text
src/components/backup/BackupControls.tsx:108
src/components/backup/BackupControls.tsx:121
src/components/backup/BackupControls.tsx:247
```

`01C` 要求 read-only 明确说明：

```text
备份可能因集合或字符串超限而无法由当前版本重新导入
```

当前文案只说明“禁止 import 覆盖超限账本”，没有提示导出的救援文件可能无法重新导入。

超 8 MiB 文案也只写“超过限制”，未使用“当前 v1 无法安全导出该超大账本”的固定边界说明。

现有 read-only 测试仅匹配“仅可导出受 8 MiB 限制的救援备份”，没有锁定上述风险语义。

### P1-04：README 与状态文件未收口

源码 README 仍声明：

```text
Week 8 可在用户确认后开始
Week 8 导入导出尚未开始
JSON 导入导出尚未实现
Week 8 尚未开始
```

位置：

```text
README.md:10
README.md:187
README.md:193
README.md:203
```

`01C` 要求通过后更新为“Week 8 修复候选完成，等待独立复审”，并记录实际测试数和提交；当前未执行。

文档仓库同时存在：

```text
00-当前开发状态.md：Week 8 Gate 已通过；216 项测试
00-Week8-Checklist.md：Week 9 可以开始
01D：223 项测试；仍有 P1；No-Go
源码 README：Week 8 尚未开始
```

必须在 P1-01 至 P1-03 关闭后统一收口。

## 5. P2

### P2-01：schema mismatch 会产生重复错误

位置：

```text
src/backup/backupEnvelope.ts:128
src/backup/backupEnvelope.ts:142
```

`ledgerSchemaVersion = 2`、`ledgerData.schemaVersion = 1` 时，同一路径会追加两条 `BACKUP_SCHEMA_VERSION_MISMATCH`。不影响拒绝结果，但会让结构化错误列表重复。

## 6. 最终判定

| Gate | 判定 |
| --- | --- |
| 原 P0 rollback | 通过 |
| retry / import 互斥 | 通过 |
| 同文件重选 | 通过 |
| 稳定错误码与结构化错误 | 通过 |
| 候选摘要与覆盖确认 | 通过 |
| 完整 fixture Hook 往返 | 部分通过；缺 re-export |
| recovery / stale / unmount | 未通过 |
| read-only 救援警告 | 未通过 |
| production 成功与拒绝场景 | 未通过 |
| README / 状态一致性 | 未通过 |
| test / lint / build / diff-check | 通过 |

最终状态：**No-Go。**

下一步只关闭本报告 4 个 P1；P2 可后置。关闭后重新执行全量验证与独立 production 验收，再决定是否合并 `main`。
