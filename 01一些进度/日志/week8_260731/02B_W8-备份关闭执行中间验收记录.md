# 02B W8 备份关闭执行中间验收记录

日期：2026-07-23
源码分支：`zhennn/week8-backup-roundtrip`
源码 HEAD：`d9159c4ad7b0ed9b9374eacc882cb7f3a10faafb`
结论：P1-02 已关闭；P1-01 已完成真实文件主链，未关闭。

## P1-02：Go

提交：`d9159c4 测试：收口真实8MiB备份边界Gate`

- 合法 `BackupEnvelopeV1` 序列化后使用尾随 ASCII JSON whitespace 填充到实际 `8 * 1024 * 1024` bytes。
- 同一字符串创建真实 `File`；不覆盖 `file.size`。
- 8 MiB 文件：`File.size`、`TextEncoder`、`parseBackupJson(...)` 和 UI 候选确认入口均通过。
- `8 MiB + 1 byte`：实际 `File.size` 超限，读取前拒绝；parser 返回 `LEDGER_RESOURCE_FILE_TOO_LARGE`；不出现确认入口。
- 导出：8 MiB 创建 1 次 `Blob` 和 1 次 `URL.createObjectURL`；`8 MiB + 1 byte` 创建 0 次 `Blob` 和 0 次 `URL.createObjectURL`。
- 已删除伪造 `File.size` 与非法 `"x"` 伪备份边界测试。

验证：

```text
npm test      23 files / 239 tests passed
npm run lint  passed
npm run build passed
git diff --check main...HEAD passed
```

## P1-01：真实文件主链已通过

生产服务：`http://localhost:3018/`

页面实际输入：

- BTC buy：`0.1 @ 70000 USD`。
- ETH buy：`2 @ 3000 USD`。
- BTC price：`75000 USD`。
- 页面显示：BTC 市值 `7500 USD`、未实现盈亏 `500 USD`；ETH 持仓 `2`。
- 每次写入后页面显示“已保存到本地”；console warning/error 为 `0 / 0`。

首次实际下载文件：

```text
文件：local-first-trading-ledger-backup-v1-20260723-101204Z.json
字节：2130
SHA-256：136b81bb9b45392dee83275edaa49f70dc50c0c418a3aa69d0ff53e53c6124be
backupFormatVersion：1
appVersion：0.1.0
exportedAt：2026-07-23T10:12:04.005Z
ledgerSchemaVersion：1
assets / trades / priceSnapshots / feeRules：3 / 2 / 1 / 0
递归 JSON 检查：无 positions 字段
```

真实文件往返：

1. UI 固定文本确认 clear，页面交易和持仓为空。
2. 选择上述首次实际下载文件；候选摘要为 `3 / 2 / 1 / 0`。
3. 确认恢复后页面显示“备份已恢复并保存到本地”。
4. 页面恢复 BTC、ETH、BTC 价格与 BTC 市值/盈亏。
5. 再次实际下载：`local-first-trading-ledger-backup-v1-20260723-111129Z.json`，`2130 bytes`，SHA-256 `fde4ead9bd92c29eed660c3ed9ce00209702a0742f71835f1668797054b1130a`。
6. 两份文件的规范化 `ledgerData` 严格相等；两份均无 `positions`。

## P1-01 未关闭项

- clear 后 `ledger:v1` record 不存在的 direct record 证据。
- import 后 `ledger:v1` 单条完整 envelope 的 direct record 证据。
- 完整 fixture 的 production 导入、刷新、再导出逐字段对比。
- production hydration error recovery：坏 `encryptedPayload`、不 clear、合法备份 recovery、刷新复验。
- 坏 JSON、format 2、schema mismatch、悬空资产、8 MiB + 1 五类拒绝的页面与 record 前后对比。

禁止将本文件当作 Week 8 Go 结论。P1-01 完成上述五项并重新终审前，Week 8 仍为 No-Go。
