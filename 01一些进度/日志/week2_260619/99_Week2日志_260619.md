# Day 1 记录：确定 Week2 骨架、Decimal 方案与验收标准

日期：2026-06-19

今天完成 Week2 Day1 设计收口：确定项目分层骨架、选定 `decimal.js` 与 `DecimalString → Decimal → 格式化展示` 策略，写好 Week2 验收标准，并统一日志命名和文档产出规范。项目路线同步收束为论文核心账本原型，保存层正式改为 IndexedDB 先行。

---

# Day 2 记录：落地核心类型与 DecimalMath

日期：2026-06-20

今天完成 Week2 Day2：核心账本类型已落地到 `src/models`，`decimal.js` 已接入，`decimalMath.ts` 已提供统一十进制计算入口，并通过 `test:decimal`、lint 和 build。PR #2 已合并进 `main`，已删除合并后的功能分支。下一步进入 Day3：写买入持仓与加权平均成本计算。

---

# Day 3 记录：整理 5 条 Trade 样例，合并买入 / 卖出计算口径

日期：2026-06-22

今天先把 Week2 Day3 的方向重新校准：原计划只写买入持仓与均价计算，但因为 Week1 的第 5 条样例已经调整为 ADA 卖出，所以 Day3 不再只做买入，而是把 `buy + sell` 作为 `positionCalculator v1` 的最小闭环。当天产出 `03A_W2-5条样例Trade数据.md`，把 5 条自然语言交易人工整理成完整 `Trade[]`，并保留 `rawText` 作为未来 NLP 解析入口；同时产出 `03B_W2-positionCalculator-v1设计说明.md`，锁定 `Trade[] -> Position[]`、`costBasis = Σ trade.totalValue`、手续费第一版不进成本、卖出按卖出前平均成本结转成本并计算 `realizedPnl`。本日不做自然语言自动解析、不接页面、不碰 IndexedDB 和加密链路。

---

# Day 4 记录：落地 positionCalculator v1，并补代码学习文档

日期：2026-06-23

今天在 `LocalFirstTradingLedger` 的功能分支 `zhennn/w2-position-calculator-v1` 上落地 `src/calculators/positionCalculator.ts` 和对应测试：计算器按 `occurredAt` 排序交易，按 `assetSymbol` 聚合仓位，买入增加 `quantity / costBasis`，卖出按平均成本结转 `soldCostBasis` 并累计 `realizedPnl`，同时保留超卖和零仓位卖出的防御性检查。验证已跑通 `npm run test:positions`、`npm run test:decimal`、`npm run lint`、`npm run build`。由于代码理解成本较高，今天又补了 `04A_W2-positionCalculator代码逐行学习笔记.md`，把 `import type`、`type`、`Map`、`Trade[]`、`Position[]`、买入逻辑、卖出逻辑和平均成本计算逐行拆开解释。下一步应先读懂测试文件 `positionCalculator.test.ts`，再决定是否提交注释改动、合并功能分支。

---

# Day 5 记录：整理仓库状态并提交 positionCalculator PR

日期：2026-06-23

今天完成 Week2 Day5 的收尾整理：外层文档仓库已提交并推送到 GitHub，旧分支已清理干净；源代码仓库补充了 `positionCalculator.ts` 注释提交，并将 `zhennn/w2-position-calculator-v1` 推送成 PR #3。当前计算器相关验证已通过，下一步是在 GitHub 检查并合并 PR。

---

# Day 6 记录：完成 TradeValidator 与 Vitest 核心验收

日期：2026-06-24

今天完成 Week2 剩余不可推迟项。先统一 5 条交易为“4 买 1 卖”，修正旧版 ADA `251.2006 / 62` 验收数据；随后设计并实现 `tradeValidator`，以 `unknown + ValidationResult` 接收不可信输入，使用稳定错误码拦截非法类型、资产不存在、数量/价格/总额非正数、负手续费、成交金额冲突和卖出超持仓。金额与持仓运算全部复用 `decimalMath`，Calculator 原有超卖异常继续作为防御层。

测试侧建立唯一 golden fixture，证明 5 条交易、ADA 多次买入和部分卖出、`realizedPnl`、容差边界、等量清仓及非法交易不进入 Calculator。安装 Vitest 4.1.9，将 DecimalMath、positionCalculator、Validator 测试统一迁移到 `npm test`，删除 `node:assert`、临时 `tsc` 脚本和 `.tmp-tests` 流程。最终 3 个测试文件共 36 项通过，lint、build 全绿，Git 工作区干净。下一步进入 Step 9：实现最新价格、市值和未实现盈亏。
