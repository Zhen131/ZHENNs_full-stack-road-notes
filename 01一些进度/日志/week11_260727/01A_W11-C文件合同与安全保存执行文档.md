# Week 11 C 文件合同与安全保存执行文档

日期：2026-07-28
状态：待用户确认和执行，尚未授权修改源码
本批目标：FILE-001、FILE-002、FILE-004、FILE-005
事实基线：源码仓库 `main`，HEAD `0026304337bfc775e76d91d38c2322c4e9e3fd2a`；行为基线 `1b5894872e6e09669058ccca2a1e3653172b6b4e`，其后只修改 README
来源文件：`000_W11-规划文件关系与批次命名规则.md`、`00A_W11-产品战略重构已确认共识.md`、`00B_W11-总需求快照与待办清单.md`、`00C_W11-NLP前预计开发批次清单.md`、`00-当前开发状态.md`、源码 README 与本批相关源码和测试

## 结论

本批建立 `.lftl` 第一版合同、用户新建或明确选择唯一 C、`current + previous` 双代完整账本保存，以及关闭写入流后的强制复读。

本批不让 C 正式取代 IndexedDB；正式接管属于 FILE-003，留给后续批次。过渡期不得双写：进入 C 会话时只允许 C Repository 接收该会话的账本保存，现有 IndexedDB Repository 继续作为尚未迁移用户的既有路径，但不能同步保存同一 C 的第二份官方账本。

## 一、前置闭包

```text
BASE-008（已完成）
→ FILE-001
→ FILE-002
→ FILE-004
→ FILE-005
```

- 四个目标的全部传递前置均已完成或已选入本批。
- 本批没有必须先解决的 DEC。
- 00C 已将这四项目标登记为预计第一批 `01A`。
- 源码仓库工作树在生成本文前为干净状态，没有 C 文件持久化相关的未提交修改。

## 二、当前事实与本批目标状态

| 维度 | 当前事实 | 本批目标 |
| --- | --- | --- |
| 正式持久化 | 正式持久化仍是 IndexedDB V2 | 增加独立的 `.lftl` 文件路径，但不在本批完成全局接管 |
| 加密 | 已有 PBKDF2-SHA-256 600,000 次派生、AES-256-GCM、128-bit tag、每次加密新 IV 和固定顺序 AAD | 复用密码派生与 AES-GCM 原语，为 `.lftl` 建立独立 AAD；不得复用 IndexedDB V2 envelope 合同 |
| 账本校验 | 已有完整 `validateLedgerData(...)`，覆盖 schema、实体、Decimal、ID、引用和交易时间线 | current 和 previous 解密后分别通过同一 Validator |
| 资源保护 | 已有独立 ResourcePolicy：8 MiB、集合数量和字符串长度上限 | 每个解密后的完整 `LedgerData` 分别执行 ResourcePolicy；文件读取前还要有由双代密文上界推导的 C 容器字节保护 |
| 保存链 | `usePersistentLedger` 已有串行保存、dirty、失败重试、旧 Repository 结果失效和切换保护 | C Repository 的 `save()` 只有在关闭写入流并复读同一 C 全部通过后才 resolve |
| 原子替换 | IndexedDB Adapter 可原子替换 whole-blob；B 恢复只有 Repository 保存成功后才替换 React state | 复用“候选先校验、存储成功后才发布成功状态”的编排，不复用 B 或 IndexedDB 的 envelope |
| 文件能力 | `.lftl` 尚未实现，当前源码没有文件选择、句柄 Adapter 或 C Repository | 用户明确新建或选择一个 `.lftl`；程序只绑定这一个句柄 |

本文描述的是后续执行目标，不代表以上 `.lftl` 能力已经存在。

## 三、四个目标的精确范围

### FILE-001：固定 `.lftl` 第一版加密文件合同

- 对应 00A：§4.4、§4.5。
- 目标：建立严格、可运行时校验的 UTF-8 加密 JSON 合同，分开文件、加密和账本三种版本。
- 可复用能力：`cryptoEncoding.ts` 的 Base64URL；现有 PBKDF2 / AES-GCM 参数；`validateLedgerData(...)`；当前固定字段顺序 AAD 测试方式。
- 新增或调整职责：
  - 新增 `.lftl V1` 类型、常量、严格外壳 Validator、解密 payload Validator 和固定顺序 AAD builder。
  - 新增加密 / 解密一代完整账本的文件专用服务。
  - 只复用加密原语，不复用 `StoredLedgerEnvelopeV2`、`formatVersion: 2` 或其 AAD。
- 通过线：
  - 外层只包含解锁和验证必需的技术字段与密文。
  - `fileFormatVersion`、`cryptoVersion`、`ledgerSchemaVersion` 含义独立。
  - file identity、版本、KDF、cipher、IV 和 revision 技术字段全部进入 AES-GCM AAD。
  - 外层业务信息和未知字段均被拒绝；解密后只得到 `savedAt + LedgerData`。
- 明确不做：文件格式迁移、多 cryptoVersion、账本 schema 迁移、B 合同修改。

### FILE-002：用户可以新建或明确选择唯一正式 C

- 对应 00A：§4.3、§7.2。
- 目标：通过系统文件选择器新建或选择一个 `.lftl`，为新 C 生成一次稳定随机 `fileId`，本次会话只绑定用户明确选中的句柄。
- 可复用能力：现有 Access Gate 的检查、设置密码、解锁、操作互斥和“未解锁不挂载 Dashboard”边界。
- 新增或调整职责：
  - 新增文件选择与句柄 Adapter，隔离 `showSaveFilePicker`、`showOpenFilePicker`、`getFile()` 和 writable stream。
  - 新增 C 访问 Controller，负责 create / open / unlock，成功后返回文件 Repository 会话。
  - Access Gate 只调用 Controller，不直接读取句柄、解析 JSON、派生密钥或写文件。
- 通过线：
  - 取消选择是无副作用结果，不创建空 C、不切换 Repository、不显示保存成功。
  - 新建只操作 save picker 返回的一个句柄；打开只操作 open picker 返回的一个句柄。
  - 不扫描目录、不登记副本、不按文件名合并、不自动制造 C1 / C2 / C3。
  - 文件名可以变化；`fileId` 才是文件身份。相同文件名且 `fileId` 不同不能被当成同一 C。
- 明确不做：记住上次句柄、权限重连、移动或删除后的重开、单标签页锁、外部 revision 冲突处理。

### FILE-004：C 保留真实的当前版与上一版

- 对应 00A：§4.5、§6、§7.2。
- 目标：同一个 C 内只保留最近两代完整加密账本。
- 可复用能力：现有加密服务的新 IV 机制、整账 Repository 边界、完整 LedgerData Validator。
- 新增或调整职责：
  - C Repository 持有最近一次已验证的外壳和解密账本。
  - 首次创建写入 `current + previous:null`。
  - 每次真实修改生成新 `revisionId`、新 IV 和新 current；原 current 原样成为 previous；更老一代丢弃。
- 通过线：
  - current 和 previous 都是完整 `LedgerData` 密文，不是补丁。
  - 两代可独立认证、解密和校验。
  - 新 current 的 `parentRevisionId` 指向 previous 的 `revisionId`。
  - 重试同一未保存 mutation 不制造一份假的新历史；同一候选保存意图复用已经生成的 revision、IV 和密文，直到确认成功或被更新候选取代。
- 明确不做：上一版恢复 UI、长期历史、增量日志、Git 式分支或副本管理。

### FILE-005：关闭写入流后复读同一个 C 才能报成功

- 对应 00A：§6、§7.3。
- 目标：把“写入完成”与“保存成功”分开；只有同一句柄关闭 writable stream 后重新读取并完整验证，Repository `save()` 才 resolve。
- 可复用能力：`usePersistentLedger` 的保存队列、dirty / retry、Repository generation 失效和成功状态；B 恢复的“保存成功后才替换页面”编排。
- 新增或调整职责：
  - C Repository 在写前验证候选，在写后重新读取同一句柄。
  - 复读核对 fileId、预期 revision、parent 链、两代 AAD、解密 payload、Validator、ResourcePolicy 和候选内容一致性。
  - 失败一律 reject；由现有 Hook 保持 dirty、显示错误并允许安全重试。
- 通过线：
  - writable stream 未关闭时绝不显示“已保存”。
  - readback 尚未完成时仍是 saving。
  - 任一核对失败都不能推进 `persistedVersion`。
  - 只有复读 current 与原候选完整一致、previous 与保存前已验证 current 完整一致时才成功。
- 明确不做：自动恢复 previous、静默回退、跨标签页抢锁、重新连接权限。

## 四、明确不在本批

本批不实现或承诺以下目标：

- FILE-003：C 正式取代 IndexedDB。
- FILE-006：上一版恢复 UI 和用户确认。
- FILE-007：多标签页单写入者及外部 revision 冲突。
- FILE-008：再次打开、权限失效和重新连接。
- FILE-009：完整核心密码生命周期重构。
- FILE-010：清空 C。
- B 历史导入、错误报告或可疑重复。
- 手续费与净盈亏。
- 交易或价格的安全编辑。
- 首页与设置 UI 重构。
- 历史 K 线。
- NLP。
- iCloud 自动同步或冲突处理。
- Tauri、Keychain、Touch ID、Face ID 或移动端。

允许预留清晰接口，但不得通过“顺手补齐”提前完成或宣称完成上述目标。

## 五、`.lftl` 第一版合同

### 5.1 固定类型边界

后续实现可以按当前项目命名习惯调整类型名，但数据边界必须等价于：

```ts
type LedgerFileV1 = {
  fileFormatVersion: 1;
  fileId: string;
  crypto: {
    cryptoVersion: 1;
    kdf: {
      name: "PBKDF2";
      hash: "SHA-256";
      iterations: 600000;
      saltBase64Url: string;
    };
    cipher: {
      name: "AES-GCM";
      keyLength: 256;
      tagLength: 128;
    };
  };
  current: EncryptedLedgerGenerationV1;
  previous: EncryptedLedgerGenerationV1 | null;
};

type EncryptedLedgerGenerationV1 = {
  revisionId: string;
  parentRevisionId: string | null;
  ledgerSchemaVersion: 1;
  ivBase64Url: string;
  ciphertextBase64Url: string;
};

type DecryptedLedgerPayloadV1 = {
  savedAt: string;
  ledgerData: LedgerData;
};
```

固定规则：

- 文件扩展名是 `.lftl`；内容是 UTF-8 JSON，不是普通 `.json` B，也不是自定义二进制。
- 第一版固定 `fileFormatVersion: 1`、`cryptoVersion: 1`、`ledgerSchemaVersion: 1`。
- KDF 固定 PBKDF2-SHA-256、600,000 次、16-byte 随机 salt。
- cipher 固定 AES-256-GCM、12-byte 独立 IV、128-bit tag。
- 同一个 C 的两代共享同一 salt 和会话密钥；每个新 generation 使用新 IV。
- `fileId` 只在新建 C 时随机生成一次；移动或重命名不改变它。
- `revisionId` 每个真实 generation 随机生成；第一代 `parentRevisionId = null`。
- `savedAt` 使用注入时钟生成的严格带时区 ISO datetime，并位于密文内。
- `LedgerData` 是完整事实集合；`Position[]`、图表数据、估值模式、筛选状态、K 线和 Agent 状态不得进入 payload。

当前项目以 `globalThis.crypto.randomUUID()` 和可注入 `generateId` 生成业务随机 ID。文件实现应沿用“生产随机生成、测试注入依赖”的方式；ID 在合同中保持不透明，不编码文件名、时间、路径或账本业务信息。

### 5.2 三种版本不得混淆

| 合同 | 当前或目标值 | 所属系统 |
| --- | --- | --- |
| `StoredLedgerEnvelopeV2.formatVersion` | `2` | 当前 IndexedDB record |
| `.lftl fileFormatVersion` | `1` | 新 C 文件外壳 |
| `.lftl cryptoVersion` | `1` | C 的 KDF、cipher 与 AAD 合同 |
| generation `ledgerSchemaVersion` | `1` | 解密后的 `LedgerData` 业务结构 |

`StoredLedgerEnvelopeV2.formatVersion = 2` 与 `.lftl fileFormatVersion = 1` 是两套独立版本系统，不存在升级或降级关系。不得把 `StoredLedgerEnvelopeV2` 扩展成 union 后交给文件 Adapter，也不得把 C V1 写入 IndexedDB 固定槽位。

### 5.3 Runtime Validator

文件合同 Validator 必须：

1. 在 JSON.parse 前检查 C 容器字节上限；上限由现有 8 MiB 明文 JSON 文件边界、两份完整 ciphertext 的 Base64URL 膨胀、GCM tag 和有界技术字段机械推导，不得错误沿用“整个 C 仍是 8 MiB”。
2. 拒绝非对象、未知字段、缺字段、额外字段、错误数组 / null 形态和不支持的版本。
3. 校验 salt、IV、ciphertext 是规范 Base64URL，并核对解码字节长度。
4. 对 `fileId`、`revisionId` 和 `parentRevisionId` 使用非空、有界、不可解释的技术 ID 规则；安全上限可复用当前 128 字符 ID 上限。
5. `previous = null` 时要求第一代 current 的 `parentRevisionId = null`。
6. `previous !== null` 时要求：
   - `current.revisionId !== previous.revisionId`；
   - `current.parentRevisionId === previous.revisionId`；
   - current 与 previous 的 IV 不同。
7. 解密 payload 只接受准确的 `savedAt` 和 `ledgerData`；`savedAt` 必须是严格带时区 ISO datetime。
8. `generation.ledgerSchemaVersion` 必须等于解密后 `ledgerData.schemaVersion`。
9. current 和 previous 解密后分别执行 `validateLedgerData(...)` 与 `evaluateLedgerResourcePolicy(...)`。

### 5.4 AAD 固定合同

每一代使用固定字段顺序创建 AAD，等价结构为：

```ts
type LedgerFileGenerationAadV1 = {
  fileFormatVersion: 1;
  fileId: string;
  crypto: {
    cryptoVersion: 1;
    kdf: {
      name: "PBKDF2";
      hash: "SHA-256";
      iterations: 600000;
      saltBase64Url: string;
    };
    cipher: {
      name: "AES-GCM";
      keyLength: 256;
      tagLength: 128;
    };
  };
  generation: {
    revisionId: string;
    parentRevisionId: string | null;
    ledgerSchemaVersion: 1;
    ivBase64Url: string;
  };
};
```

AAD builder 必须从已校验字段重新构造新对象，再按固定键顺序 UTF-8 编码；不得直接 stringify 不可信输入对象。

`current` / `previous` 的槽位名称不进入 generation AAD。原因是成功保存时原 current 必须原样移动为 previous，不能为了换槽位重新使用旧 IV 加密，也不能制造一代假的新 revision。槽位关系由严格外壳规则保证：新 current 的 `parentRevisionId` 必须等于 previous 的 `revisionId`；删除、交换或伪造槽位都会因 AAD 或 revision 链失败。

密文内的 `savedAt` 和 `ledgerData` 由 AES-GCM 密文本身认证。`ciphertextBase64Url` 不重复进入 AAD。

## 六、双代安全保存状态机

### 6.1 主数据流

```text
已加载 current
→ 形成完整候选 LedgerData
→ Validator
→ ResourcePolicy
→ 生成新 revision 和新 IV
→ 加密完整候选版
→ 原 current 成为 previous
→ 写入同一个 C
→ 关闭写入流
→ 重新打开并读取同一个 C
→ 核对 fileId、revision 链、AAD、current 和 previous
→ 分别解密并完整校验
→ 核对复读 current 等于候选、previous 等于保存前 current
→ 全部通过后 Repository.save() 才 resolve
→ usePersistentLedger 才推进 persistedVersion 并报告已保存
```

### 6.2 三种代际结果

| 操作 | current | previous |
| --- | --- | --- |
| 首次创建 300 笔账本 | 300 笔，revision A，parent `null` | `null` |
| 第一次真实新增 BTC | 301 笔，revision B，parent A | 原 300 笔 revision A |
| 再次真实新增 ETH | 302 笔，revision C，parent B | 原 301 笔 revision B |

- current 和 previous 都是完整账本，不是增量补丁。
- 第三次及以后保存只保留最近两代；更老 generation 不再进入 C。
- “真实修改”以 Hook 的 mutation version 和候选完整内容为准；同一失败保存的重试不是新修改。
- 首次创建可以接收空初始账本或完整 300 笔候选账本；生产新建与未来 B 导入如何安排首次候选由后续批次决定，本批不得实现 B 导入。

### 6.3 失败与重试

- Validator 或 ResourcePolicy 失败：不得打开 writable stream，旧 C 字节、已验证 envelope 和 persistedVersion 全部不变。
- 加密失败：不得写入，旧 C 不变。
- write 或 close 失败：Adapter 必须 abort / 失败退出，不能把部分内容发布为有效 C；旧可用文件是否原样保留必须由 Adapter 自动化测试和真实 Chrome 文件测试证明。
- close 后复读失败：不得显示“已保存”，不得更新内存中的“最后已验证 generation”，页面保持 dirty 并进入可重试错误状态。
- current 可用而 previous 不可用：本批判定保存失败并报告上一版不可用，不允许成功；不进入 FILE-006 恢复 UI。
- current 不可用而 previous 可用：只识别“上一版可用”并返回明确恢复所需状态；不得自动加载 previous、替换页面或声称最新已保存。
- current 与 previous 都不可用：阻断打开或保存，不得创建空账本覆盖。

同一 mutation 的重试必须幂等：

1. 保存意图生成后保留预期 fileId、base revision、新 revision、IV、ciphertext 和完整序列化候选。
2. 重试先读同一 C：
   - 已是预期新 revision：重新完成全部 readback 验证，成功后直接确认，不再生成一代。
   - 仍是保存前 base revision：重写同一保存意图，再关闭和复读。
   - 是其他 revision 或其他 fileId：拒绝覆盖并要求重新打开；不得把 FILE-007 的完整并发方案顺手做进来。
3. 新 mutation 到来时，先确定磁盘上一个保存意图是否已经完整可验证，再决定以上一代还是原 base 形成新候选；不得跳过一份已实际成功的 generation。

### 6.4 成功核对清单

Repository `save()` resolve 前必须同时成立：

- 复读来自创建 writable stream 的同一个 `FileSystemFileHandle`。
- 外壳 exact validator 通过。
- `fileId` 等于会话绑定的 fileId。
- current revision 等于本次预期 revision。
- 首次创建时 current parent 为 null；后续保存时 current parent 等于保存前 current revision。
- 有 previous 时，previous revision 等于 current parent。
- current / previous revision 和 IV 关系合法。
- current 和 previous 分别通过 AES-GCM 认证和解密。
- 两份 payload 的时间与 schema 合法。
- 两份 `LedgerData` 分别通过 Validator 与 ResourcePolicy。
- current 与本次候选逐字段一致。
- previous 存在时，与保存前最后已验证 current 逐字段一致；首次创建时 previous 必须为 null。

## 七、建议源码落点

以下地图以当前实际目录职责为基础。文件名可在执行前按同一职责做小幅调整，但不得改变分层。

| 文件或模块 | 后续职责 | 本批边界 |
| --- | --- | --- |
| 新增 `src/encryption/ledgerFileContract.ts` | `.lftl V1` 类型、常量、exact runtime validator、payload validator、固定顺序 AAD builder | 不导出或复用 IndexedDB `formatVersion: 2` |
| 新增 `src/encryption/ledgerFileCrypto.ts` | 由同一 passphrase / salt 派生会话 key；按 generation AAD 加解密完整 payload | 只复用 PBKDF2 / AES-GCM 原语和 Base64URL，不复用 V2 envelope |
| 保留 `src/encryption/cryptoEnvelope.ts` 与 `webCryptoEncryptionService.ts` | 继续服务 IndexedDB V2 | 如提取共享 crypto primitive，必须保证现有 V2 字节合同和测试不变 |
| 新增 `src/adapters/ledgerFileHandleAdapter.ts` | 系统 open/save picker、扩展名过滤、取消语义、读取字节保护、同句柄 `getFile()`、createWritable / write / close | 不扫描目录、不保存句柄元数据、不管理复制件 |
| 保留 `src/adapters/storageAdapter.ts` 与 `indexedDbStorageAdapter.ts` | 继续保存 IndexedDB `StoredLedgerEnvelopeV2` | 不把接口扩大成 V2 / C union |
| 新增 `src/repositories/ledgerFileRepository.ts` | 实现完整 C load / create / save、双代移动、pending save intent、关闭后复读和错误映射 | `save()` 未通过 readback 不得 resolve；本批不实现清空 C |
| 保留 `src/repositories/ledgerRepository.ts` | 继续服务当前 IndexedDB；其接口和错误分层可作为文件 Repository 的上层形状参考 | 不把 DefaultLedgerRepository 改成同时写两个后端 |
| 新增 `src/composition/ledgerFileAccessController.ts` | 协调新建、选择、读取外壳、设置 / 输入密码、创建文件 Repository 会话 | 页面不能拿到 CryptoKey、明文 payload 或底层写流 |
| 调整 `src/components/security/LedgerAccessGate.tsx` | 增加“新建 C / 选择 C”最小入口，显示取消、错误和解锁状态；成功后只接收 Controller 返回的会话 | 不做设置页重构、记住上次文件、权限重连或立即锁定 |
| 最小调整 `src/components/dashboard/DashboardShell.tsx` | 接收会话存储类型 / capability；C 会话的成功文案只能由 Repository readback 成功触发 | C 会话隐藏或禁用 clear，避免提前实现 FILE-010 |
| 复用 `src/hooks/usePersistentLedger.ts` | 继续负责串行 mutation、dirty、retry、Repository generation 和切换保护 | 不读取 File handle，不解析 `.lftl`；只有必要时增加不改变语义的错误 / capability plumbing |
| 复用 `src/validators/ledgerDataValidator.ts` 与 `resourcePolicy.ts` | 分别校验两代解密账本 | 不把文件外壳规则塞进业务 Validator |
| 参考 `src/backup/backupEnvelope.ts` 与 `BackupControls.tsx` | 参考读取前字节检查、严格解析、取消 / 旧异步结果失效和“写成功后才替换页面” | 不复用 `BackupEnvelopeV1` 作为 C，也不在本批实现 B 导入 |

### 页面允许与禁止

页面只允许：

- 请求 Controller 新建或选择 C。
- 提交密码 / 确认密码。
- 接收结构化的取消、等待、解锁、保存失败和恢复所需状态。
- 将 Controller 返回的单一 Repository 会话交给现有 Dashboard / Hook。

页面不得：

- 直接调用 `showOpenFilePicker`、`showSaveFilePicker`、`createWritable` 或 `File.text()`。
- 直接构造 `.lftl` JSON、AAD、revision、IV 或 ciphertext。
- 持有可导出的密钥。
- 在 C 保存之外再调用 IndexedDB Repository 镜像写入。
- 以文件名判断文件身份。

### 防止提前完成 FILE-003

- 当前 IndexedDB Access Controller、V2 envelope 和 Repository 保留，既有用户路径不在本批迁移或删除。
- C 会话和 IndexedDB 会话互斥；同一 Dashboard 只接收一个 Repository。
- 选择 C 后，该会话的 mutation 只写 C；不得双写 IndexedDB。
- 本批不把完整 C 或完整 LedgerData 复制进 IndexedDB，也不持久化“第二份官方账本”。
- 本批不承诺下次启动自动回到 C，不清理已有 IndexedDB record，不宣布 IndexedDB 已淘汰。
- 若 `LedgerRepository.clear()` 的现有接口迫使文件 Repository提供方法，该方法在本批必须明确返回不支持，并由 capability 阻止页面调用；不得借机实现 FILE-010。

## 八、错误语义

本批固定错误类别、责任边界和用户语义，不预先锁死最终错误码名称。

| 错误类别 | 责任层 | 用户可见语义 |
| --- | --- | --- |
| 用户取消文件选择 | Adapter / Controller | 正常取消；不创建、不切换、不报“文件损坏” |
| 不支持的扩展名 | Adapter / Controller | 请选择 `.lftl`；不读取为 B 或普通 JSON |
| 不支持的外壳版本 | 合同 Validator | 当前版本不能打开；不得迁移或覆盖 |
| 文件结构错误 | 合同 Validator | 文件不是合法 C；不得尝试宽松修复 |
| 加密参数不支持 | 合同 Validator | 当前版本不支持该加密合同；不得降级参数 |
| AAD 认证 / ciphertext 解密失败 | crypto / Repository | 文件认证失败；在解锁场景与密码错误使用不泄露细节的统一提示 |
| 密码错误 | Controller | “密码错误或文件认证失败”；不写文件、不暴露认证 oracle |
| current 校验失败 | Repository | 最新版不可用，不进入 Dashboard / 不报告保存成功 |
| previous 校验失败 | Repository | 上一版不可用，本次保存不成功；FILE-006 恢复 UI 尚未实现 |
| fileId 不一致 | Repository | 复读的不是当前会话绑定的 C；拒绝继续 |
| revision 链不一致 | Repository | 文件版本关系变化或损坏；拒绝旧状态覆盖 |
| LedgerData Validator 失败 | Repository | 账本结构或业务时间线不合法；候选不写入 |
| ResourcePolicy 失败 | Repository | 账本或文件超过安全边界；候选不写入 |
| 写入失败 | Adapter / Repository | 原候选仍 dirty；不得显示成功 |
| close 失败 | Adapter / Repository | 写入未提交；不得复读为成功 |
| 关闭后复读失败 | Adapter / Repository | 无法确认刚才的文件；保持 dirty，可安全重试 |
| 复读内容与候选不一致 | Repository | 文件没有保存预期账本；拒绝成功 |

结构错误、错误密码和认证失败在日志中可以保留不同内部阶段，但 UI 不显示攻击者可利用的低层加密细节，也不得把任何失败解释成“已经保存”。

## 九、测试与验收设计

### 9.1 纯合同测试

建议文件：`src/encryption/ledgerFileContract.test.ts`

- exact V1 外壳通过；缺字段、额外字段、未知版本、错误 null / object 形态全部拒绝。
- `.lftl fileFormatVersion: 1` 与 IndexedDB `StoredLedgerEnvelopeV2.formatVersion: 2` 同时存在且互不接受。
- salt、IV、ciphertext 的 Base64URL 和字节长度受控。
- `previous:null` 只接受第一代 parent 为 null。
- 有 previous 时要求 current parent 指向 previous revision，并拒绝相同 revision / IV。
- payload 只接受 `savedAt + ledgerData`，拒绝派生字段和未知字段。
- C 外层规范序列化不包含 BTC、ETH、ADA 名称、数量、金额、平台或 savedAt；解密 payload 则必须准确包含密文内的 `savedAt + LedgerData`。
- 双代容器字节上限按公式覆盖两份接近 8 MiB 的合法 plaintext，不把 C 错限为 8 MiB；超上界在 JSON.parse 前拒绝。

### 9.2 加密与 AAD 测试

建议文件：`src/encryption/ledgerFileCrypto.test.ts`

- 同一密码和 salt 可分别认证、解密 current 与 previous。
- 错误密码、不同 salt、ciphertext 篡改和错误 tag 全部失败。
- 对 fileFormatVersion、fileId、cryptoVersion、KDF、salt、cipher、tagLength、revisionId、parentRevisionId、ledgerSchemaVersion、IV 逐项修改后，使用被修改字段形成的 AAD 解密必须失败；不支持值在正式入口还应先被合同 Validator 拒绝。
- AAD 字节顺序固定，并明确不包含人类可读业务字段。
- 同一会话只派生一次 key；两代共享 salt，但每个真实 generation 使用不同 IV。
- 同一保存意图重试只复用同一 plaintext / revision / IV，不允许同一 IV 加密不同 plaintext。

### 9.3 Adapter 测试

建议文件：`src/adapters/ledgerFileHandleAdapter.test.ts`

- open / save picker 只返回用户选中的一个句柄。
- 用户取消不调用 createWritable，不留下应用创建的空 C。
- `.json` 或其他扩展名不被当成 `.lftl`。
- 超限文件在读取完整文本和 JSON.parse 前拒绝。
- write 完成但 close 未完成时 readback 不开始。
- write、close、getFile 或 text 失败分别向上返回不同阶段。
- 写入 / close 失败时 fake atomic handle 的旧字节保持不变。
- close 后复读确实使用同一个绑定句柄，不按文件名重新搜索。
- 旧的异步读取在取消、选择新句柄或组件卸载后不能发布结果，复用现有 BackupControls 的 token / lifecycle 测试模式。

### 9.4 Repository 与保存状态机测试

建议文件：`src/repositories/ledgerFileRepository.test.ts`

建立确定性的 300 笔合法 fixture，交易分布覆盖 BTC、ETH、ADA，使用固定时钟、固定 ID 序列和固定随机字节：

1. 创建 300 笔 C 后：
   - current 解密为 300 笔；
   - previous 为 null；
   - current parent 为 null；
   - write、close、readback 全部发生一次后才 resolve。
2. 新增一笔 BTC 后：
   - current 为 301 笔；
   - previous 为原 300 笔；
   - 新 current parent 等于 300 版 revision。
3. 再新增一笔 ETH 后：
   - current 为 302 笔；
   - previous 为原 301 笔；
   - 300 版不再进入文件。
4. current 与 previous 分别独立认证、解密、Validator 和 ResourcePolicy 通过。
5. 三次真实 generation 的 revision 和 IV 两两不同，salt 保持一致。
6. 传入非法 LedgerData 或超 ResourcePolicy 候选时，Adapter 零写入且原 C 字节不变。
7. 加密失败、write 失败、close 失败时不得 resolve，最后已验证 generation 不变。
8. close 后 readback 失败、current 失败或 previous 失败时不得 resolve。
9. fileId、预期 revision、parent 关系或候选内容任一不一致时拒绝成功。
10. 同一失败保存重复 retry 不新增 revision；若第一次实际写入成功但第一次 readback 暂时失败，第二次验证同一 revision 后成功。
11. 同名句柄但不同 fileId 不能满足“重新读取同一个 C”的身份核对。
12. 解密 payload 只含四个 `LedgerData` 集合；`Position[]`、图表数据、K 线和 UI 状态不存在。

### 9.5 React 交互测试

建议扩展：`LedgerAccessGate.test.tsx`、`DashboardShell.interaction.test.tsx`，必要时新增文件访问组件测试。

- “新建 C”和“选择 C”只调用高层 Controller。
- 用户取消后仍停留在原状态，没有空 C、Repository 切换或成功提示。
- 文件读取 / 解锁期间不挂载 Dashboard。
- 保存 pending 和 writable close 期间只显示 saving，不显示 saved。
- readback 完成前没有“已保存”；readback 全部通过后才显示。
- readback 失败时页面保持 dirty、显示可重试错误；重试成功后才清 dirty。
- C 会话只调用 C Repository，IndexedDB Adapter 零写入。
- C 会话不显示或不可调用 clear；现有 IndexedDB clear 回归不变。
- Repository 切换沿用 dirty 阻断；不得因选择新 C 丢失未保存状态。
- 同名不同 fileId 的重新连接结果不能被视为当前会话原 C。
- 现有 IndexedDB setup / unlock、密码遮蔽、B 导入、dirty、retry 和旧 Repository 结果失效测试继续通过。

### 9.6 后续真实 Chrome 验收

自动化通过后，NND 准备真实 Chrome 文件验收：

- 在普通本地目录新建真实 `.lftl`，确认实际文件可见且外层无 BTC / ETH / ADA 业务明文。
- 使用正式 UI 新增可人工核对的 BTC、ETH、ADA 小数据，观察每次都在 close + readback 后才出现成功。
- 关闭页面后从系统文件选择器重新选择该 C，使用正确密码验证完整数据；错误密码零写入。
- 模拟取消 picker、只读 / 写入失败、close 后读取失败，确认没有虚假成功。
- 将浏览器网络切断后继续打开和保存普通本地 C，证明云端可用性不是前置。
- 若用户把 C 选在 iCloud Drive，只陈述“已写入用户选择的位置”，不得声称上传、同步或冲突处理已完成。
- 300 / 301 / 302 双代结果由本批自动化 Repository 测试提供；B 历史导入尚未实现，不得为了浏览器造一个临时导入入口。

“功能正常”不是通过线；每项必须保存调用次数、预期 revision、两代笔数、错误状态和磁盘 / fake handle 结果。

## 十、直接适用安全红线

| GATE | 本批落实方式 |
| --- | --- |
| GATE-003 | C 会话只写 C，不镜像写 IndexedDB；既有 IndexedDB 路径保留但不宣称已接管 |
| GATE-004 | 只绑定用户选中的一个句柄；不扫描、不复制、不编号、不管理其他 C |
| GATE-006 | Repository `save()` 在 close 后 readback 全部通过前不 resolve；Hook 不推进 persistedVersion |
| GATE-019 | iCloud Drive 只表述为用户选择的文件位置，不表述为自动同步或冲突处理 |
| GATE-022 | 普通本地 C 的打开和保存不依赖网络、iCloud 或市场数据 |
| GATE-023 | 8 月 3 日只是目标日期；不得删除 AAD、双代、readback 或失败保护换取按时完成 |

NND 最终仍要复核全部 GATE；直接适用映射不能成为顺手开发其他 GATE 对应功能的理由。

## 十一、后续实施顺序

1. 固定 `.lftl V1` 类型、严格 runtime validator、payload validator 和容器字节上限公式。
2. 建立固定顺序 AAD，以及一代完整 payload 的加密 / 解密；保持 IndexedDB V2 字节合同不变。
3. 建立文件选择与句柄 Adapter，先关闭取消、扩展名、读取上限、write / close 和同句柄复读测试。
4. 建立首次 C 创建：注入 fileId、revision、IV、时钟和初始完整 LedgerData，得到 `current + previous:null`。
5. 建立双代候选保存和幂等 retry：原 current 原样进入 previous，只保留最近两代。
6. 把 close 后 readback、身份、revision、两代解密、Validator、ResourcePolicy 和候选一致性全部纳入 C Repository `save()`。
7. 在 Access Gate 接入最小“新建 / 选择 C”状态；页面只拿 Controller 结果，C 会话不双写 IndexedDB、不开放 clear。
8. 补齐合同、AAD、Adapter、Repository、Hook / React 回归，完成 300 → 301 → 302 固定验收。
9. 运行定向测试后，再运行全量 `npm test`、typecheck、lint、production build 和源码 `git diff --check`，准备独立 Chrome 验收材料。

不得一次重写整个持久化系统；每一步都必须保持现有 IndexedDB、B、Access Gate 和 dirty 回归可独立验证。

## 十二、四项目标最终通过线

| 目标 | 本批 NNB 可报告的实现证据 | NND 最终通过线 |
| --- | --- | --- |
| FILE-001 | 合同、AAD、crypto 测试和源码 diff | exact validator、三种版本分离、全部技术字段认证、业务明文不外泄 |
| FILE-002 | picker / Controller / React 测试 | 可新建或明确选择一个 C；取消无副作用；fileId 身份正确；不扫描副本 |
| FILE-004 | 300 / 301 / 302 Repository 测试 | current / previous 为相邻完整账本，独立认证解密，新 IV / revision，只有最近两代 |
| FILE-005 | close / readback 故障注入、dirty / saved 交互测试 | 同句柄复读全部通过前绝不成功，任一失败保持 dirty 且不污染最后已验证状态 |

本文完成不等于四项目标完成。只有后续 01B 实现、01C 独立代码审查和 01D 独立测试全部通过，才允许回写 00B。

## 十三、Git 与交付边界

后续执行阶段必须遵守：

- 源码修改只能进入独立源码仓库 `01一些进度/产出/LocalFirstTradingLedger/`。
- 规划、实现记录、审查与验收文档只能进入工作区根仓库。
- 两个仓库不得混合暂存或提交。
- `01B` 只记录实际实现、测试、源码提交和已知风险，不是最终通过证明。
- 只有 `01C` 与 `01D` 都通过后，才能回写 00B 的 FILE-001、FILE-002、FILE-004、FILE-005。
- 本轮只生成 01A，不生成 02A；若未来 01C / 01D 不通过，修复按 000 使用当时下一个可用批次编号。
- 不推送、不合并，除非用户在后续执行任务中另行授权。
