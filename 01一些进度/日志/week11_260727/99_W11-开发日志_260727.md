# Week 11 开发日志

记录范围：2026-07-27 至 2026-07-31
状态：第一批已由 01D-6 最终独立 PASS；第二批 02D 为 BLOCKED；第三批阶段一和浏览器可执行范围内的阶段二已完成，但严格事务保证受纯浏览器平台能力阻塞

## 2026-07-27

### 7 月 27 日完成

- 重新审查账本愿景、当前源码能力、原 Week 11–13、NLP 课程与 DEIK.AI Challenge。
- 确认旧路线并非错误，但不再适合作为当前执行入口。
- 将原 Week 11–13 计划移入 `Z99Archive/`，归档提交为 `c8054a4`。
- 建立产品战略共识和本周开发日志，提交为 `9dcdf38`。
- 对照完整讨论补齐真实启用动机、离线导出、终端启动、跨端复用、只读手机、稀疏行情和沟通规则。

### 关键决策

- 8 月 3 日真实使用优先级固定为：C 加密文件取代 IndexedDB、B 完整历史导入、手续费进入净盈亏、交易与价格安全纠错、首页与设置重构。
- 任一结果未通过即顺延，不用降低安全要求换取日期。
- NLP 课程与比赛复用同一自然语言录入链，但不能替代账本真实使用和论文 benchmark。
- 长期顺序保持 Mac 写入、iPhone 先只读，再处理多设备写冲突。

## 2026-07-28

### 7 月 28 日完成

- 补录 `.lftl` 文件外壳、三种版本、目标 TypeScript 合同、文件身份、revision、保存后复读、历史导入预检、可疑重复和密码迁移等确认结论。
- 补录 Binance-first 历史 K 线、独立可重建缓存、日 / 周 / 月 K、单资产详情页和离线降级边界。
- NLP 只冻结近期产品方向：本地 `qwen3:8b`、两个 Agent、第一版纯文字、受约束的多行批量录入和两道确认门；课程实验细节继续后移。
- 上述共识补录以提交 `597e43a` 收口。
- 将共识文件从 `01A` 重命名为长期基线 `00A`。
- 新建 `000_W11-规划文件关系与批次命名规则.md`，固定 00 层与 01、02、03 等开发批次的关系。
- 00A 重命名和文件治理规则以提交 `4a1c606` 独立收口。

### 文件流程

```text
000 文件规则
→ 00A 产品与架构共识
→ 00B 总需求快照与待办清单
→ 同时生成 NNA 开发执行文档 + NNC 独立测试审查执行文档
→ 开发 AI 执行 NNA，生成 NNB 开发执行报告
→ 另一独立 AI 执行 NNC，生成 NND 独立测试审查报告
→ 通过后回写 00B
```

### 边界与下一入口

- 本轮没有修改 `LocalFirstTradingLedger` 源码。
- `00B_W11-总需求快照与待办清单.md` 已生成，建立提交为 `4f73752`；表格与图谱视图同步提交为 `59c0d0f`。
- 00B 已完成只读全链路审查：63 个普通目标、7 个 DEC、23 个 GATE 均无重复、缺号、缺失前置或循环依赖，11 个已完成目标有当前源码与测试证据。
- 审查发现的两个 P1 已修复：当前开发状态不再声称 00B 缺失；FILE-003 必须等待 FILE-005 的安全保存与复读链。
- 审查发现的两个 P2 已修复：IMPORT-001 明确覆盖当前 C→明文 B 的主动灾难恢复导出；00B 增加目标范围与直接适用 GATE 映射。
- 源码 README 已用提交 `0026304` 明确区分 Week 10 已实现能力和 Week 11 外层规划，源码行为没有变化。
- 用户已选择第一批 `FILE-001、FILE-002、FILE-004、FILE-005`；01A 已执行并生成 01B，源码修改仍保持未暂存、未提交。
- 发现原 000 把 NNC / NND 都写成结果文件，与用户预期不符；现已统一为 A/C 输入、B/D 报告，未来 NNA 与 NNC 必须同时生成。
- 旧流程生成的 01C 静态审查结果移入 `Z99Archive`，不冒充 01D；活动 01C 改为独立测试审查目标模式执行文档。
- 下一步由另一独立 AI 执行 01C；无论 PASS、FAIL 或 BLOCKED，都生成 01D，且不顺手修源码。

### 7 月 28 日下午至晚间：独立审查

- 独立 AI 已按 01C 执行 T0–T9；正式源码全程只读，对抗测试只写入完整临时副本。
- 已生成 `01D_W11-C文件合同与安全保存独立测试审查报告.md`，最终结论为 `FAIL`，不是 `BLOCKED`。
- 正式仓库的指定测试分别为 39、33、78 项通过；全量 48 个测试文件、434 项测试通过，typecheck、lint、production build 和 diff-check 通过。
- P0 已复现：磁盘 `crypto.kdf.salt` 漂移后，创建、普通保存和 pending-intent reconcile 可能被错误确认；Hook 可显示 `saved`，但文件随后无法用正确密码重新打开。
- P1 已复现：慢 A / 快 B、取消 A、Gate 卸载和成功创建新文件四种场景中，晚到的旧文件选择都可能重新绑定旧句柄。
- P2 已确认：正式测试没有覆盖 salt 三路径、Hook 保存状态门和四个 deferred stale-selection 场景，因此 434 项测试全绿仍未发现 P0 / P1。
- T8 没有进入真实 Chrome：T0–T7 已产生确定性 FAIL，按 01C 的强失败优先规则停止；这不是环境阻塞。
- 审查前后正式源码文件清单、字节数和冻结哈希一致；临时副本已删除，没有把独立测试写回正式源码。
- 已向产品负责人用通俗语言解释：P0 是“软件说已保存，但文件可能重开失败”，P1 是“用户选了 B，晚到的 A 可能重新顶回来”，P2 是“正式测试没有覆盖前两类死角”。

### 当日收尾决定

- 产品负责人确认 7 月 28 日不再继续修复 P0 / P1 / P2，避免疲劳状态下修改安全保存链。
- 第一批不得回写 00B，不得表述为完成，也不得合入源码 `main`。
- 当前工作现场已用中文提交 `2e5c7e5`（`阶段：保存C文件首批候选与独立验收失败`）保存并推送到 `origin/zhennn/w11-c-file-core-save`；源码功能分支本地与远端一致，未合入 `main`。
- 明日入口是同时准备下一组 `02A + 02C`，开发与独立复验继续分离；只有后续独立报告 PASS 才允许回写 00B。

## 2026-07-29

### 01D 内部返工

- [x] 用户明确选择不占用 02 编号，使用 01D-1 作为 F-01、F-02、F-03 的修复输入。
- [x] 01D-2 已记录开发侧 PASS：完整 metadata 精确比较、operation generation、Gate cleanup 和 9 项正式对抗测试完成。
- [x] 开发侧与本轮独立重跑均为 48 个测试文件、443 项测试；typecheck、lint、production build、diff-check 通过。
- [x] 修复源码独立复验前后 8 个候选文件 SHA-256 一致。

### 独立复验新结论

- 01D-3 判定 `FAIL`，不是 `BLOCKED`。
- 已确认 F-01、F-02、F-03 修复有效；FILE-002、FILE-004、FILE-005 的本轮技术项通过。
- 新的强制失败是 FILE-001：`LedgerFileCrypto` 仍调用读取 IndexedDB V2 常量的共享 `deriveLedgerKey(...)`，C V1 的实际 KDF 没有由自己的版本常量显式决定。
- 该问题此前已在归档静态预审记录为 P2-02，但正式 01D 漏记，01D-1 / 01D-2 因而没有修复。
- 当前两套参数相同，现有 443 项测试可通过；风险在于未来只改 V2 参数时，C V1 会在 `cryptoVersion` 不变的情况下改变解锁合同。
- T8 因 T2 已确定强制 FAIL 而未进入真实 Chrome，不记环境阻塞。

### 当时守门（已由 01D-6 PASS 解除）

- [x] 00B 的 FILE-001、FILE-002、FILE-004、FILE-005 已在 01D-6 PASS 后回写。
- [x] 源码候选已合入并推送 `main`；本地与远端功能分支均已删除。
- [ ] FILE-006、FILE-007 的 02A / 02C 暂不开始。
- [x] 01D-4 / 01D-5 已完成 C V1 与 IndexedDB V2 的 KDF 参数解耦和开发侧自验。
- [x] 01D-6 已由独立 AI 执行并判定 PASS。

### 第二轮返工：01D-4 / 01D-5

- [x] 新增格式无关的参数化密钥派生 primitive；底层不读取 C 或 IndexedDB 的版本常量。
- [x] C V1 setup / unlock 显式使用 `LEDGER_FILE_V1_CONSTANTS`，不再导入 V2 加密服务。
- [x] IndexedDB V2 保留原 `deriveLedgerKey(...)` 接口，并显式使用 `LEDGER_CRYPTO_CONSTANTS`。
- [x] 用两组不同 iterations / keyLength 证明两套合同可独立传参。
- [x] 加入修复前固定 C V1 密文 fixture，修复后仍能用原密码解密。
- [x] F-01～F-03 的 salt、Hook、picker、双代和 reconcile 正式对抗场景无回归。
- [x] 开发侧定向测试、49 个测试文件 / 445 项测试、typecheck、lint、production build 与 diff-check 全部通过。

### 01D-6 前边界

- 上述结果是开发侧 PASS，不是独立验收；01D-3 的历史 FAIL 不被覆盖或改写。
- 01D-6 必须只读复验当前完整候选，重新核对 KDF 解耦、固定 fixture、IndexedDB V2 兼容、F-01～F-03 和全部质量门。
- 01D-6 复验前后必须记录源码哈希；执行者不得修复源码、回写 00B、提交或推送。

### 01D-6 最终独立复验与产品合同更正

- [x] Chrome `Default` profile 的扩展阻塞已解除，真实 Google Chrome、系统 open / save picker 和宿主可见 `.lftl` 已完成验收。
- [x] 新建外层只含五个技术字段；BTC / ETH / ADA、业务字段和测试备注均未以明文出现。
- [x] 正式 UI 完成 BTC、ETH、ADA 三次保存；错密零写入，正密重开恢复完整数据。
- [x] open / save picker 取消均无副作用；可见磁盘 300 / 301 / 302 为 `300/null`、`301/300`、`302/301`。
- [x] 独立重跑 49 files / 445 tests、typecheck、lint、production build 和 whitespace 全部通过；正式源码前后聚合 SHA-256 一致。
- [x] 产品负责人明确更正同名保存规则：系统已显示替换确认且用户主动确认后，覆盖属于正常 save picker 行为；此前“非空目标一律拒绝”是过严合同，不是代码 P0。
- [x] T8.6 以源码依赖扫描和确定性自动化证明 C 核心不依赖网络；没有把未执行的整机断网冒充真实实测。
- [x] 01D-6 最终判定 **PASS**，P0 / P1 / P2 / P3 与 BLOCKED 均为 0。
- [x] 00B 已回写 FILE-001、FILE-002、FILE-004、FILE-005；已完成目标由 11 增至 15。
- [ ] FILE-003、FILE-006～010 保持未完成；02A / 02C 等待用户明确选择，不自动开始。

历史 01D 与 01D-3 的 FAIL 继续保留，分别证明当时真实存在的 salt / picker 与 KDF 版本耦合问题；最终状态以 01D-6 为准。

### Git 收口状态

- [x] 完整源码候选重新运行 49 files / 445 tests、typecheck、lint、production build 与 whitespace，均通过。
- [x] 13 个返工路径已提交为 `203a020`（`修复：完成C文件安全保存返工并通过验收`）。
- [x] 功能分支已在本地合入源码 `main`，合并提交为 `b3366a7`（`合并：完成C文件合同与安全保存`）。
- [x] 源码 `main` 已推送到 `origin/main`，远端接收范围为 `1b58948..b3366a7`。
- [x] 远端 `zhennn/w11-c-file-core-save` 已删除；本地同名分支确认已合入后删除。
- [x] `01D-1`～`01D-6`、00 层、01A～01D、当前状态和本日志纳入同一文档收口提交并推送。

## 2026-07-30

### 第二批 C 文件安全闭环与正式接管（开发侧）

- [x] Gate 0 后，源码从 `b3366a7` 创建并保持在候选分支 `zhennn/w11-c-safety-takeover`；整个开发过程没有 stage、commit、push、merge 或切回源码 `main`。
- [x] 完成 `FILE-006`：只有 current 确实损坏、previous 独立验证有效时才显示恢复；取消零写入，确认恢复必须 write、close、同句柄 readback 与完整两代验证。
- [x] 完成 `FILE-007`：以 `isSameEntry()` 判断真实文件，加入跨页面 session lease、短时写锁和写前复读；复制件不误锁，外部 R302 变化时旧页面停止保存。
- [x] 用户选择 dirty 立即锁定方案 B：有未保存内容时先提醒，可重试保存或明确放弃后锁定；密码、会话和文件占用按 quiesce / drain 安全收口。
- [x] 完成 `FILE-008 / FILE-009`：IndexedDB 只保存最小 connection record；刷新、权限、重连、错误文件、旧异步结果、密码生命周期和 release retry 均 fail closed。
- [x] 用户选择旧完整 IndexedDB 账本退出方案 A：只读验证旧账本，迁移到新的 C，完成 write、close、readback、身份和内容验证后，用户再以固定文本确认删除旧记录。
- [x] 完成 `FILE-003`：production 正常入口只使用 C；旧 `ledger:v1` 只保留迁移读取与原 envelope 条件删除能力，connection record 与 legacy 完整账本分离，取消、失败、source changed 和卸载晚到均保留 legacy。
- [x] 完成 `FILE-010`：只允许 ready C 的固定文本清空；无参 clear、伪造 / 过期 / 跨 session 授权、quiesce 后调用、hydration error 都零写入拒绝。清空强制生成新 current，旧 current 成为 previous，并在 close-readback 完整验证后才显示成功。
- [x] 额外关闭两项跨阶段安全问题：未同步 claim 的 clear execution context 会立即失效，晚到微任务不能写入；删除授权在 Gate 卸载后晚到时不会触发 legacy 删除。

### 开发侧验证与交接

- [x] 阶段一、二、三定向测试分别为 `6 files / 166 tests`、`9 files / 218 tests`、`8 files / 184 tests`，均通过。
- [x] 01 批强制回归 `8 files / 168 tests` 通过；全量 `51 files / 596 tests` 通过；typecheck、lint、production build 与 whitespace 检查通过。
- [x] 唯一合并开发记录为 `02B_W11-C文件安全闭环与正式接管合并执行记录.md`，记录了六个 FILE、两个用户决定、所有命令结果、22 个修改路径和 4 个新增路径。
- [ ] 未运行真实 Chrome；这不是独立验收 PASS。后续必须在第三个全新目标模式聊天中，以同一未提交候选 working tree 执行 02C。

## 2026-07-31

### 今日完成

- [x] 按 `03A_W11-B历史导入预检与整本原子替换合并执行文档.md` 执行第三批；没有执行 03C，也没有生成 03D。
- [x] 阶段一 `IMPORT-001 / IMPORT-003 / IMPORT-004` 完成：支持完整明文 B 导出、一次性只读预检、准确错误路径、完整 Markdown 报告和可疑重复分组；硬错误、取消和只读预检均保持 C 零写入。
- [x] 阶段二 `IMPORT-002` 的正常链完成：只允许本次会话中新建且仍为空的 C，导入前冻结 B identity、候选 LedgerData、fileId、revision 和 session generation；成功后 C 恰好等于完整 B，不做合并、跳过或自动去重。
- [x] 写入链保存导入前 C 的完整 bytes、fileId、current revision 和 previous；新 candidate 完成 write、close、readback、解密、Validator、parent / previous lineage 和页面替换后才报告成功。
- [x] post-close 异常只在磁盘仍是本事务精确 candidate 时尝试恢复旧 C；无法证明安全恢复时进入 `IMPORT_RECOVERY_BLOCKED`，停止当前 repository 的读取和后续写入，不伪造成功。
- [x] 导入生命周期与当前 repository、session、generation、fileId、revision、B identity、选择代次和 React commit phase 绑定；取消、锁定、卸载、换文件和迟到异步结果不能继续写入旧 C。
- [x] 新增 5 组永久虚构 fixture，覆盖合法 300 笔、第 147 笔硬错误、混合错误与重复、1001 条报告截断和仅可疑重复。
- [x] 最终全量为 `55 files / 698 tests`，typecheck、lint、Next production build、whitespace 和调试残留扫描通过。
- [x] 真实 Chrome 正常链通过：macOS 系统 Save/Open picker、新 `.lftl`、合法 300 笔 B、导入后文件 hash / revision、锁定和同一 C 重开均取得证据。
- [x] 源码候选以提交 `7dcd297`（`实现：补齐历史导入预检与可恢复写入`）保存在 `zhennn/w11-b-import-atomic`，提交时工作树干净。

### 遇到的问题与阻塞

- 第三批不是卡在普通代码缺口，也不是再次卡在文件选择器权限。真正阻塞是 03A 的绝对合同：任何 post-close 失败都必须在函数返回前恢复并复读为旧 C。
- 纯浏览器单文件模型只能在 JavaScript 仍存活、权限仍有效、磁盘仍是本事务 candidate 时补偿。Chrome 在 close 后崩溃、权限永久丢失或外部程序先改写时，网页没有操作系统文件锁、compare-and-swap、进程死亡后的回滚或启动恢复能力。
- 当前候选已达到“浏览器可执行范围内的安全上限”：成功链可验证，失败链尽力补偿，无法证明时 fail closed；但这不能冒充严格合同已经完成。
- 因此 03A 整批记为 **BLOCKED**。阶段一和阶段二正常链的开发事实保留，03C / 03D 未执行，00B 不回写，也不把源码进入 `main` 表述成独立 PASS。
- 02D 的历史 BLOCKED 同样保留；本轮真实 Chrome 正常链证据不能反向补成第二批完整独立验收。

### 已确认的下一方向

- 产品负责人确认把 Mac 桌面端提前为下一阶段的优先方向；当前纯浏览器版本固定为过渡候选，手机端暂缓，不与桌面迁移同时实施。
- 严格桌面方向至少需要同目录临时文件、文件与目录同步、原子替换、稳定文件锁和启动恢复；不能理解为简单增加三个 API。
- 当前尚未决定桌面端的完整技术边界、事务合同、浏览器数据迁移、打包发布和批次拆分。下一步先进入计划模式，用通俗方式重新确认“已经走了多远、现在在哪里、原 07～20 计划哪些保留、哪些改顺序”。
- 在计划模式形成正式结论前，不生成新的桌面 NNA / NNC，不实施 Tauri、iCloud、Keychain、Touch ID、Windows 或移动端代码。

### Git 收口结果

- [x] 源码 README 以 `0c72d47`（`文档：同步历史导入与桌面迁移边界`）提交在候选分支。
- [x] 源码候选经 `084ae7d`（`合并：收口历史导入浏览器候选`）无冲突合入 `main`。
- [x] 合并后的最终 `main` 重跑 `55 files / 698 tests`、typecheck、lint、production build 和 whitespace，全部通过。
- [x] 源码 `084ae7d` 已推送至 `origin/main`；本地与远端 ahead / behind 为 `0 / 0`，工作树干净。
- [x] 已合并的本地 `zhennn/w11-b-import-atomic` 已删除；远端本来就没有该候选分支，刷新后只有 `origin/main`。
- [x] 03A、03B、03C、当前状态、00A、00C、本日志和源码 README 已同步为同一口径：实现可合入，03B 仍为 BLOCKED，桌面端只是下一阶段方向。
